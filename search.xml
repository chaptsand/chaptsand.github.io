<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Arch linux安装记录</title>
    <url>/posts/654b0e13.html</url>
    <content><![CDATA[<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/archlinux-vert-dark.svg" ></p>
<p>官网: <a class="link"   href="https://archlinux.org/" >https://archlinux.org<i class="fas fa-external-link-alt"></i></a><br>Wiki: <a class="link"   href="https://wiki.archlinux.org/" >https://wiki.archlinux.org<i class="fas fa-external-link-alt"></i></a></p>
<p>新手推荐直接按这个来，非常完整，并且有视频教程 <a class="link"   href="https://archlinuxstudio.github.io/ArchLinuxTutorial/#/" >https://archlinuxstudio.github.io/ArchLinuxTutorial/#/<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="1-1-镜像下载"><a href="#1-1-镜像下载" class="headerlink" title="1.1.镜像下载"></a>1.1.镜像下载</h4><p><a class="link"   href="https://archlinux.org/download/" >https://archlinux.org/download/<i class="fas fa-external-link-alt"></i></a><br><a class="link"   href="http://mirrors.bfsu.edu.cn/archlinux/iso/" >http://mirrors.bfsu.edu.cn/archlinux/iso/<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="1-2-刻录启动盘"><a href="#1-2-刻录启动盘" class="headerlink" title="1.2.刻录启动盘"></a>1.2.刻录启动盘</h4><ul>
<li>Linux 下用 dd 命令进行刻录</li>
</ul>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo dd bs=4M if=/path/to/archlinux.iso of=/dev/sdx status=progress oflag=sync</span><br></pre></td></tr></table></figure>

<ul>
<li>Windows<br>Rufus：<u><a class="link"   href="https://rufus.ie/" >https://rufus.ie<i class="fas fa-external-link-alt"></i></a></u></li>
</ul>
<h4 id="1-3-关闭-Secure-Boot"><a href="#1-3-关闭-Secure-Boot" class="headerlink" title="1.3.关闭 Secure Boot"></a>1.3.关闭 Secure Boot</h4><h4 id="2-1-连接网络"><a href="#2-1-连接网络" class="headerlink" title="2.1.连接网络"></a>2.1.连接网络</h4><p>因为网卡不能驱动，使用有线网络连接</p>
<h4 id="2-2-检查网络连接"><a href="#2-2-检查网络连接" class="headerlink" title="2.2.检查网络连接"></a>2.2.检查网络连接</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ping www.baidu.com</span><br></pre></td></tr></table></figure>

<h4 id="2-3-更新系统时钟"><a href="#2-3-更新系统时钟" class="headerlink" title="2.3.更新系统时钟"></a>2.3.更新系统时钟</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">timedatectl set-ntp true</span><br></pre></td></tr></table></figure>

<h4 id="更换国内镜像源"><a href="#更换国内镜像源" class="headerlink" title="更换国内镜像源"></a>更换国内镜像源</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure>

<p>放在最上面的行是会使用的更新源，我这里北外的镜像源效果最好</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Server = https://mirrors.bfsu.edu.cn/archlinux/$repo/os/$arch</span><br><span class="line">Server = https://mirrors.ustc.edu.cn/archlinux/$repo/os/$arch</span><br><span class="line">Server = https://mirrors.tuna.tsinghua.edu.cn/archlinux/$repo/os/$arch</span><br></pre></td></tr></table></figure>

<h4 id="2-4-分区"><a href="#2-4-分区" class="headerlink" title="2.4.分区"></a>2.4.分区</h4><table>
<thead>
<tr>
<th>分区</th>
<th>挂载点</th>
<th>大小</th>
</tr>
</thead>
<tbody><tr>
<td>EFI 分区</td>
<td>&#x2F;efi</td>
<td>800M</td>
</tr>
<tr>
<td>swap分区</td>
<td>none</td>
<td>16G</td>
</tr>
<tr>
<td>根目录</td>
<td>&#x2F;</td>
<td>剩余全部</td>
</tr>
</tbody></table>
<p>我这里没有单独分home分区，有需要的可以自己调整</p>
<h5 id="2-4-1-将磁盘转换为-gpt-类型（对于新硬盘）"><a href="#2-4-1-将磁盘转换为-gpt-类型（对于新硬盘）" class="headerlink" title="2.4.1.将磁盘转换为 gpt 类型（对于新硬盘）"></a>2.4.1.将磁盘转换为 gpt 类型（对于新硬盘）</h5><p>这里的<code>nvme0n1</code>是我硬盘的代号，要以<code>lsblk</code>看到的实际情况为准</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsblk                       <span class="comment">#显示分区情况</span></span><br><span class="line">parted /dev/nvme0n1         <span class="comment">#执行parted，进行磁盘类型变更</span></span><br><span class="line">(parted)mktable             <span class="comment">#输入mktable</span></span><br><span class="line">New disk label <span class="built_in">type</span>? gpt    <span class="comment">#输入gpt 将磁盘类型转换为gpt 如磁盘有数据会警告，输入yes即可</span></span><br><span class="line">(parted)quit                <span class="comment">#最后quit退出parted命令行交互</span></span><br></pre></td></tr></table></figure>

<h5 id="2-4-2-使用-cfdisk-命令对磁盘分区"><a href="#2-4-2-使用-cfdisk-命令对磁盘分区" class="headerlink" title="2.4.2.使用 cfdisk 命令对磁盘分区"></a>2.4.2.使用 cfdisk 命令对磁盘分区</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cfdisk /dev/nvme0n1</span><br></pre></td></tr></table></figure>

<p>new &gt; 创建分区，输入大小回车</p>
<p>type &gt; 选择分区类型</p>
<p>完事后使用<code>fdisk -l</code> 检查分区情况</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">设备                起点       末尾      扇区   大小 类型</span><br><span class="line">/dev/nvme0n1p1      2048    1640447   1638400   800M EFI 系统</span><br><span class="line">/dev/nvme0n1p2   1640448   35194879  33554432    16G Linux swap</span><br><span class="line">/dev/nvme0n1p3  35194880 1000215182 755305103 460.2G Linux 文件系统</span><br></pre></td></tr></table></figure>

<h4 id="2-5-格式化"><a href="#2-5-格式化" class="headerlink" title="2.5.格式化"></a>2.5.格式化</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkfs.ext4 /dev/nvmen1p3             <span class="comment">#格式化根目录分区</span></span><br><span class="line">mkswap /dev/nvme0n1p2               <span class="comment">#创建swap分区</span></span><br><span class="line">mkfs.vfat /dev/nvme0n1p1            <span class="comment">#格式化efi分区</span></span><br></pre></td></tr></table></figure>

<h4 id="2-6-挂载"><a href="#2-6-挂载" class="headerlink" title="2.6.挂载"></a>2.6.挂载</h4><p>挂载是有顺序的，需要从根目录开始挂载</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mount /dev/nvme0n1p3 /mnt           <span class="comment">#挂载根目录</span></span><br><span class="line"><span class="built_in">mkdir</span> /mnt/efi                      <span class="comment">#创建efi文件夹</span></span><br><span class="line">mount /dev/nvme0n1p1 /mnt/efi       <span class="comment">#挂载efi分区</span></span><br><span class="line">swapon /dev/nvme0n1p2               <span class="comment">#挂载swap分区</span></span><br></pre></td></tr></table></figure>

<h4 id="2-7-安装系统"><a href="#2-7-安装系统" class="headerlink" title="2.7.安装系统"></a>2.7.安装系统</h4><ul>
<li>基础包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pacstrap /mnt base base-devel linux linux-headers linux-firmware</span><br></pre></td></tr></table></figure>

<ul>
<li>功能性软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pacstrap /mnt dhcpcd iwd vim bash-completion</span><br></pre></td></tr></table></figure>

<h4 id="2-8-生成-fstab-文件"><a href="#2-8-生成-fstab-文件" class="headerlink" title="2.8.生成 fstab 文件"></a>2.8.生成 fstab 文件</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</span><br></pre></td></tr></table></figure>

<p> <code>cat /mnt/etc/fstab</code>复查一下 &#x2F;mnt&#x2F;etc&#x2F;fstab 确保没有错误</p>
]]></content>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Debian配置iptables防火墙</title>
    <url>/posts/d3794e8f.html</url>
    <content><![CDATA[<p>之前服务器上一直用的Cent OS，默认没有开启防火墙，不需要放行80 443等端口就可以http|https访问.最近换到Debian9之后发现https访问网页服务被拦截了，就研究了一下iptables防火墙的配置</p>
<span id="more"></span>

<h2 id="系统要求："><a href="#系统要求：" class="headerlink" title="系统要求："></a>系统要求：</h2><p>Debian6.x&#x2F;7.x及以上版本</p>
<h2 id="配置步骤"><a href="#配置步骤" class="headerlink" title="配置步骤"></a>配置步骤</h2><h3 id="1-首先确定你的系统已经安装Iptables"><a href="#1-首先确定你的系统已经安装Iptables" class="headerlink" title="1.首先确定你的系统已经安装Iptables"></a>1.首先确定你的系统已经安装Iptables</h3><p>打开SSH终端，输入 <code>whereis iptables</code><br>如果输出以下信息，说明你的系统已经安装了iptables</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">root@debian:~# whereis iptables</span><br><span class="line">iptables: /sbin/iptables /etc/iptables.rules /usr/share/iptables /usr/share/man/man8/iptables.8.gz</span><br><span class="line">root@debian:~#</span><br></pre></td></tr></table></figure>
<p>如果没有的话，先安装iptables<br><code>apt-get install iptables</code><br>###2.安装成功后查看iptables端口开放情况###</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">root@debian:~# iptables -L</span><br><span class="line">Chain INPUT (policy ACCEPT)</span><br><span class="line">target     prot opt source               destination         </span><br><span class="line"></span><br><span class="line">Chain FORWARD (policy ACCEPT)</span><br><span class="line">target     prot opt source               destination         </span><br><span class="line"></span><br><span class="line">Chain OUTPUT (policy ACCEPT)</span><br><span class="line">target     prot opt source               destination         </span><br><span class="line">root@debian:~#</span><br></pre></td></tr></table></figure>
<p>默认情况没有任何配置</p>
<h3 id="3-配置iptables"><a href="#3-配置iptables" class="headerlink" title="3.配置iptables"></a>3.配置iptables</h3><p>编辑配置文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vim /etc/iptables.rules</span><br></pre></td></tr></table></figure>
<p>加入以下内容</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">Generated by iptables-save</span></span><br><span class="line">*filter</span><br><span class="line">:INPUT ACCEPT [0:0]</span><br><span class="line">:FORWARD ACCEPT [0:0]</span><br><span class="line">:OUTPUT ACCEPT [0:0]</span><br><span class="line">-I INPUT -p tcp --dport 8888 -j ACCEPT</span><br><span class="line">-I INPUT -p udp --dport 8888 -j ACCEPT</span><br><span class="line">-A INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT</span><br><span class="line">-A INPUT -p icmp -j ACCEPT</span><br><span class="line">-A INPUT -i lo -j ACCEPT</span><br><span class="line">-A INPUT -m state --state NEW -p tcp --dport 22 -j ACCEPT</span><br><span class="line">-A INPUT -m state --state NEW -p tcp --dport 80 -j ACCEPT</span><br><span class="line">-A INPUT -m state --state NEW -p tcp --dport 443 -j ACCEPT</span><br><span class="line"></span><br><span class="line">-A INPUT -j REJECT --reject-with icmp-host-prohibited</span><br><span class="line">-A FORWARD -j REJECT --reject-with icmp-host-prohibited</span><br><span class="line">COMMIT</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">Completed</span></span><br></pre></td></tr></table></figure>

<p><code>ESC :wq</code>保存退出<br>这只是一个简单的模板，放行了tcp22 80 443端口，以及8888的tcp和udp端口<br>可自行修改</p>
<h3 id="4-加载iptables-rules规则"><a href="#4-加载iptables-rules规则" class="headerlink" title="4.加载iptables.rules规则"></a>4.加载iptables.rules规则</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">root@debian:~# iptables-restore &lt; /etc/iptables.rules</span><br><span class="line">root@debian:~# iptables -L</span><br><span class="line">Chain INPUT (policy ACCEPT)</span><br><span class="line">target     prot opt source               destination         </span><br><span class="line">ACCEPT     udp  --  anywhere             anywhere             udp dpt:8888</span><br><span class="line">ACCEPT     tcp  --  anywhere             anywhere             tcp dpt:8888</span><br><span class="line">ACCEPT     all  --  anywhere             anywhere             state RELATED,ESTABLISHED</span><br><span class="line">ACCEPT     icmp --  anywhere             anywhere            </span><br><span class="line">ACCEPT     all  --  anywhere             anywhere            </span><br><span class="line">ACCEPT     tcp  --  anywhere             anywhere             state NEW tcp dpt:ssh</span><br><span class="line">ACCEPT     tcp  --  anywhere             anywhere             state NEW tcp dpt:http</span><br><span class="line">ACCEPT     tcp  --  anywhere             anywhere             state NEW tcp dpt:https</span><br><span class="line">REJECT     all  --  anywhere             anywhere             reject-with icmp-host-prohibited</span><br></pre></td></tr></table></figure>
<p>可以看到规则以及被加载了</p>
<h3 id="5-设置开机启动"><a href="#5-设置开机启动" class="headerlink" title="5.设置开机启动"></a>5.设置开机启动</h3><p><code>vim /etc/network/if-pre-up.d/iptables</code><br>加入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">/sbin/iptables-restore &lt; /etc/iptables.rules</span><br></pre></td></tr></table></figure>
<p><code>ESC :wq</code> 保存退出</p>
<p><code>chmod +x /etc/network/if-pre-up.d/iptables</code></p>
<p>这样就搞定了，可以重启试试有没有正常加载</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">root@debian:~# reboot</span><br><span class="line">root@debian:~# iptables -L</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Fimo胶片摄影—书房</title>
    <url>/posts/52b4a119.html</url>
    <content><![CDATA[<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/FIMO_1588667331180.JPG"  alt="吊灯"></p>
<span id="more"></span>


<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/FIMO_1588667297361.JPG"  alt="书橱"><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/FIMO_1588666878200.JPG"  alt="笔记本"><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/FIMO_1588666767949.JPG"  alt="笔记本"><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/FIMO_1588668150508.JPG"  alt="百乐78g+"></p>
]]></content>
      <categories>
        <category>图图</category>
      </categories>
      <tags>
        <tag>摄影</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac os 配置 python3环境</title>
    <url>/posts/c4cc58f5.html</url>
    <content><![CDATA[<p>截止Mac os 10.15.4 (Catalina) 版本，系统自带的python环境依然是python2，但我们用到的当然是python3咯。以下是几种在Mac上配置python3环境的方法记录</p>
<span id="more"></span>


<h3 id="0、查看-mac-自带系统版本"><a href="#0、查看-mac-自带系统版本" class="headerlink" title="0、查看 mac 自带系统版本"></a>0、查看 mac 自带系统版本</h3><pre><code>#查看系统自带的python版本
open /System/Library/Frameworks/Python.framework/Versions
#系统当前的python版本
python -V
</code></pre>
<h3 id="1、到官网下载安装程序Python官网"><a href="#1、到官网下载安装程序Python官网" class="headerlink" title="1、到官网下载安装程序Python官网"></a>1、到官网下载安装程序<a class="link"   href="https://www.python.org/downloads/release" >Python官网<i class="fas fa-external-link-alt"></i></a></h3><p>找到适合于Mac安装的程序即可</p>
<h3 id="2、用homebrew安装"><a href="#2、用homebrew安装" class="headerlink" title="2、用homebrew安装"></a>2、用homebrew安装</h3><p>首先当然是要有homebrew咯<br>官网:<a class="link"   href="https://brew.sh/" >homebrew<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="2-1、开始安装"><a href="#2-1、开始安装" class="headerlink" title="2.1、开始安装"></a>2.1、开始安装</h4><p>安装前先搜索一下是否已经存在python3的包：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">brew search python3</span><br></pre></td></tr></table></figure>

<p>已经存在，我们可以直接安装了：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">brew install python3</span><br></pre></td></tr></table></figure>

<h3 id="2-2、如果出现如下报错"><a href="#2-2、如果出现如下报错" class="headerlink" title="2.2、如果出现如下报错"></a>2.2、如果出现如下报错</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Error: An unexpected error occurred during the `brew link` step</span><br><span class="line">The formula built, but is not symlinked into /usr/local</span><br><span class="line">Permission denied @ dir_s_mkdir - /usr/local/Frameworks</span><br><span class="line">Error: Permission denied @ dir_s_mkdir - /usr/local/Frameworks</span><br></pre></td></tr></table></figure>
<p>then</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo mkdir /usr/local/Frameworks</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">再来解决权限问题：</span></span><br><span class="line">sudo chown $(whoami):admin /usr/local/Frameworks</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">手动执行一下安装时未完成的创建连接：</span></span><br><span class="line">brew link python3</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">当前系统下的python3的信息：</span></span><br><span class="line">brew info python3</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">系统当前的python版本。</span></span><br><span class="line">python -V</span><br></pre></td></tr></table></figure>

<p>你会发现出现的还是2.7对不对</p>
<h3 id="3-修改当前系统的默认版本为3-版本"><a href="#3-修改当前系统的默认版本为3-版本" class="headerlink" title="3.修改当前系统的默认版本为3.*版本"></a>3.修改当前系统的默认版本为3.*版本</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">#</span><span class="language-bash">查找python3安装路径</span></span><br><span class="line">brew info python3</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">修改 Mac 系统配置文件bash</span></span><br><span class="line">vi ~/.bash_profile  </span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">如果用zsh</span></span><br><span class="line">vi ~/.zshrc</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">添加配置信息</span></span><br><span class="line">alias python=&quot;/usr/local/bin/python3&quot;</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">编译系统配置文件</span></span><br><span class="line">source ~/.bash_profile</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">zsh</span></span><br><span class="line">source ~/.zshrc</span><br><span class="line"><span class="meta prompt_">#</span><span class="language-bash">系统当前的python版本。</span></span><br><span class="line">python -V</span><br></pre></td></tr></table></figure>
<p>搞定</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Mac OS</tag>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Manjaro安装简记</title>
    <url>/posts/edf26bc3.html</url>
    <content><![CDATA[<h2 id="一、什么是Manjaro"><a href="#一、什么是Manjaro" class="headerlink" title="一、什么是Manjaro?"></a>一、什么是Manjaro?</h2><blockquote>
<p>Manjaro是Linux的发行版本之一，基于 Arch Linux ，是近两年热度最高，最受欢迎的Linux发行版本之一（广告法杀我）</p>
</blockquote>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/01.png"  alt="Manjaro"></p>
<p>官方网站：<a class="link"   href="https://manjaro.org/" >https://manjaro.org/<i class="fas fa-external-link-alt"></i></a></p>
<span id="more"></span>


<p>继承了Arch Linux的众多优点，比Arch友好的多多多多。有很强大的软件库，简洁易用</p>
<p>另外也推荐使用deepin，，符合国人审美且有良好的软件生态，网易云，QQ等好多软件都可以直接安装，是非常适合国人的Linux操作系统。</p>
<blockquote>
<p>此前美国商务部对华为的禁令限制微软对华为提供Windows授权，如果未取得授权，华为也就不能在新发布的电脑里将预装Windows系统。虽然这对华为来说并不是一个好结果，但华为也不会因此丢掉电脑业务。华为或许正在开发完善自己的桌面操作系统，同时国内也有一个现成且优秀的的Linux桌面系统供华为选择，那就是deepin。华为在自己部分笔记本产品中预装了deepin<br>Linux操作系统。比windows版便宜，对于想购机的朋友来说，Linux版其实是非常不错的选择</p>
</blockquote>
<p>deepin官网：<a class="link"   href="https://www.deepin.org/" >https://www.deepin.org/<i class="fas fa-external-link-alt"></i></a></p>
<h2 id="二、安装步骤"><a href="#二、安装步骤" class="headerlink" title="二、安装步骤"></a>二、安装步骤</h2><p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/02.png"  alt="选择桌面版本"></p>
<p>manjaro官方提供三个桌面版本下载：xfce、kde以及gnome,另外还有一些社区版本。今天我们要装的是Manjaro kde</p>
<h3 id="1-下载系统镜像"><a href="#1-下载系统镜像" class="headerlink" title="1.下载系统镜像"></a>1.下载系统镜像</h3><p>推荐从国内开源镜像站下载，下面是华为云<br>manjaro-kde-18.1.5下载链接：<a class="link"   href="https://mirrors.huaweicloud.com/manjaro-cd/kde/18.1.5/manjaro-kde-18.1.5-191229-linux54.iso" >https://mirrors.huaweicloud.com/manjaro-cd/kde/18.1.5/manjaro-kde-18.1.5-191229-linux54.iso<i class="fas fa-external-link-alt"></i></a><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/03.png"  alt="下载完成"></p>
<h3 id="2-刻录安装U盘"><a href="#2-刻录安装U盘" class="headerlink" title="2.刻录安装U盘"></a>2.刻录安装U盘</h3><p><a class="link"   href="https://github.com/pbatard/rufus/releases/download/v3.9/rufus-3.9.exe" >点击下载刻录工具<i class="fas fa-external-link-alt"></i></a></p>
<p>安装后打开，插上空闲U盘一个（8G就可），U盘上的文件先备份，待会会被清空。插上U盘软件就会自动识别到<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/04.png"  alt="刻录安装U盘"><br>选择刚才下载好的iso文件即可<br>其他默认选项不要修改，然后点开始</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/05.png"  alt="正在刻录"><br>刻录时间和U盘性能有关，耐心等等或者下楼喝杯水<br>有一个坑就是，刻录完之后没有提示，只是进度条清零了，这时候不要以为刻录失败了又点一下开始，那就刻录两遍啦</p>
<h3 id="3-调整分区"><a href="#3-调整分区" class="headerlink" title="3.调整分区"></a>3.调整分区</h3><p>使用Windows自带的磁盘管理为Manjaro小姐姐分一个房子<br>我这里直接从D盘分出60个G来给Manjaro<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/06.png"  alt="压缩D盘"></p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/07.png"  alt="Manjaro的新家"><br>分60个GB给Manjaro,就是60*1024 MB<br>然后压缩</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/08.png"  alt="分配磁盘"></p>
<p>然后在如图的60G未分配磁盘上新建一个分区：右键-新建简单卷-分区大小默认-不分配驱动器符号-不要格式化这个卷，完成后是这样</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/09.png"  alt="60G RAW分区"></p>
<p>然后就可以关机了</p>
<h3 id="4-设置BIOS启动"><a href="#4-设置BIOS启动" class="headerlink" title="4.设置BIOS启动"></a>4.设置BIOS启动</h3><blockquote>
<p>不同机型进入BIOS的方法不同，以我的机型为例，开机时按F2（手速要快，多按几下），关闭安全启动，然后调整启动顺序，将USB设备调整到最前面<br>或者开机直接按F10，选择以usb驱动器启动</p>
</blockquote>
<h3 id="5-进入安装界面"><a href="#5-进入安装界面" class="headerlink" title="5.进入安装界面"></a>5.进入安装界面</h3><p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/10.png"  alt="安装界面"></p>
<p>把时区改为上海，键盘默认美式键盘，语言中文简体zh_CN<br>这里如果是N卡用户driver类型选择nofree，进系统会自动安装好显卡驱动<br>然后就选择boot回车，就会启动到manjaro liveCD进行安装</p>
<p>如果只有一个硬盘，或者打算和windows双系统共存装在同一磁盘，就选择安装时取代一个分区（提前分出来的60GB）,只是简单体验的话也不用去管boot &#x2F;home怎么分区了，默认就行</p>
<p>然后装完就可以重启进入系统了</p>
<h3 id="6-修改启动项"><a href="#6-修改启动项" class="headerlink" title="6.修改启动项"></a>6.修改启动项</h3><p>再次进入BIOS，将manjaro启动项调整至第一，重启即可进入manjaro桌面<br>长这个亚子：</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/11.png"  alt="Manjaro Hello"></p>
<h2 id="三、大功告成——只完成了第一步"><a href="#三、大功告成——只完成了第一步" class="headerlink" title="三、大功告成——只完成了第一步"></a>三、大功告成——只完成了第一步</h2><p>先来一个screenfetch，看看我的manjaro配置</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/12.png"  alt="screenfetch"></p>
<blockquote>
<p>换源，配置常用软件以后再写……<br>更要命的是我发现我装好后无线网挂了，没有WIFI选项，这是由于之前换的博通网卡的锅，见<a href="https://www.chaptsand.top/posts/5b71d0ea.html">https://www.chaptsand.top/posts/5b71d0ea.html</a></p>
</blockquote>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/13.png"  alt="update"></p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>One night in Beijing (1)</title>
    <url>/posts/529aa99b.html</url>
    <content><![CDATA[<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/Beijing.png" ></p>
<p> 2019年8月18日下午五点四十三分，我在历经了三十多个小时的火车旅程后到达了北京。</p>
<p>  最后的一个多小时，列车穿过西五环来到了三环内的北京西站，我看着窗外的建筑从砖瓦平房变成耸立的集群大厦。<strong>若非亲眼所见，我不会相信北京也有农村，看起来繁华和他们无关。</strong></p>
<span id="more"></span>

<p>到了站，同学小宇说已经到南广场等我了，过多的出站口给我们的见面增添了一些困难，我在好不容易找到他之后和他一起去了已经找好的民宿。在公交车上，我从一车的普通话里听出了北京话的不一样。<br>小宇是我在大学里交的朋友，黑龙江人。给我印象深刻的是第一次见面自我介绍时说喜欢读书，尤其是喜欢王小波的作品。我猜这个人会比较幽默，而且是带有点淡淡黑色的那种。<br>到了他提前找好的特价民宿。我仔细观察了这房子的成色，楼梯间转角处墙上贴了很多广告，被清理过但是还不如不清理，<strong>清理广告的人肯定不懂美学，不能体现出残缺的美</strong>。这种老式居民楼放在我老家的城市估计有十五年历史。在北京我想肯定是二十年以上了。<br>这个地方是小宇好不容易找到的，两百六一晚，这大概是附近最便宜的住处。屋里陈设很简单，两张床，床占据了屋内三分之二的空间，毕竟这间屋子主要的功能就是让人睡觉。一台可能和我年龄差不多的电视机，我没打开过，听小宇说能看，但是画面比较模糊。比较难得的是屋里装了一台空调，在这易热易流汗的时节，能派上很大用场。<br>我把行李放好，躺在床上准备睡会。小宇说他饿的不行了，就早上喝了一碗粥，一天还没吃饭。看到我来了很高兴，打算带我去王府井大街吃全聚德 。先前他打听了一下价格，两个人吃，加上些乱码七糟的东西差不多300元。在火车上没怎么吃东西的我也是饥肠辘辘，没和他说我不吃鸭。<br>我们坐晚高峰的地铁1号线穿过人民大会堂，到王府井站下车。出地铁，街边的路灯已经亮起。小宇说王府井肯定是以前一个人的名字。我想这人的名字可真难听啊，居然还被用来命名这个地方。<br>我和小宇顺着人流的方向，走在北京繁华的夜色中。各式各样的人说着各式各样话，<strong>也许这一辈子都不会有交集的人，在这一时刻，同成为王府井大街上的一个路人。</strong></p>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
        <tag>旅游</tag>
      </tags>
  </entry>
  <entry>
    <title>OpenCore使用自定义三码教程</title>
    <url>/posts/29bb6c12.html</url>
    <content><![CDATA[<p>如果你是使用别人配置好的EFI文件，那里面写的三码基本上已经被很多人使用了。如果你想登陆apple id的话，有可能会登陆不上</p>
<p>怎样改回自己的三码信息呢？</p>
<span id="more"></span>


<table>
<thead>
<tr>
<th>需要准备的</th>
</tr>
</thead>
<tbody><tr>
<td>SystemSerialNumber:</td>
</tr>
<tr>
<td>MLB:</td>
</tr>
<tr>
<td>SmUUID:</td>
</tr>
<tr>
<td>ROM:</td>
</tr>
</tbody></table>
<p>至于生成随机三码，可以参考这个教程</p>
<p><a href="https://sleele.com/2019/03/21/smbios/"><strong>为自己的黑苹果生成随机三码</strong></a></p>
<p>一、修改OpenCore配置文件config.plist</p>
<p>用<a href="https://github.com/corpnewt/ProperTree"><strong>ProperTree编辑器</strong></a>编辑<code>config.plist</code>，需要Python环境，如果没有<strong>先安装Python</strong><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/propertree.png"  alt="Propertree"></p>
<p><strong>Windows运行ProperTree.bat</strong><br><strong>Mac运行 ProperTree.command</strong></p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/openfile.png"  alt="File-Open"><br>右上角导入config.plist文件<br>找到**【Platfrominfo】<strong>项<br>删除</strong>【Datahub,PlatfromNVRAM,SMBIOS】**三个子项（如果存在）<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/Platfrominfo.png"  alt="Platfrominfo"></p>
<p>然后展开**【Generic】<strong>，<br>修改</strong>【SystemSerialNumber, MLB,SmUUID,ROM】<strong>为自己的<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/sm.png" ><br><strong>SmUUID前面的数可随机生成</strong>，<strong>后十二位</strong>要改为你</strong>网卡的Mac地址**<br>ROM其实就是网卡的Mac地址，<strong>即SmUUID后十二位和ROM要一致</strong></p>
<p>保存退出，替换EFI内的config.plist为你<strong>修改过</strong>的即可</p>
<p>本文参考：<a class="link"   href="https://sleele.com/2019/10/31/opencore-guide/#" >[2019&#x2F;11&#x2F;19更新]OpenCore快速导读<i class="fas fa-external-link-alt"></i></a></p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Hackintosh</tag>
        <tag>黑苹果</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch 学习笔记-Week5</title>
    <url>/posts/81e7205c.html</url>
    <content><![CDATA[<p>这周学习了从零开始使用PyTorch构建一个完整机器学习项目的全过程。通过两个经典案例——PIMA糖尿病<strong>分类</strong>和泰坦尼克号生还预测<strong>分类</strong>，系统地学习了数据处理、模型构建、训练、评估和优化的核心技能。</p>
<h2 id="1-核心概念与工作流"><a href="#1-核心概念与工作流" class="headerlink" title="1. 核心概念与工作流"></a>1. 核心概念与工作流</h2><h3 id="1-1-训练三元组：Epoch-Batch-Iteration"><a href="#1-1-训练三元组：Epoch-Batch-Iteration" class="headerlink" title="1.1 训练三元组：Epoch, Batch, Iteration"></a>1.1 训练三元组：Epoch, Batch, Iteration</h3><p>这是理解模型训练过程的基础。</p>
<ul>
<li><strong>Epoch (周期)</strong>: 指的是整个训练数据集被完整地、系统地过了一遍。</li>
<li><strong>Batch-Size (批次大小)</strong>: 由于数据集太大无法一次性载入内存，我们将其分成小批次（batch）。Batch-Size就是每个批次中包含的样本数量。</li>
<li><strong>Iteration (迭代)</strong>: 模型更新一次权重的过程。每处理一个batch的数据，就完成了一次迭代。</li>
<li><strong>关系</strong>: <code>Iterations_per_Epoch = Total_Samples / Batch_Size</code></li>
</ul>
<h3 id="1-2-标准机器学习工作流"><a href="#1-2-标准机器学习工作流" class="headerlink" title="1.2 标准机器学习工作流"></a>1.2 标准机器学习工作流</h3><p>一个标准的项目遵循以下流程：</p>
<ol>
<li><strong>数据加载与探索 (EDA)</strong></li>
<li><strong>数据预处理与清洗</strong></li>
<li><strong>划分训练集&#x2F;验证集</strong></li>
<li><strong>构建数据管道 (DataLoader)</strong></li>
<li><strong>定义模型架构</strong></li>
<li><strong>训练模型</strong></li>
<li><strong>评估模型性能</strong></li>
<li><strong>（可选）超参数调优</strong></li>
<li><strong>（可选）对新数据进行预测</strong></li>
</ol>
<h2 id="2-数据处理与加载-Pandas-PyTorch"><a href="#2-数据处理与加载-Pandas-PyTorch" class="headerlink" title="2. 数据处理与加载 (Pandas &amp; PyTorch)"></a>2. 数据处理与加载 (Pandas &amp; PyTorch)</h2><h3 id="2-1-探索性数据分析-EDA"><a href="#2-1-探索性数据分析-EDA" class="headerlink" title="2.1 探索性数据分析 (EDA)"></a>2.1 探索性数据分析 (EDA)</h3><p>在接触任何数据集时，首先要了解它。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载数据</span></span><br><span class="line">df = pd.read_csv(<span class="string">&#x27;./datasets/titanic/train.csv&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看前5行</span></span><br><span class="line"><span class="built_in">print</span>(df.head())</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看数据结构、非空值数量和数据类型</span></span><br><span class="line"><span class="comment"># 这是发现缺失值和错误数据类型的关键步骤</span></span><br><span class="line">df.info()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看数值列的统计摘要</span></span><br><span class="line"><span class="built_in">print</span>(df.describe())</span><br></pre></td></tr></table></figure>

<h3 id="2-2-数据预处理"><a href="#2-2-数据预处理" class="headerlink" title="2.2 数据预处理"></a>2.2 数据预处理</h3><p>这是将原始数据转换为模型可用格式的关键步骤。</p>
<h4 id="a-处理缺失值"><a href="#a-处理缺失值" class="headerlink" title="a. 处理缺失值"></a>a. 处理缺失值</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 对于数值型特征，用中位数填充更稳健</span></span><br><span class="line">age_median = df[<span class="string">&#x27;Age&#x27;</span>].median()</span><br><span class="line">df[<span class="string">&#x27;Age&#x27;</span>] = df[<span class="string">&#x27;Age&#x27;</span>].fillna(age_median)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对于类别型特征，用众数填充</span></span><br><span class="line">embarked_mode = df[<span class="string">&#x27;Embarked&#x27;</span>].mode()[<span class="number">0</span>]</span><br><span class="line">df[<span class="string">&#x27;Embarked&#x27;</span>] = df[<span class="string">&#x27;Embarked&#x27;</span>].fillna(embarked_mode)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对于缺失值过多的列，直接删除</span></span><br><span class="line">df = df.drop(<span class="string">&#x27;Cabin&#x27;</span>, axis=<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<h4 id="b-处理类别特征（独热编码）"><a href="#b-处理类别特征（独热编码）" class="headerlink" title="b. 处理类别特征（独热编码）"></a>b. 处理类别特征（独热编码）</h4><p>神经网络只能处理数字，因此文本特征需要转换。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># drop_first=True 用于避免多重共线性，是一种好习惯</span></span><br><span class="line">df = pd.get_dummies(df, columns=[<span class="string">&#x27;Sex&#x27;</span>, <span class="string">&#x27;Embarked&#x27;</span>], drop_first=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>

<h4 id="c-特征选择"><a href="#c-特征选择" class="headerlink" title="c. 特征选择"></a>c. 特征选择</h4><p>删除与预测目标不相关的列。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">df = df.drop([<span class="string">&#x27;PassengerId&#x27;</span>, <span class="string">&#x27;Name&#x27;</span>, <span class="string">&#x27;Ticket&#x27;</span>], axis=<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<h4 id="d-特征缩放-Standardization"><a href="#d-特征缩放-Standardization" class="headerlink" title="d. 特征缩放 (Standardization)"></a>d. 特征缩放 (Standardization)</h4><p>将所有特征缩放到相似的范围，有助于模型更快、更稳定地收敛。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line"><span class="comment"># 先划分数据</span></span><br><span class="line">X_train, X_val, y_train, y_val = train_test_split(X, y, test_size=<span class="number">0.2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 创建scaler对象</span></span><br><span class="line">scaler = StandardScaler()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在训练集上fit_transform</span></span><br><span class="line">X_train = scaler.fit_transform(X_train)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在验证/测试集上只用transform</span></span><br><span class="line">X_val = scaler.transform(X_val)</span><br></pre></td></tr></table></figure>

<h3 id="正确的顺序和做法"><a href="#正确的顺序和做法" class="headerlink" title="正确的顺序和做法"></a>正确的顺序和做法</h3><p>正确的流程应该是严格模拟真实世界：我们只能根据已有的训练数据来学习规律，然后将这些规律应用到未知的测试数据上。</p>
<p><strong>正确的顺序是：1. 先分割，2. 后处理。</strong></p>
<ol>
<li><strong>先分割数据</strong>：将数据集划分为训练集和测试集。</li>
<li><strong>拟合和转换训练集</strong>：在 <strong>仅有训练集</strong> (<code>x_train</code>) 上调用 <code>fit_transform()</code>。这一步会计算出只属于训练集的均值和标准差，并用它们来转换训练集。</li>
<li><strong>仅转换测试集</strong>：使用<strong>同一个</strong>（已经在训练集上拟合好的）scaler，在测试集 (<code>x_test</code>) 上<strong>只调用</strong> <code>transform()</code>。这一步是用训练集的均值和标准差来转换测试集，以确保两边处理标准一致。</li>
</ol>
<h3 id="额外的重要提示：不要标准化标签"><a href="#额外的重要提示：不要标准化标签" class="headerlink" title="额外的重要提示：不要标准化标签"></a>额外的重要提示：不要标准化标签</h3><h3 id="2-3-PyTorch数据管道"><a href="#2-3-PyTorch数据管道" class="headerlink" title="2.3 PyTorch数据管道"></a>2.3 PyTorch数据管道</h3><h4 id="a-自定义-Dataset"><a href="#a-自定义-Dataset" class="headerlink" title="a. 自定义 Dataset"></a>a. 自定义 <code>Dataset</code></h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TitanicDataset</span>(<span class="title class_ inherited__">Dataset</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, features, labels</span>):</span><br><span class="line">        <span class="comment"># 将NumPy数组转换为PyTorch张量</span></span><br><span class="line">        self.features = torch.tensor(features, dtype=torch.float32)</span><br><span class="line">        self.labels = torch.tensor(labels, dtype=torch.float32)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__getitem__</span>(<span class="params">self, index</span>):</span><br><span class="line">        <span class="comment"># 定义如何获取单个样本</span></span><br><span class="line">        <span class="keyword">return</span> self.features[index], self.labels[index]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__len__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="comment"># 返回数据集总长度</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">len</span>(self.features)</span><br></pre></td></tr></table></figure>

<h4 id="b-DataLoader"><a href="#b-DataLoader" class="headerlink" title="b. DataLoader"></a>b. <code>DataLoader</code></h4><p>将<code>Dataset</code>封装成一个可迭代对象，实现批处理和数据打乱。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"></span><br><span class="line">train_dataset = TitanicDataset(X_train, y_train)</span><br><span class="line">train_loader = DataLoader(dataset=train_dataset,</span><br><span class="line">                          batch_size=<span class="number">32</span>,</span><br><span class="line">                          shuffle=<span class="literal">True</span>) <span class="comment"># 打乱数据对训练很重要</span></span><br></pre></td></tr></table></figure>

<h2 id="3-模型构建与训练"><a href="#3-模型构建与训练" class="headerlink" title="3. 模型构建与训练"></a>3. 模型构建与训练</h2><h3 id="3-1-模型定义"><a href="#3-1-模型定义" class="headerlink" title="3.1 模型定义"></a>3.1 模型定义</h3><p>使用<code>nn.Module</code>定义模型结构，用<code>nn.Sequential</code>可以使结构更简洁。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Classifier</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, num_features</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.net = nn.Sequential(</span><br><span class="line">            nn.Linear(num_features, <span class="number">16</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Linear(<span class="number">16</span>, <span class="number">8</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Linear(<span class="number">8</span>, <span class="number">1</span>) <span class="comment"># 输出层，对于BCEWithLogitsLoss，输出原始logit</span></span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="keyword">return</span> self.net(x)</span><br></pre></td></tr></table></figure>

<h3 id="3-2-训练循环"><a href="#3-2-训练循环" class="headerlink" title="3.2 训练循环"></a>3.2 训练循环</h3><p>这是模型学习的核心。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 初始化模型、损失函数和优化器</span></span><br><span class="line">model = Classifier(input_features)</span><br><span class="line">criterion = nn.BCEWithLogitsLoss() <span class="comment"># 适用于二分类</span></span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=<span class="number">0.001</span>)</span><br><span class="line"></span><br><span class="line">model.train() <span class="comment"># 将模型设置为训练模式</span></span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> train_loader:</span><br><span class="line">        <span class="comment"># 1. 前向传播</span></span><br><span class="line">        outputs = model(features)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 2. 计算损失</span></span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 3. 反向传播与优化</span></span><br><span class="line">        optimizer.zero_grad() <span class="comment"># 清空之前的梯度</span></span><br><span class="line">        loss.backward()       <span class="comment"># 计算当前梯度</span></span><br><span class="line">        optimizer.step()      <span class="comment"># 更新权重</span></span><br></pre></td></tr></table></figure>

<h2 id="4-模型评估与实践技巧"><a href="#4-模型评估与实践技巧" class="headerlink" title="4. 模型评估与实践技巧"></a>4. 模型评估与实践技巧</h2><h3 id="4-1-评估循环"><a href="#4-1-评估循环" class="headerlink" title="4.1 评估循环"></a>4.1 评估循环</h3><p>在验证集上检查模型性能，全程不计算梯度。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line">model.<span class="built_in">eval</span>() <span class="comment"># 将模型设置为评估模式</span></span><br><span class="line">all_predictions = []</span><br><span class="line"><span class="keyword">with</span> torch.no_grad(): <span class="comment"># 在此代码块中不计算梯度</span></span><br><span class="line">    <span class="keyword">for</span> features, _ <span class="keyword">in</span> val_loader:</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        predicted = (torch.sigmoid(outputs) &gt; <span class="number">0.5</span>).<span class="built_in">float</span>()</span><br><span class="line">        all_predictions.extend(predicted.numpy())</span><br><span class="line"></span><br><span class="line">accuracy = accuracy_score(y_val, all_predictions)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;验证集上的准确率: <span class="subst">&#123;accuracy*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&quot;</span>)</span><br></pre></td></tr></table></figure>

<h3 id="4-2-Kaggle提交流程"><a href="#4-2-Kaggle提交流程" class="headerlink" title="4.2 Kaggle提交流程"></a>4.2 Kaggle提交流程</h3><p>关键在于对<code>test.csv</code>应用<strong>完全相同</strong>的预处理流程。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 加载测试数据</span></span><br><span class="line">test_df = pd.read_csv(<span class="string">&#x27;test.csv&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 应用所有预处理步骤 (填充缺失值、独热编码...)</span></span><br><span class="line"><span class="comment">#    关键：使用从训练集计算出的median/mode/scaler</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 对齐列 (重要！)</span></span><br><span class="line"><span class="comment">#    确保测试集的列与训练集的列完全一致</span></span><br><span class="line">test_df = test_df.reindex(columns=train_columns, fill_value=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 使用训练好的模型进行预测</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 5. 生成提交文件</span></span><br><span class="line">submission_df.to_csv(<span class="string">&#x27;submission.csv&#x27;</span>, index=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>

<h3 id="4-3-超参数调优"><a href="#4-3-超参数调优" class="headerlink" title="4.3 超参数调优"></a>4.3 超参数调优</h3><p>通过实验寻找最佳的模型配置。</p>
<ul>
<li><strong>关键超参数</strong>: 学习率、网络结构（层数、神经元数）、训练周期数等。</li>
<li><strong>策略</strong>: 一次只调整一个参数，观察其对验证集性能的影响。</li>
<li><strong>自动化</strong>: 将训练&#x2F;评估过程封装成函数，用循环来自动化测试不同的超参数组合。</li>
</ul>
<h2 id="5-代码实战"><a href="#5-代码实战" class="headerlink" title="5.代码实战"></a>5.代码实战</h2><h3 id="5-1PIMA糖尿病分类"><a href="#5-1PIMA糖尿病分类" class="headerlink" title="5.1PIMA糖尿病分类"></a>5.1PIMA糖尿病分类</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> TensorDataset, DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score, precision_score, recall_score, f1_score</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">DiabetesClassifier</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.layer1 = nn.Linear(<span class="number">8</span>, <span class="number">6</span>)</span><br><span class="line">        self.layer2 = nn.Linear(<span class="number">6</span>, <span class="number">4</span>)</span><br><span class="line">        self.layer3 = nn.Linear(<span class="number">4</span>, <span class="number">1</span>)</span><br><span class="line">        self.relu = nn.ReLU()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        x = self.relu(self.layer1(x))</span><br><span class="line">        x = self.relu(self.layer2(x))</span><br><span class="line">        x = self.layer3(x)</span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line">EPOCHS = <span class="number">100</span></span><br><span class="line">LR = <span class="number">0.01</span></span><br><span class="line">BATCH_SIZE = <span class="number">32</span></span><br><span class="line">RANDOM_STATE = <span class="number">42</span></span><br><span class="line"></span><br><span class="line">raw_data = np.loadtxt(<span class="string">&#x27;./datasets/diabetes.csv&#x27;</span>, delimiter=<span class="string">&#x27;,&#x27;</span>, dtype=np.float32)</span><br><span class="line">features = raw_data[:, :-<span class="number">1</span>]</span><br><span class="line">labels = raw_data[:, [-<span class="number">1</span>]]</span><br><span class="line"></span><br><span class="line">X_train, X_test, y_train, y_test = train_test_split(</span><br><span class="line">    features, labels, test_size=<span class="number">0.2</span>, random_state=RANDOM_STATE</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">scaler = StandardScaler()</span><br><span class="line">X_train = scaler.fit_transform(X_train)</span><br><span class="line">X_test = scaler.transform(X_test)</span><br><span class="line"></span><br><span class="line">train_dataset = TensorDataset(torch.from_numpy(X_train), torch.from_numpy(y_train))</span><br><span class="line">test_dataset = TensorDataset(torch.from_numpy(X_test), torch.from_numpy(y_test))</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line">test_loader = DataLoader(dataset=test_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">device = torch.device(<span class="string">&#x27;cuda&#x27;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&#x27;cpu&#x27;</span>)</span><br><span class="line">model = DiabetesClassifier().to(device)</span><br><span class="line"></span><br><span class="line">criterion = nn.BCEWithLogitsLoss()</span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line"></span><br><span class="line">loss_history = []</span><br><span class="line">model.train()</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    epoch_loss = <span class="number">0.0</span></span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> train_loader:</span><br><span class="line">        features, labels = features.to(device), labels.to(device)</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line"></span><br><span class="line">        epoch_loss += loss.item()</span><br><span class="line">    </span><br><span class="line">    avg_loss = epoch_loss / <span class="built_in">len</span>(train_loader)</span><br><span class="line">    loss_history.append(avg_loss)</span><br><span class="line"></span><br><span class="line">all_labels = []</span><br><span class="line">all_predictions = []</span><br><span class="line">model.<span class="built_in">eval</span>()</span><br><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> test_loader:</span><br><span class="line">        features, labels = features.to(device), labels.to(device)</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        predicted = (torch.sigmoid(outputs) &gt; <span class="number">0.5</span>).<span class="built_in">float</span>()</span><br><span class="line"></span><br><span class="line">        all_labels.extend(labels.cpu().numpy())</span><br><span class="line">        all_predictions.extend(predicted.cpu().numpy())</span><br><span class="line"></span><br><span class="line">accuracy = accuracy_score(all_labels, all_predictions)</span><br><span class="line">precision = precision_score(all_labels, all_predictions)</span><br><span class="line">recall = recall_score(all_labels, all_predictions)</span><br><span class="line">f1 = f1_score(all_labels, all_predictions)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;Accuracy: <span class="subst">&#123;accuracy*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;Precision: <span class="subst">&#123;precision*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;Recall: <span class="subst">&#123;recall*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;F1 Score: <span class="subst">&#123;f1*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"></span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>, <span class="number">5</span>))</span><br><span class="line">plt.plot(<span class="built_in">range</span>(<span class="number">1</span>, EPOCHS+<span class="number">1</span>), loss_history)</span><br><span class="line">plt.xlabel(<span class="string">&quot;Epoch&quot;</span>)</span><br><span class="line">plt.ylabel(<span class="string">&quot;Loss&quot;</span>)</span><br><span class="line">plt.title(<span class="string">&quot;Training Loss Curve&quot;</span>)</span><br><span class="line">plt.grid()</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>

<h3 id="5-2泰坦尼克号生还预测分类"><a href="#5-2泰坦尼克号生还预测分类" class="headerlink" title="5.2泰坦尼克号生还预测分类"></a>5.2泰坦尼克号生还预测分类</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> TensorDataset, DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line">df = pd.read_csv(<span class="string">&#x27;./datasets/titanic/train.csv&#x27;</span>)</span><br><span class="line"></span><br><span class="line">age_median = df[<span class="string">&#x27;Age&#x27;</span>].median()</span><br><span class="line">df[<span class="string">&#x27;Age&#x27;</span>] = df[<span class="string">&#x27;Age&#x27;</span>].fillna(age_median)</span><br><span class="line">embarked_mode = df[<span class="string">&#x27;Embarked&#x27;</span>].mode()[<span class="number">0</span>]</span><br><span class="line">df[<span class="string">&#x27;Embarked&#x27;</span>] = df[<span class="string">&#x27;Embarked&#x27;</span>].fillna(embarked_mode)</span><br><span class="line"></span><br><span class="line">df = pd.get_dummies(df, columns=[<span class="string">&#x27;Sex&#x27;</span>, <span class="string">&#x27;Embarked&#x27;</span>], drop_first=<span class="literal">True</span>)</span><br><span class="line">df = df.drop([<span class="string">&#x27;PassengerId&#x27;</span>, <span class="string">&#x27;Name&#x27;</span>, <span class="string">&#x27;Ticket&#x27;</span>, <span class="string">&#x27;Cabin&#x27;</span>], axis=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">X = df.drop([<span class="string">&#x27;Survived&#x27;</span>], axis=<span class="number">1</span>).to_numpy()</span><br><span class="line">y = df[<span class="string">&#x27;Survived&#x27;</span>].to_numpy().reshape(-<span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">X_train, X_val, y_train, y_val = train_test_split(</span><br><span class="line">    X, y, test_size=<span class="number">0.2</span>, random_state=<span class="number">42</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">scaler = StandardScaler()</span><br><span class="line">X_train = scaler.fit_transform(X_train)</span><br><span class="line">X_val = scaler.transform(X_val)</span><br><span class="line"></span><br><span class="line">X_train = torch.tensor(X_train, dtype=torch.float32)</span><br><span class="line">X_val = torch.tensor(X_val, dtype=torch.float32)</span><br><span class="line">y_train = torch.tensor(y_train, dtype=torch.float32)</span><br><span class="line">y_val = torch.tensor(y_val, dtype=torch.float32)</span><br><span class="line"></span><br><span class="line">train_dataset = TensorDataset(X_train, y_train)</span><br><span class="line">val_dataset = TensorDataset(X_val, y_val)</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=<span class="number">32</span>, shuffle=<span class="literal">True</span>)</span><br><span class="line">val_loader = DataLoader(dataset=val_dataset, batch_size=<span class="number">32</span>, shuffle=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line">input_features = X_train.shape[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Classifier</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, num_features</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.net = nn.Sequential(</span><br><span class="line">            nn.Linear(num_features, <span class="number">16</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Linear(<span class="number">16</span>, <span class="number">8</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Linear(<span class="number">8</span>, <span class="number">1</span>),</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="keyword">return</span> self.net(x)</span><br><span class="line"></span><br><span class="line">EPOCHS = <span class="number">200</span></span><br><span class="line">LR = <span class="number">0.001</span></span><br><span class="line">model = Classifier(input_features)</span><br><span class="line">criterion = nn.BCEWithLogitsLoss()</span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line"></span><br><span class="line">model.train()</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> train_loader:</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line"></span><br><span class="line">model.<span class="built_in">eval</span>()</span><br><span class="line">all_predictions = []</span><br><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> val_loader:</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        predicted = (torch.sigmoid(outputs) &gt; <span class="number">0.5</span>).<span class="built_in">float</span>()</span><br><span class="line">        all_predictions.extend(predicted.numpy())</span><br><span class="line"></span><br><span class="line">accuracy = accuracy_score(y_val, all_predictions)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;Accuracy in val set: <span class="subst">&#123;accuracy*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"></span><br><span class="line">test_df = pd.read_csv(<span class="string">&#x27;./datasets/titanic/test.csv&#x27;</span>)</span><br><span class="line">passenger_ids = test_df[<span class="string">&#x27;PassengerId&#x27;</span>]</span><br><span class="line"></span><br><span class="line">fare_median = df[<span class="string">&#x27;Fare&#x27;</span>].median()</span><br><span class="line">test_df[<span class="string">&#x27;Age&#x27;</span>] = test_df[<span class="string">&#x27;Age&#x27;</span>].fillna(age_median)</span><br><span class="line">test_df[<span class="string">&#x27;Fare&#x27;</span>] = test_df[<span class="string">&#x27;Fare&#x27;</span>].fillna(fare_median)</span><br><span class="line"></span><br><span class="line">test_df = pd.get_dummies(test_df, columns=[<span class="string">&#x27;Sex&#x27;</span>, <span class="string">&#x27;Embarked&#x27;</span>], drop_first=<span class="literal">True</span>)</span><br><span class="line">test_features_df = test_df.drop([<span class="string">&#x27;PassengerId&#x27;</span>, <span class="string">&#x27;Name&#x27;</span>, <span class="string">&#x27;Ticket&#x27;</span>, <span class="string">&#x27;Cabin&#x27;</span>], axis=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">train_columns = df.drop([<span class="string">&#x27;Survived&#x27;</span>], axis=<span class="number">1</span>).columns</span><br><span class="line">test_features_df = test_features_df.reindex(columns=train_columns, fill_value=<span class="number">0</span>).to_numpy()</span><br><span class="line"></span><br><span class="line">X_test = scaler.transform(test_features_df)</span><br><span class="line">X_test = torch.tensor(X_test, dtype=torch.float32)</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    test_outputs = model(X_test)</span><br><span class="line">    predictions = (torch.sigmoid(test_outputs) &gt; <span class="number">0.5</span>).<span class="built_in">int</span>().numpy()</span><br><span class="line"></span><br><span class="line">submission_df = pd.DataFrame(&#123;<span class="string">&#x27;PassengerId&#x27;</span>: passenger_ids,<span class="string">&#x27;Survived&#x27;</span>: predictions.flatten()&#125;)</span><br><span class="line">submission_df.to_csv(<span class="string">&#x27;submission.csv&#x27;</span>, index=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>DL, Pytorch</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch学习笔记-Week7</title>
    <url>/posts/360729a6.html</url>
    <content><![CDATA[<p>本周从计算机视觉 (CNN) 领域迈入了一个全新的领域：<strong>自然语言处理 (NLP)<strong>与</strong>循环神经网络（RNN）</strong>。</p>
<p>核心挑战在于，神经网络不理解文字，只理解数字。首要任务就是学会如何将人类的语言“翻译”成机器能够计算的格式。</p>
<p>通过古诗生成案例，从最基础的文本处理，到构建、训练、并最终实现一个能够生成古诗的循环神经网络 (RNN)</p>
<h4 id="一：自然语言处理-NLP-入门"><a href="#一：自然语言处理-NLP-入门" class="headerlink" title="一：自然语言处理 (NLP) 入门"></a><strong>一：自然语言处理 (NLP) 入门</strong></h4><h5 id="1-1-核心翻译流程"><a href="#1-1-核心翻译流程" class="headerlink" title="1.1 核心翻译流程"></a><strong>1.1 核心翻译流程</strong></h5><ol>
<li><strong>分词 (Tokenization)</strong>: 将一个完整的句子或段落，拆分成最小的语言单元（“词元”或“Token”）。对于中文，这通常是单个的汉字。</li>
<li><strong>构建词汇表 (Vocabulary)</strong>: 收集所有在数据集中出现过的独立词元，并为每一个词元分配一个独一无二的整数ID。</li>
<li><strong>数值化 (Numericalization)</strong>: 使用构建好的词汇表，将原始的词元序列转换成一个整数ID序列。</li>
</ol>
<h5 id="1-2-文本预处理-preprocess-poems-函数"><a href="#1-2-文本预处理-preprocess-poems-函数" class="headerlink" title="1.2 文本预处理 (preprocess_poems 函数)"></a><strong>1.2 文本预处理 (<code>preprocess_poems</code> 函数)</strong></h5><p>通过一个函数封装了所有预处理步骤，这是一种非常好的编程实践。</p>
<ul>
<li><p><strong>思考与讨论</strong>: 构建词汇表的两种方法：</p>
<ol>
<li>在循环中逐行读取，用 <code>set.update()</code> 逐行去重。</li>
<li>先用 <code>&quot;&quot;.join(lines)</code> 将所有行合并成一个大字符串，然后用 <code>set()</code> 一次性去重。</li>
</ol>
<ul>
<li><strong>结论</strong>: 第二种方法更简洁、更符合Python风格 (Pythonic)，并且通常效率更高。</li>
</ul>
</li>
<li><p><strong>关键实现 (<code>poetry_generator_rnn.py</code>)</strong>:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">preprocess_poems</span>(<span class="params">file_path</span>):</span><br><span class="line">    <span class="comment"># 1. 逐行读取，清洗数据 (移除标点和空白)</span></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(file_path, <span class="string">&#x27;r&#x27;</span>, encoding=<span class="string">&#x27;utf-8&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        lines = [re.sub(<span class="string">f&quot;[，。？！、：]&quot;</span>, <span class="string">&quot;&quot;</span>, line.strip()) <span class="keyword">for</span> line <span class="keyword">in</span> f <span class="keyword">if</span> line.strip()]</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 2. 构建词汇表</span></span><br><span class="line">    <span class="comment"># a. 合并所有文本并去重</span></span><br><span class="line">    all_chars = <span class="built_in">sorted</span>(<span class="built_in">list</span>(<span class="built_in">set</span>(<span class="string">&quot;&quot;</span>.join(lines))))</span><br><span class="line">    <span class="comment"># b. 创建 id &lt;-&gt; word 映射</span></span><br><span class="line">    word2id = &#123;word : i <span class="keyword">for</span> i, word <span class="keyword">in</span> <span class="built_in">enumerate</span>(all_chars)&#125;</span><br><span class="line">    id2word = &#123;i : word <span class="keyword">for</span> i, word <span class="keyword">in</span> <span class="built_in">enumerate</span>(all_chars)&#125;</span><br><span class="line">    <span class="comment"># c. 添加未知词标记</span></span><br><span class="line">    word2id[<span class="string">&#x27;&lt;UNK&gt;&#x27;</span>] = <span class="built_in">len</span>(word2id)</span><br><span class="line">    id2word[<span class="built_in">len</span>(id2word)] = <span class="string">&#x27;&lt;UNK&gt;&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># 3. 数值化</span></span><br><span class="line">    <span class="comment"># 使用嵌套列表推导式高效地将所有诗句转换为ID序列</span></span><br><span class="line">    id_seqs = [[word2id.get(char, word2id[<span class="string">&#x27;&lt;UNK&gt;&#x27;</span>]) <span class="keyword">for</span> char <span class="keyword">in</span> line] <span class="keyword">for</span> line <span class="keyword">in</span> lines]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> id_seqs, word2id, id2word</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h4 id="二：为语言模型构建数据管道"><a href="#二：为语言模型构建数据管道" class="headerlink" title="二：为语言模型构建数据管道"></a><strong>二：为语言模型构建数据管道</strong></h4><h5 id="2-1-自定义-Dataset-PoetryDataset"><a href="#2-1-自定义-Dataset-PoetryDataset" class="headerlink" title="2.1 自定义 Dataset - PoetryDataset"></a><strong>2.1 自定义 <code>Dataset</code> - <code>PoetryDataset</code></strong></h5><ul>
<li><p><strong>核心任务</strong>: 将连续的诗歌文本，转换成适用于语言模型训练的 <code>(输入序列, 目标序列)</code> 数据对。</p>
</li>
<li><p><strong>实现方式</strong>: 使用<strong>滑动窗口 (Sliding Window)</strong> 的思想。</p>
</li>
<li><p><strong>关键实现 (<code>poetry_generator_rnn.py</code>)</strong>:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PoetryDataset</span>(<span class="title class_ inherited__">Dataset</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, id_seqs, seq_len</span>):</span><br><span class="line">        self.data = []</span><br><span class="line">        <span class="comment"># 遍历每一首独立的诗</span></span><br><span class="line">        <span class="keyword">for</span> id_seq <span class="keyword">in</span> id_seqs:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(id_seq) &lt; seq_len + <span class="number">1</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="comment"># 在每一首诗内部创建滑动窗口</span></span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="built_in">len</span>(id_seq) - seq_len):</span><br><span class="line">                <span class="comment"># 输入序列 (features)</span></span><br><span class="line">                features = id_seq[i : i + seq_len]</span><br><span class="line">                <span class="comment"># 目标序列 (labels)，是输入序列向后错一位</span></span><br><span class="line">                labels = id_seq[i + <span class="number">1</span> : i + seq_len + <span class="number">1</span>]</span><br><span class="line">                self.data.append((features, labels))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__getitem__</span>(<span class="params">self, index</span>):</span><br><span class="line">        features, labels = self.data[index]</span><br><span class="line">        <span class="comment"># 关键：转换发生在 __getitem__ 中，实现“懒加载”</span></span><br><span class="line">        <span class="keyword">return</span> torch.tensor(features, dtype=torch.long), torch.tensor(labels, dtype=torch.long)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__len__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">len</span>(self.data)</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="2-2-getitem-中的“懒加载”-Lazy-Loading"><a href="#2-2-getitem-中的“懒加载”-Lazy-Loading" class="headerlink" title="2.2 __getitem__ 中的“懒加载” (Lazy Loading)"></a><strong>2.2 <code>__getitem__</code> 中的“懒加载” (Lazy Loading)</strong></h5><ul>
<li><strong>思考</strong>: 为什么不像以前一样，在 <code>__init__</code> 中就把所有数据都转换成<code>torch.tensor</code>？</li>
<li><strong>结论</strong>: 这是一种更高级、更节省内存的<strong>最佳实践</strong>。<ul>
<li><code>__init__</code> 只存储轻量级的Python列表，几乎不消耗内存。</li>
<li>真正的张量转换操作被推迟到 <code>__getitem__</code> 中。</li>
<li>只有当 <code>DataLoader</code> 需要获取某个特定样本时，<code>__getitem__</code> 才会被调用，实现**“即时转换”**。</li>
<li>这使得我们能够处理远超内存大小的超大型数据集。</li>
</ul>
</li>
<li><strong>核心关系</strong>: 理解了 <code>DataLoader</code> 的工作机制——它就像一个图书管理员，隐式地、自动地调用 <code>Dataset</code> 的 <code>__getitem__</code> 方法来按批次“取书”。</li>
</ul>
<hr>
<h5 id="2-3-张量创建：torch-tensor-vs-torch-LongTensor"><a href="#2-3-张量创建：torch-tensor-vs-torch-LongTensor" class="headerlink" title="2.3 张量创建：torch.tensor vs. torch.LongTensor"></a><strong>2.3 张量创建：<code>torch.tensor</code> vs. <code>torch.LongTensor</code></strong></h5><ul>
<li><strong>思考</strong>: <code>torch.tensor(data, dtype=torch.long)</code> 和 <code>torch.LongTensor(data)</code> 有什么区别？</li>
<li><strong>结论</strong>:<ul>
<li><code>torch.LongTensor()</code> 是旧版的、特定类型的快捷函数。</li>
<li><code>torch.tensor()</code> 是现代的、统一的、通用的张量构造函数。</li>
<li>使用 <code>torch.tensor(..., dtype=...)</code> 的写法更清晰、更灵活，是<strong>官方推荐的最佳实践</strong>。</li>
</ul>
</li>
</ul>
<hr>
<h4 id="三：RNN模型架构与核心原理"><a href="#三：RNN模型架构与核心原理" class="headerlink" title="三：RNN模型架构与核心原理"></a><strong>三：RNN模型架构与核心原理</strong></h4><h5 id="3-1-核心组件-nn-RNN-nn-LSTM"><a href="#3-1-核心组件-nn-RNN-nn-LSTM" class="headerlink" title="3.1 核心组件 (nn.RNN, nn.LSTM)"></a><strong>3.1 核心组件 (<code>nn.RNN</code>, <code>nn.LSTM</code>)</strong></h5><ul>
<li><p><strong><code>nn.RNN</code> vs. <code>nn.RNNCell</code></strong>:</p>
<ul>
<li><strong>思考与辨析</strong>: 深入探讨了这两者的区别。</li>
<li><strong>结论</strong>:<ul>
<li><code>nn.RNNCell</code> 是一个**“单个工作站”<strong>，只处理一个时间步的数据，需要我们</strong>手动编写<code>for</code>循环**来迭代整个序列。它提供了最高的灵活性，适用于需要在时间步之间进行复杂干预的高级研究。</li>
<li><code>nn.RNN</code> 是一条**“自动化流水线”<strong>，它内部封装了循环逻辑，可以一次性处理整个序列。它更高效、更简洁，是</strong>绝大多数标准序列任务的首选**。</li>
</ul>
</li>
</ul>
</li>
<li><p><strong><code>nn.LSTM</code> (长短期记忆网络)</strong>:</p>
<ul>
<li>在实践中，选择使用<code>nn.LSTM</code>来替代基础的<code>nn.RNN</code>。LSTM通过引入“门控机制”（输入门、遗忘门、输出门），能够更好地捕捉长距离依赖关系，有效缓解了简单RNN的梯度消失&#x2F;爆炸问题，是现代NLP任务中更常用的基石。</li>
</ul>
</li>
<li><p><strong>关键组件参数</strong>:</p>
<ul>
<li><code>input_size</code>: 输入序列中，<strong>每一个元素</strong>的特征维度。在当前项目中，这等于<strong>词嵌入的维度 (<code>embedding_dim</code>)</strong>。</li>
<li><code>hidden_size</code>: RNN<strong>隐藏状态</strong>的维度，可以理解为模型的“记忆容量”。</li>
<li><code>num_layers</code>: 堆叠的RNN层数。<code>num_layers &gt; 1</code>可以构建<strong>深层RNN</strong>，让网络学习到更抽象的时间模式。</li>
<li><code>batch_first=True</code>: 一个至关重要的参数，它指定输入和输出张量的维度顺序为 <code>(批次大小, 序列长度, 特征数)</code>，这更直观，也是现代PyTorch代码的<strong>推荐实践</strong>。</li>
</ul>
<blockquote>
<p><strong>关键实现</strong>:</p>
</blockquote>
<blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 在模型中定义LSTM层 ---</span></span><br><span class="line">self.rnn = nn.LSTM(</span><br><span class="line">embedding_dim,</span><br><span class="line">hidden_size,</span><br><span class="line">num_layers,</span><br><span class="line">batch_first=<span class="literal">True</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure></blockquote>
</li>
</ul>
<h5 id="3-2-forward-方法：一个灵活的设计模式"><a href="#3-2-forward-方法：一个灵活的设计模式" class="headerlink" title="3.2 forward 方法：一个灵活的设计模式"></a><strong>3.2 <code>forward</code> 方法：一个灵活的设计模式</strong></h5><ul>
<li><p><strong>思考与辨析</strong>:</p>
<ul>
<li><strong>问题</strong>: <code>forward</code>方法中是否需要手动初始化<code>hidden</code>状态？</li>
<li><strong>结论</strong>: <strong>不应该</strong>。如果<code>hidden</code>状态为<code>None</code>，PyTorch的RNN层会<strong>自动创建一个全零的初始隐藏状态</strong>，并且其<code>batch_size</code>和<code>device</code>会自动匹配输入张量，这避免了因最后一个批次大小不匹配或设备不一致而导致的常见错误。</li>
<li><strong>问题</strong>: <code>forward</code>方法的签名写成<code>forward(self, x, hidden=None)</code>是否规范？</li>
<li><strong>结论</strong>: 这是<strong>最专业、最推荐的最佳实践</strong>。这个设计模式极其灵活，使得同一个模型可以：<ol>
<li>在<strong>无状态</strong>的训练&#x2F;分类任务中，通过 <code>model(features)</code> 调用，让PyTorch自动处理<code>hidden</code>状态。</li>
<li>在<strong>有状态</strong>的文本生成任务中，通过 <code>output, hidden = model(input, hidden)</code> 的循环调用，手动传递和更新<code>hidden</code>状态，以维持“记忆”的连贯性。</li>
</ol>
</li>
</ul>
</li>
<li><p><strong>关键实现 (<code>PoetryRNN</code>)</strong>:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 灵活的forward方法实现 ---</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PoetryRNN</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, ...</span>):</span><br><span class="line">        <span class="comment"># ... (Embedding, LSTM, Linear layers) ...</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x, hidden=<span class="literal">None</span></span>):</span><br><span class="line">        x = self.embed(x)</span><br><span class="line">        <span class="comment"># 将输入和（可能存在的）hidden状态传给RNN</span></span><br><span class="line">        x, hidden = self.rnn(x, hidden)</span><br><span class="line">        <span class="comment"># 将RNN所有时间步的输出都通过全连接层</span></span><br><span class="line">        x = self.fc(x)</span><br><span class="line">        <span class="keyword">return</span> x, hidden</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="3-3-RNN信息流：hidden-vs-output"><a href="#3-3-RNN信息流：hidden-vs-output" class="headerlink" title="3.3 RNN信息流：hidden vs output"></a><strong>3.3 RNN信息流：<code>hidden</code> vs <code>output</code></strong></h5><ul>
<li><strong>思考与辨析</strong>:<ul>
<li><strong>问题</strong>: “每一个output传给下一个cell作为hidden输入”，这个理解对吗？</li>
<li><strong>结论</strong>: 这个理解只在<strong>单层RNN</strong>中成立。在<strong>多层RNN</strong>中，<code>hidden</code>和<code>output</code>有明确的分工：<ul>
<li><strong><code>hidden state</code> (隐藏状态)</strong>: 负责将信息<strong>水平地</strong>传递给<strong>同一层</strong>的<strong>下一个时间步</strong>。它是层的“内部记忆”。</li>
<li><strong><code>output</code></strong>: 指的是<strong>最后一层（最顶层）在每一个时间步</strong>的隐藏状态。它负责将信息<strong>垂直地</strong>传递出去，作为整个RNN模块的最终输出。</li>
</ul>
</li>
</ul>
</li>
</ul>
<hr>
<h4 id="四：训练与生成"><a href="#四：训练与生成" class="headerlink" title="四：训练与生成"></a><strong>四：训练与生成</strong></h4><h5 id="4-1-训练循环"><a href="#4-1-训练循环" class="headerlink" title="4.1 训练循环"></a><strong>4.1 训练循环</strong></h5><ul>
<li><p><strong>梯度清零的位置</strong>:</p>
<ul>
<li><strong>思考</strong>: <code>optimizer.zero_grad()</code> 应该放在循环的开头还是末尾？</li>
<li><strong>结论</strong>: 两种写法在功能上等价，但将其放在<strong>循环开头</strong>是更现代、更清晰的<strong>最佳实践</strong>。这符合“每一次迭代都是一个全新的开始，先擦干净黑板再写新东西”的逻辑。</li>
</ul>
</li>
<li><p><strong>Seq-to-Seq损失计算</strong>:</p>
<ul>
<li><strong>问题</strong>: 在<code>PoetryDataset</code>中，<code>labels</code>的形状是 <code>[BATCH_SIZE, SEQ_LEN]</code>，而模型<code>outputs</code>的形状是 <code>[BATCH_SIZE, SEQ_LEN, VOCAB_SIZE]</code>。如何计算损失？</li>
<li><strong>解决方案</strong>: <code>nn.CrossEntropyLoss</code>在处理这种时间序列任务时，期望的输入<code>outputs</code>形状是 <code>[BATCH_SIZE, VOCAB_SIZE, SEQ_LEN]</code>。因此，我们需要对模型的输出进行维度转换。</li>
</ul>
<blockquote>
<p><strong>关键实现</strong>:</p>
</blockquote>
<blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 训练循环中的损失计算 ---</span></span><br><span class="line">model.train()</span><br><span class="line"><span class="keyword">for</span> features, labels <span class="keyword">in</span> dataloader:</span><br><span class="line">features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line"></span><br><span class="line">optimizer.zero_grad()</span><br><span class="line">	outputs, _ = model(features) <span class="comment"># outputs: [B, S, V]</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 交换SeqLen和Vocab_Size维度以匹配CrossEntropyLoss的要求</span></span><br><span class="line">	loss = criterion(outputs.transpose(<span class="number">1</span>, <span class="number">2</span>), labels)</span><br><span class="line"></span><br><span class="line">loss.backward()</span><br><span class="line">	optimizer.step()</span><br></pre></td></tr></table></figure></blockquote>
</li>
</ul>
<h5 id="4-2-文本生成-generate-poem-函数"><a href="#4-2-文本生成-generate-poem-函数" class="headerlink" title="4.2 文本生成 (generate_poem 函数)"></a><strong>4.2 文本生成 (<code>generate_poem</code> 函数)</strong></h5><ul>
<li><p><strong>核心原理</strong>: <strong>自回归 (Auto-regressive)</strong> 和 <strong>状态延续 (Stateful)</strong>。</p>
<ol>
<li><p>用“开头”的词或句子“预热”RNN，得到一个初始的<code>hidden</code>状态。</p>
</li>
<li><p>将最后一个词作为下一步的输入。</p>
</li>
<li><p>在循环中：</p>
<p>a.  将当前词和上一步的hidden状态一起送入模型。</p>
<p>b.  从模型输出的概率分布中，随机采样出下一个词的ID。</p>
<p>c.  将这个新生成的词ID作为下一次循环的输入。</p>
<p>d.  将模型返回的新的hidden状态保存，用于下一次循环。</p>
</li>
</ol>
</li>
<li><p><strong>创造性采样 (Creative Sampling)</strong>:</p>
<ul>
<li><p><strong>问题</strong>: 如果总是选择概率最高的词（贪婪搜索），生成的文本会非常单调和重复。</p>
</li>
<li><p><strong>解决方案</strong>: 使用<strong>温度 (Temperature)</strong> 和 <strong>多项式采样 (<code>torch.multinomial</code>)</strong>。</p>
<blockquote>
<p><strong>关键实现</strong>:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 带温度的随机采样 ---</span></span><br><span class="line"><span class="comment"># output是模型在当前步的logits输出, shape: [1, 1, vocab_size]</span></span><br><span class="line">output_dist = output.view(-<span class="number">1</span>).div(temperature).exp()</span><br><span class="line">next_id_tensor = torch.multinomial(output_dist, <span class="number">1</span>)[<span class="number">0</span>]</span><br></pre></td></tr></table></figure>

<p><code>temperature</code>值越低，采样结果越保守，接近贪婪搜索；值越高，结果越随机，更具创造性。</p>
</blockquote>
</li>
</ul>
</li>
<li><p><strong>结构化生成逻辑</strong>:</p>
<ul>
<li><strong>思考与调试</strong>: 最初尝试了一个非常复杂的单行<code>for</code>循环来控制诗句长度，但通过调试发现它在处理空<code>start_token</code>时存在<strong>边界bug</strong>。</li>
<li><strong>最终方案</strong>: 采纳了一种更健壮、更清晰的逻辑，即在每个诗句生成前，<strong>显式地计算</strong>出还需要生成多少个字<code>num_chars_to_gen</code>，然后用一个简单的<code>for</code>循环来执行固定次数的生成。这完美地解决了所有边界问题。</li>
</ul>
<blockquote>
<p><strong>关键实现</strong>:</p>
</blockquote>
<blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 健壮的结构化生成循环 ---</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(line_num):</span><br><span class="line"><span class="keyword">for</span> j, interpunction <span class="keyword">in</span> <span class="built_in">enumerate</span>([<span class="string">&quot;，&quot;</span>, <span class="string">&quot;。\n&quot;</span>]):</span><br><span class="line">  <span class="comment"># 只有在诗歌最开头才计算偏移</span></span><br><span class="line">  offset = <span class="built_in">len</span>(poem) <span class="keyword">if</span> i == <span class="number">0</span> <span class="keyword">and</span> j == <span class="number">0</span> <span class="keyword">else</span> <span class="number">0</span></span><br><span class="line">  num_chars_to_gen = line_length - offset</span><br><span class="line">  <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(num_chars_to_gen):</span><br><span class="line">      <span class="comment"># ... (执行一次单字生成) ...</span></span><br></pre></td></tr></table></figure></blockquote>
</li>
</ul>
<h4 id="五：完整代码"><a href="#五：完整代码" class="headerlink" title="五：完整代码"></a><strong>五：完整代码</strong></h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">from</span> torch <span class="keyword">import</span> nn, optim</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset, DataLoader</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"></span><br><span class="line">SEQ_LEN = <span class="number">24</span></span><br><span class="line">BATCH_SIZE = <span class="number">64</span></span><br><span class="line">EPOCHS = <span class="number">30</span></span><br><span class="line">LR = <span class="number">1e-3</span></span><br><span class="line">DEVICE = torch.device(<span class="string">&quot;cuda&quot;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&quot;cpu&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;Using device: <span class="subst">&#123;DEVICE&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">preprocess_poems</span>(<span class="params">file_path</span>):</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(file_path, <span class="string">&#x27;r&#x27;</span>, encoding=<span class="string">&#x27;utf-8&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        lines = [re.sub(<span class="string">f&quot;[，。？！、：]&quot;</span>, <span class="string">&quot;&quot;</span>, line.strip()) <span class="keyword">for</span> line <span class="keyword">in</span> f <span class="keyword">if</span> line.strip()]</span><br><span class="line">    </span><br><span class="line">    all_chars = <span class="built_in">sorted</span>(<span class="built_in">list</span>(<span class="built_in">set</span>(<span class="string">&quot;&quot;</span>.join(lines))))</span><br><span class="line"></span><br><span class="line">    word2id = &#123;word : i <span class="keyword">for</span> i, word <span class="keyword">in</span> <span class="built_in">enumerate</span>(all_chars)&#125;</span><br><span class="line">    id2word = &#123;i : word <span class="keyword">for</span> i, word <span class="keyword">in</span> <span class="built_in">enumerate</span>(all_chars)&#125;</span><br><span class="line">    word2id[<span class="string">&#x27;&lt;UNK&gt;&#x27;</span>] = <span class="built_in">len</span>(word2id)</span><br><span class="line">    id2word[<span class="built_in">len</span>(word2id)] = <span class="string">&#x27;&lt;UNK&gt;&#x27;</span></span><br><span class="line"></span><br><span class="line">    id_seqs = [[word2id.get(char, word2id[<span class="string">&#x27;&lt;UNK&gt;&#x27;</span>]) <span class="keyword">for</span> char <span class="keyword">in</span> line] <span class="keyword">for</span> line <span class="keyword">in</span> lines]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> id_seqs, word2id, id2word</span><br><span class="line"></span><br><span class="line">id_seqs, word2id, id2word = preprocess_poems(<span class="string">&#x27;../datasets/poems.txt&#x27;</span>)</span><br><span class="line">VOCAB_SIZE = <span class="built_in">len</span>(id2word)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PoetryDataset</span>(<span class="title class_ inherited__">Dataset</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, id_seqs, seq_len</span>):</span><br><span class="line">        self.data = []</span><br><span class="line">        <span class="keyword">for</span> id_seq <span class="keyword">in</span> id_seqs:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(id_seq) &lt; seq_len + <span class="number">1</span>:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="built_in">len</span>(id_seq) - seq_len):</span><br><span class="line">                features = id_seq[i : i + seq_len]</span><br><span class="line">                labels = id_seq[i + <span class="number">1</span> : i + <span class="number">1</span> + seq_len]</span><br><span class="line">                self.data.append((features, labels))</span><br><span class="line">                </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__getitem__</span>(<span class="params">self, index</span>):</span><br><span class="line">        features, labels = self.data[index]</span><br><span class="line">        <span class="keyword">return</span> torch.tensor(features, dtype=torch.long), torch.tensor(labels, dtype=torch.long)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__len__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">len</span>(self.data)</span><br><span class="line">    </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PoetryRNN</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, vocab_size, embedding_dim, hidden_size, num_layers=<span class="number">1</span></span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.embed = nn.Embedding(vocab_size, embedding_dim)</span><br><span class="line">        self.rnn = nn.LSTM(embedding_dim, hidden_size, num_layers, batch_first=<span class="literal">True</span>)</span><br><span class="line">        self.fc = nn.Linear(hidden_size, vocab_size)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x, hidden=<span class="literal">None</span></span>):</span><br><span class="line">        x = self.embed(x)</span><br><span class="line">        x, hidden = self.rnn(x, hidden)</span><br><span class="line">        x = self.fc(x)</span><br><span class="line">        <span class="keyword">return</span> x, hidden</span><br><span class="line"></span><br><span class="line">dataset = PoetryDataset(id_seqs, SEQ_LEN)</span><br><span class="line">dataloader = DataLoader(dataset=dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">model = PoetryRNN(vocab_size=VOCAB_SIZE, embedding_dim=<span class="number">128</span>, hidden_size=<span class="number">256</span>, num_layers=<span class="number">2</span>).to(DEVICE)</span><br><span class="line">criterion = nn.CrossEntropyLoss()</span><br><span class="line">optimizer = optim.Adam(model.parameters(), lr=LR)</span><br><span class="line"></span><br><span class="line">model.train()</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    running_loss = <span class="number">0.0</span></span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> dataloader:</span><br><span class="line">        features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        outputs, _ = model(features)</span><br><span class="line"></span><br><span class="line">        loss = criterion(outputs.transpose(<span class="number">1</span>, <span class="number">2</span>), labels)</span><br><span class="line"></span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line"></span><br><span class="line">        running_loss += loss.item()</span><br><span class="line">    </span><br><span class="line">    avg_loss = running_loss / <span class="built_in">len</span>(dataloader)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span>/<span class="subst">&#123;EPOCHS&#125;</span> | Loss <span class="subst">&#123;avg_loss:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">generate_poem</span>(<span class="params">model, start_token, line_length=<span class="number">5</span>, line_num=<span class="number">2</span>, temperature=<span class="number">0.8</span></span>):</span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    poem = <span class="built_in">list</span>(start_token)</span><br><span class="line">    hidden = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">if</span> start_token:</span><br><span class="line">            input_seq = [word2id.get(w, word2id[<span class="string">&#x27;&lt;UNK&gt;&#x27;</span>]) <span class="keyword">for</span> w <span class="keyword">in</span> start_token]</span><br><span class="line">            input_tensor = torch.tensor([input_seq], dtype=torch.long).to(DEVICE)</span><br><span class="line">            _, hidden = model(input_tensor, <span class="literal">None</span>)</span><br><span class="line">            next_input_id = input_seq[-<span class="number">1</span>]</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            next_input_id = np.random.randint(<span class="number">0</span>, VOCAB_SIZE)</span><br><span class="line">            poem.append(id2word[next_input_id])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(line_num):</span><br><span class="line">            <span class="keyword">for</span> j, interpunction <span class="keyword">in</span> <span class="built_in">enumerate</span>([<span class="string">&quot;，&quot;</span>, <span class="string">&quot;。\n&quot;</span>]):</span><br><span class="line"></span><br><span class="line">                offset = <span class="built_in">len</span>(poem) <span class="keyword">if</span> i ==<span class="number">0</span> <span class="keyword">and</span> j ==<span class="number">0</span> <span class="keyword">else</span> <span class="number">0</span></span><br><span class="line">                num_chars_to_gen = line_length - offset</span><br><span class="line"></span><br><span class="line">                <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(num_chars_to_gen):</span><br><span class="line">                    input_tensor = torch.tensor([[next_input_id]], dtype=torch.long).to(DEVICE)</span><br><span class="line">                    output, hidden = model(input_tensor, hidden)</span><br><span class="line"></span><br><span class="line">                    output_dist = output.view(-<span class="number">1</span>).div(temperature).exp()</span><br><span class="line">                    next_input_id = torch.multinomial(output_dist, <span class="number">1</span>)[<span class="number">0</span>]</span><br><span class="line"></span><br><span class="line">                    next_char = id2word.get(<span class="built_in">int</span>(next_input_id.item()), <span class="string">&#x27;&lt;UNK&gt;&#x27;</span>)</span><br><span class="line">                    poem.append(next_char)</span><br><span class="line">    </span><br><span class="line">                poem.append(interpunction)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;&quot;</span>.join(poem)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;\n--- 模型生成古诗 ---&quot;</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">5</span>):</span><br><span class="line">    start = <span class="string">&quot;春江&quot;</span></span><br><span class="line">    poem = generate_poem(model, start, line_length=<span class="number">5</span>, line_num=<span class="number">2</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;(<span class="subst">&#123;i+<span class="number">1</span>&#125;</span>)\n<span class="subst">&#123;poem.strip()&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">5</span>):</span><br><span class="line">    start = <span class="string">&quot;明月&quot;</span></span><br><span class="line">    poem = generate_poem(model, start, line_length=<span class="number">7</span>, line_num=<span class="number">4</span>)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;(<span class="subst">&#123;i+<span class="number">6</span>&#125;</span>)\n<span class="subst">&#123;poem.strip()&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>DL, Pytorch</tag>
      </tags>
  </entry>
  <entry>
    <title>PyTorch学习笔记-Week6</title>
    <url>/posts/5a8a6c8d.html</url>
    <content><![CDATA[<p>本周深入学习了多类别分类任务和CNN</p>
<p>通过 <strong>Otto Group 产品分类案例</strong>，深入理解了多类别分类任务的关键——包括标签编码、数据类型与形状规范、分层抽样、模型输出层设计以及交叉熵损失的使用。</p>
<p>在 <strong>CNN 部分</strong>，学习了如何利用卷积层、池化层和残差结构来提升模型的特征提取能力和分类性能，并掌握了 ResNet 等经典网络的设计思想。</p>
<p>整体上，已经从简单的二分类问题，逐步迈向了复杂的多类别场景，并借助 CNN 这一强大的模型架构，将理论与实践结合，掌握了构建和优化深度神经网络解决实际分类任务的能力。</p>
<h2 id="Part-1：多类别分类"><a href="#Part-1：多类别分类" class="headerlink" title="Part 1：多类别分类"></a><strong>Part 1：多类别分类</strong></h2><h3 id="项目案例：Otto-Group-产品分类-9个类别"><a href="#项目案例：Otto-Group-产品分类-9个类别" class="headerlink" title="项目案例：Otto Group 产品分类 (9个类别)"></a><strong>项目案例：Otto Group 产品分类 (9个类别)</strong></h3><p>从二分类迈向多类别分类，是掌握分类任务的关键一步。<br> 本项目中，遇到了三个核心挑战：<strong>标签处理、模型输出层设计和损失函数选择</strong>，并逐一攻克。</p>
<hr>
<h3 id="1-1-数据预处理的关键决策"><a href="#1-1-数据预处理的关键决策" class="headerlink" title="1.1 数据预处理的关键决策"></a><strong>1.1 数据预处理的关键决策</strong></h3><h4 id="1-1-1-标签编码-Label-Encoding"><a href="#1-1-1-标签编码-Label-Encoding" class="headerlink" title="1.1.1 标签编码 (Label Encoding)"></a><strong>1.1.1 标签编码 (Label Encoding)</strong></h4><ul>
<li><strong>问题</strong>: 原始目标标签是文本（如 <code>&#39;Class_1&#39;</code>, <code>&#39;Class_2&#39;</code>），神经网络无法直接处理。</li>
<li><strong>解决方案</strong>: 转换为整数索引（0, 1, 2, …）。</li>
</ul>
<p><strong>实现代码：</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 标签编码实现 ---</span></span><br><span class="line"><span class="comment"># 将 &#x27;Class_X&#x27; 转换为整数 X-1</span></span><br><span class="line">df[<span class="string">&#x27;target&#x27;</span>] = df[<span class="string">&#x27;target&#x27;</span>].<span class="built_in">str</span>.replace(<span class="string">&#x27;Class_&#x27;</span>, <span class="string">&#x27;&#x27;</span>).astype(<span class="built_in">int</span>) - <span class="number">1</span></span><br></pre></td></tr></table></figure>

<hr>
<h4 id="1-1-2-标签的数据类型与形状-for-nn-CrossEntropyLoss"><a href="#1-1-2-标签的数据类型与形状-for-nn-CrossEntropyLoss" class="headerlink" title="1.1.2 标签的数据类型与形状 (for nn.CrossEntropyLoss)"></a><strong>1.1.2 标签的数据类型与形状 (for <code>nn.CrossEntropyLoss</code>)</strong></h4><ul>
<li><strong>核心问题</strong>: <code>CrossEntropyLoss</code> 对输入标签的<strong>形状</strong>和<strong>类型</strong>有严格要求。</li>
<li><strong>辨析</strong>:<ol>
<li><strong>形状</strong><ul>
<li>二分类中我们常用 <code>.reshape(-1, 1)</code> 得到列向量。</li>
<li><strong>结论</strong>: 多类别分类 <strong>不需要</strong>，标签应为一维张量 <code>[batch_size]</code>。</li>
</ul>
</li>
<li><strong>数据类型</strong><ul>
<li>标签必须是 <strong><code>torch.long</code></strong>，因为损失函数会把它作为 <strong>类别索引</strong> 使用。</li>
</ul>
</li>
</ol>
</li>
</ul>
<p><strong>实现代码：</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">OttoDataset</span>(<span class="title class_ inherited__">Dataset</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, features, labels</span>):</span><br><span class="line">        self.features = torch.tensor(features, dtype=torch.float32)</span><br><span class="line">        <span class="comment"># 标签必须是 LongTensor</span></span><br><span class="line">        self.labels = torch.tensor(labels, dtype=torch.long)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__getitem__</span>(<span class="params">self, index</span>):</span><br><span class="line">        <span class="keyword">return</span> self.features[index], self.labels[index]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__len__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">len</span>(self.features)</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="1-1-3-分层抽样-Stratified-Sampling"><a href="#1-1-3-分层抽样-Stratified-Sampling" class="headerlink" title="1.1.3 分层抽样 (Stratified Sampling)"></a><strong>1.1.3 分层抽样 (Stratified Sampling)</strong></h4><ul>
<li><strong>问题</strong>: Otto 数据集类别不均衡，随机划分可能导致验证集分布失衡。</li>
<li><strong>解决方案</strong>: 使用 <code>train_test_split(..., stratify=y)</code>。</li>
</ul>
<p><strong>实现代码：</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"></span><br><span class="line">X_train, X_val, y_train, y_val = train_test_split(</span><br><span class="line">    X, y, test_size=<span class="number">0.2</span>, random_state=<span class="number">42</span>, stratify=y</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="1-2-模型架构与损失函数"><a href="#1-2-模型架构与损失函数" class="headerlink" title="1.2 模型架构与损失函数"></a><strong>1.2 模型架构与损失函数</strong></h3><ul>
<li><strong>输出层</strong>: <code>nn.Linear</code> 的输出神经元数必须等于类别数。</li>
<li><strong>最佳实践</strong>: 动态获取类别数，而非硬编码。</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"></span><br><span class="line">num_classes = <span class="built_in">len</span>(np.unique(y))  <span class="comment"># 动态获取类别数</span></span><br><span class="line">model = YourModel(num_features=..., num_classes=num_classes)</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>损失函数</strong>: 使用 <code>nn.CrossEntropyLoss</code>。</li>
<li><strong>注意</strong>: 不需要手动添加 <code>Softmax</code>，因为它已集成在损失函数内部。</li>
</ul>
<hr>
<h3 id="1-3-Kaggle-提交格式"><a href="#1-3-Kaggle-提交格式" class="headerlink" title="1.3 Kaggle 提交格式"></a><strong>1.3 Kaggle 提交格式</strong></h3><ul>
<li><strong>问题</strong>: Kaggle 要求提交的是各类别概率，而不是类别索引。</li>
<li><strong>说明</strong>: 提交样例 <code>1,0,0,0,0,0,0,0,0</code> 代表<strong>100%概率属于 Class_1</strong>，而不是标签。</li>
</ul>
<p><strong>实现代码：</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.nn.functional <span class="keyword">as</span> F</span><br><span class="line"></span><br><span class="line">model.<span class="built_in">eval</span>()</span><br><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    test_outputs = model(X_test_tensor)</span><br><span class="line">    <span class="comment"># 将 logits 转换为概率</span></span><br><span class="line">    probabilities = F.softmax(test_outputs, dim=<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">probabilities_np = probabilities.cpu().numpy()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 构建 DataFrame</span></span><br><span class="line">class_columns = [<span class="string">f&#x27;Class_<span class="subst">&#123;i&#125;</span>&#x27;</span> <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">10</span>)]</span><br><span class="line">submission_df = pd.DataFrame(probabilities_np, columns=class_columns)</span><br><span class="line">submission_df.insert(<span class="number">0</span>, <span class="string">&#x27;id&#x27;</span>, test_ids)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存为 CSV</span></span><br><span class="line">submission_df.to_csv(<span class="string">&#x27;submission.csv&#x27;</span>, index=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>

<h3 id="1-4-Otto-Group-产品分类完整代码"><a href="#1-4-Otto-Group-产品分类完整代码" class="headerlink" title="1.4 Otto Group 产品分类完整代码"></a><strong>1.4 Otto Group 产品分类完整代码</strong></h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> Dataset, DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.preprocessing <span class="keyword">import</span> StandardScaler</span><br><span class="line"><span class="keyword">from</span> sklearn.model_selection <span class="keyword">import</span> train_test_split</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line">EPOCHS = <span class="number">50</span></span><br><span class="line">LR = <span class="number">0.005</span></span><br><span class="line">BATCH_SIZE = <span class="number">128</span></span><br><span class="line">DEVICE = torch.device(<span class="string">&#x27;cuda&#x27;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&#x27;cpu&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    df = pd.read_csv(<span class="string">&#x27;../datasets/otto/train.csv&#x27;</span>)</span><br><span class="line"><span class="keyword">except</span> FileNotFoundError:</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;Dataset file not found&quot;</span>)</span><br><span class="line">    exit()</span><br><span class="line"></span><br><span class="line">df[<span class="string">&#x27;target&#x27;</span>] = df[<span class="string">&#x27;target&#x27;</span>].<span class="built_in">str</span>.replace(<span class="string">&#x27;Class_&#x27;</span>, <span class="string">&#x27;&#x27;</span>).astype(<span class="built_in">int</span>) - <span class="number">1</span></span><br><span class="line"></span><br><span class="line">X = df.drop([<span class="string">&#x27;id&#x27;</span>, <span class="string">&#x27;target&#x27;</span>], axis=<span class="number">1</span>).to_numpy()</span><br><span class="line">y = df[<span class="string">&#x27;target&#x27;</span>].to_numpy()</span><br><span class="line"></span><br><span class="line">X_train, X_val, y_train, y_val = train_test_split(</span><br><span class="line">    X, y, test_size=<span class="number">0.2</span>, random_state=<span class="number">42</span>, stratify=y</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">scaler = StandardScaler()</span><br><span class="line">X_train = scaler.fit_transform(X_train)</span><br><span class="line">X_val = scaler.transform(X_val)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">OttoDataset</span>(<span class="title class_ inherited__">Dataset</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, features, labels</span>):</span><br><span class="line">        self.features = torch.tensor(features, dtype=torch.float32)</span><br><span class="line">        self.labels = torch.tensor(labels, dtype=torch.long)</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__getitem__</span>(<span class="params">self, index</span>):</span><br><span class="line">        <span class="keyword">return</span> self.features[index], self.labels[index]</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__len__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">len</span>(self.features)</span><br><span class="line"></span><br><span class="line">train_dataset = OttoDataset(X_train, y_train)</span><br><span class="line">val_dataset = OttoDataset(X_val, y_val)</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line">val_loader = DataLoader(dataset=val_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line">input_features = X_train.shape[<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">OttoClassifier</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, num_features, num_classes</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.net = nn.Sequential(</span><br><span class="line">            nn.Linear(num_features, <span class="number">128</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Dropout(p=<span class="number">0.3</span>),</span><br><span class="line"></span><br><span class="line">            nn.Linear(<span class="number">128</span>, <span class="number">64</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Dropout(p=<span class="number">0.3</span>),</span><br><span class="line"></span><br><span class="line">            nn.Linear(<span class="number">64</span>, <span class="number">32</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.Dropout(p=<span class="number">0.2</span>),</span><br><span class="line"></span><br><span class="line">            nn.Linear(<span class="number">32</span>, num_classes)</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="keyword">return</span> self.net(x)</span><br><span class="line">    </span><br><span class="line">model = OttoClassifier(num_features=input_features, num_classes=<span class="number">9</span>).to(DEVICE)</span><br><span class="line">criterion = nn.CrossEntropyLoss()</span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line"></span><br><span class="line">model.train()</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    epoch_loss = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> train_loader:</span><br><span class="line">        features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line"></span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line">        </span><br><span class="line">        epoch_loss += loss.item()</span><br><span class="line">    avg_loss = epoch_loss / <span class="built_in">len</span>(train_loader)</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;Epoch <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span>/<span class="subst">&#123;EPOCHS&#125;</span>, Loss: <span class="subst">&#123;avg_loss:<span class="number">.4</span>f&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">model.<span class="built_in">eval</span>()</span><br><span class="line">all_predictions = []</span><br><span class="line"><span class="keyword">with</span> torch.no_grad():</span><br><span class="line">    <span class="keyword">for</span> features, _ <span class="keyword">in</span> val_loader:</span><br><span class="line">        features = features.to(DEVICE)</span><br><span class="line">        outputs = model(features)</span><br><span class="line"></span><br><span class="line">        _, predicted = torch.<span class="built_in">max</span>(outputs, <span class="number">1</span>)</span><br><span class="line">        all_predictions.extend(predicted.cpu().numpy())</span><br><span class="line"></span><br><span class="line">accuracy = accuracy_score(y_val, all_predictions)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;Accuracy: <span class="subst">&#123;accuracy*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="Part2：计算机视觉入门-CNN"><a href="#Part2：计算机视觉入门-CNN" class="headerlink" title="Part2：计算机视觉入门 (CNN)"></a><strong>Part2：计算机视觉入门 (CNN)</strong></h2><p><strong>项目案例</strong>: MNIST 手写数字识别、Fashion-MNIST 服装分类</p>
<hr>
<h2 id="2-1-图像数据预处理-torchvision-transforms"><a href="#2-1-图像数据预处理-torchvision-transforms" class="headerlink" title="2.1 图像数据预处理 (torchvision.transforms)"></a><strong>2.1 图像数据预处理 (<code>torchvision.transforms</code>)</strong></h2><ul>
<li><strong>核心工具</strong>: <code>transforms.Compose</code> —— 图像处理的“流水线”，将多个转换按顺序组合。</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">transform = transforms.Compose([</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize((<span class="number">0.1307</span>,), (<span class="number">0.3081</span>,)) <span class="comment"># MNIST手写数字集均值、标准差</span></span><br><span class="line">])</span><br></pre></td></tr></table></figure>

<h3 id="关键转换操作"><a href="#关键转换操作" class="headerlink" title="关键转换操作"></a><strong>关键转换操作</strong></h3><ol>
<li><strong><code>transforms.ToTensor()</code></strong><ul>
<li>格式转换：<code>PIL Image → Tensor</code></li>
<li>数值归一化：<code>[0, 255] → [0.0, 1.0]</code></li>
<li>维度重排：<code>(H, W, C) → (C, H, W)</code></li>
</ul>
</li>
<li><strong><code>transforms.Normalize(mean, std)</code></strong><ul>
<li>将数据标准化为均值0、标准差1，提高收敛速度与稳定性。</li>
<li><strong>为什么 <code>(0.1307,)</code> 要写成单元素元组？</strong><br>因为函数期望输入元组，每个元素对应一个通道。MNIST 是单通道灰度图，因此需要 <code>(mean,)</code> 的形式。</li>
</ul>
</li>
</ol>
<h3 id="数据增强-Data-Augmentation"><a href="#数据增强-Data-Augmentation" class="headerlink" title="数据增强 (Data Augmentation)"></a><strong>数据增强 (Data Augmentation)</strong></h3><ul>
<li><strong>核心思想</strong>: 不增加新样本，通过对训练集随机变换（裁剪、翻转等）生成多样性数据，防止过拟合。</li>
<li><strong>原则</strong>: <strong>只增强训练集，不动测试集</strong>，确保评估客观。</li>
</ul>
<p><strong>实现代码：</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># --- 训练集（含数据增强） ---</span></span><br><span class="line">train_transform = transforms.Compose([</span><br><span class="line">    transforms.RandomCrop(<span class="number">32</span>, padding=<span class="number">4</span>),   <span class="comment"># 随机裁剪</span></span><br><span class="line">    transforms.RandomHorizontalFlip(),      <span class="comment"># 随机水平翻转</span></span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize(mean, std)</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line"><span class="comment"># --- 测试集（无数据增强） ---</span></span><br><span class="line">test_transform = transforms.Compose([</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize(mean, std)</span><br><span class="line">])</span><br></pre></td></tr></table></figure>

<hr>
<h2 id="2-2-从全连接到CNN：空间结构的保留"><a href="#2-2-从全连接到CNN：空间结构的保留" class="headerlink" title="2.2 从全连接到CNN：空间结构的保留"></a><strong>2.2 从全连接到CNN：空间结构的保留</strong></h2><ul>
<li><p><strong>问题</strong>: 为什么全连接网络在图像任务表现差？</p>
</li>
<li><p><strong>结论</strong>: 因为输入需要“压平”为一维向量（<code>28*28=784</code>），破坏了像素之间的空间关系，模型无法学习边缘&#x2F;形状等特征。</p>
</li>
<li><p><strong>CNN的优势</strong>: 卷积核滑动操作保留二维结构，能高效提取空间模式。</p>
<p><img   src="C:\Users\Chaptsand\AppData\Roaming\Typora\typora-user-images\image-20251004221138308.png"  alt="image-20251004221138308"></p>
</li>
</ul>
<hr>
<h2 id="2-3-简单CNN的维度变化追踪"><a href="#2-3-简单CNN的维度变化追踪" class="headerlink" title="2.3 简单CNN的维度变化追踪"></a><strong>2.3 简单CNN的维度变化追踪</strong></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">CNNModel</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.feature_extractor = nn.Sequential(</span><br><span class="line">            <span class="comment"># Conv Layer 1</span></span><br><span class="line">            nn.Conv2d(in_channels=<span class="number">1</span>, out_channels=<span class="number">16</span>, kernel_size=<span class="number">5</span>, stride=<span class="number">1</span>, padding=<span class="number">2</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=<span class="number">2</span>),</span><br><span class="line">            <span class="comment"># Conv Layer 2</span></span><br><span class="line">            nn.Conv2d(in_channels=<span class="number">16</span>, out_channels=<span class="number">32</span>, kernel_size=<span class="number">5</span>, stride=<span class="number">1</span>, padding=<span class="number">2</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=<span class="number">2</span>)</span><br><span class="line">        )</span><br><span class="line">        self.classifier = nn.Linear(<span class="number">32</span> * <span class="number">7</span> * <span class="number">7</span>, <span class="number">10</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        x = self.feature_extractor(x)</span><br><span class="line">        x = x.view(-<span class="number">1</span>, <span class="number">32</span> * <span class="number">7</span> * <span class="number">7</span>)</span><br><span class="line">        x = self.classifier(x)</span><br><span class="line">        <span class="keyword">return</span> x</span><br></pre></td></tr></table></figure>

<p>假设 <code>batch_size = 64</code>：</p>
<ol>
<li>输入: <code>[64, 1, 28, 28]</code></li>
<li><code>Conv2d(1, 16, kernel_size=5, padding=2)</code> → <code>[64, 16, 28, 28]</code></li>
<li><code>MaxPool2d(2)</code> → <code>[64, 16, 14, 14]</code></li>
<li><code>Conv2d(16, 32, kernel_size=5, padding=2)</code> → <code>[64, 32, 14, 14]</code></li>
<li><code>MaxPool2d(2)</code> → <code>[64, 32, 7, 7]</code></li>
<li><code>.view(-1, 32*7*7)</code> → <code>[64, 1568]</code></li>
<li><code>Linear(1568, 10)</code> → <code>[64, 10]</code></li>
</ol>
<hr>
<p>简单CNN实现MNIST手写数据集识别</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> torchvision <span class="keyword">import</span> datasets, transforms</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">EPOCHS = <span class="number">10</span></span><br><span class="line">BATCH_SIZE = <span class="number">64</span></span><br><span class="line">DEVICE = torch.device(<span class="string">&#x27;cuda&#x27;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&#x27;cpu&#x27;</span>)</span><br><span class="line">LR = <span class="number">0.01</span></span><br><span class="line"></span><br><span class="line">transform = transforms.Compose([</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize((<span class="number">0.1307</span>,), (<span class="number">0.3081</span>,))</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line">train_dataset = datasets.MNIST(root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">True</span>, transform=transform, download=<span class="literal">True</span>)</span><br><span class="line">test_dataset = datasets.MNIST(root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">False</span>, transform=transform, download=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line">test_loader = DataLoader(dataset=test_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">CNNModel</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.feature_extractor = nn.Sequential(</span><br><span class="line">            <span class="comment"># Conv layer1</span></span><br><span class="line">            nn.Conv2d(in_channels=<span class="number">1</span>, out_channels=<span class="number">16</span>, kernel_size=<span class="number">5</span>, stride=<span class="number">1</span>, padding=<span class="number">2</span>),</span><br><span class="line">            nn.BatchNorm2d(<span class="number">16</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=<span class="number">2</span>),</span><br><span class="line"></span><br><span class="line">            <span class="comment"># Conv layer2</span></span><br><span class="line">            nn.Conv2d(in_channels=<span class="number">16</span>, out_channels=<span class="number">32</span>, kernel_size=<span class="number">5</span>, stride=<span class="number">1</span>, padding=<span class="number">2</span>),</span><br><span class="line">            nn.BatchNorm2d(<span class="number">32</span>),</span><br><span class="line">            nn.ReLU(),</span><br><span class="line">            nn.MaxPool2d(kernel_size=<span class="number">2</span>),</span><br><span class="line">        )</span><br><span class="line">        self.classifier = nn.Sequential(</span><br><span class="line">            nn.Dropout(<span class="number">0.5</span>),</span><br><span class="line">            nn.Linear(<span class="number">32</span> * <span class="number">7</span> * <span class="number">7</span>, <span class="number">10</span>)</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># [64, 1, 28, 28] -&gt; [64, 16, 28, 28] -&gt; [64, 16, 14, 14]</span></span><br><span class="line">        <span class="comment"># [64, 16, 14, 14] -&gt; [64, 32, 14, 14] -&gt; [64, 32, 7, 7]</span></span><br><span class="line">        x = self.feature_extractor(x)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># [64, 32, 7, 7] -&gt; [64, 32 * 7 * 7]</span></span><br><span class="line">        x = x.view(-<span class="number">1</span>, <span class="number">32</span> * <span class="number">7</span> * <span class="number">7</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># [64, 32 * 7 * 7] -&gt; [64, 10]</span></span><br><span class="line">        x = self.classifier(x)</span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br><span class="line">model = CNNModel().to(DEVICE)</span><br><span class="line">criterion = nn.CrossEntropyLoss()</span><br><span class="line"><span class="comment"># optimizer = torch.optim.SGD(model.parameters(), lr=LR, momentum=0.9)</span></span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line">scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=<span class="number">3</span>, gamma=<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line">loss_history = []</span><br><span class="line">accuracy_history = []</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    epoch_loss = <span class="number">0.0</span></span><br><span class="line">    model.train()</span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> train_loader:</span><br><span class="line">        features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line"></span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line"></span><br><span class="line">        epoch_loss += loss.item()</span><br><span class="line">    </span><br><span class="line">    avg_loss = epoch_loss / <span class="built_in">len</span>(train_loader)</span><br><span class="line">    loss_history.append(avg_loss)</span><br><span class="line"></span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    all_predictions = []</span><br><span class="line">    all_labels = []</span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">for</span> features, labels <span class="keyword">in</span> test_loader:</span><br><span class="line">            features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line">            outputs = model(features)</span><br><span class="line">            _, predicted = torch.<span class="built_in">max</span>(outputs, <span class="number">1</span>)</span><br><span class="line">            all_predictions.extend(predicted.cpu().numpy())</span><br><span class="line">            all_labels.extend(labels.cpu().numpy())</span><br><span class="line">    </span><br><span class="line">    epoch_accuracy = accuracy_score(all_labels, all_predictions)</span><br><span class="line">    accuracy_history.append(epoch_accuracy)</span><br><span class="line"></span><br><span class="line">    scheduler.step()</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;Epoch <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span>/<span class="subst">&#123;EPOCHS&#125;</span> | Train Loss: <span class="subst">&#123;avg_loss:<span class="number">.4</span>f&#125;</span> | Val Accuracy: <span class="subst">&#123;epoch_accuracy*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line"></span><br><span class="line">plt.figure(figsize=(<span class="number">12</span>, <span class="number">8</span>))</span><br><span class="line"></span><br><span class="line">plt.subplot(<span class="number">1</span>, <span class="number">2</span>, <span class="number">1</span>)</span><br><span class="line">plt.plot(<span class="built_in">range</span>(<span class="number">1</span>, EPOCHS + <span class="number">1</span>), loss_history, label=<span class="string">&#x27;Training Loss&#x27;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&#x27;Epoch&#x27;</span>)</span><br><span class="line">plt.ylabel(<span class="string">&#x27;Average Loss&#x27;</span>)</span><br><span class="line">plt.title(<span class="string">&#x27;Traning Loss Curve&#x27;</span>)</span><br><span class="line">plt.grid(<span class="literal">True</span>)</span><br><span class="line">plt.legend()</span><br><span class="line"></span><br><span class="line">plt.subplot(<span class="number">1</span>, <span class="number">2</span>, <span class="number">2</span>)</span><br><span class="line">plt.plot(<span class="built_in">range</span>(<span class="number">1</span>, EPOCHS + <span class="number">1</span>), accuracy_history, label=<span class="string">&#x27;Validation Accuracy&#x27;</span>, color=<span class="string">&#x27;orange&#x27;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&#x27;Epoch&#x27;</span>)</span><br><span class="line">plt.ylabel(<span class="string">&#x27;Accuracy&#x27;</span>)</span><br><span class="line">plt.title(<span class="string">&#x27;Validation Accuracy Curve&#x27;</span>)</span><br><span class="line">plt.grid(<span class="literal">True</span>)</span><br><span class="line">plt.legend()</span><br><span class="line"></span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>

<h2 id="2-4-ResNet架构：跨越深度的鸿沟"><a href="#2-4-ResNet架构：跨越深度的鸿沟" class="headerlink" title="2.4 ResNet架构：跨越深度的鸿沟"></a><strong>2.4 ResNet架构：跨越深度的鸿沟</strong></h2><p>现代 CNN 的基石，解决了“深度的诅咒”。</p>
<h3 id="2-4-1-深度的诅咒：梯度消失"><a href="#2-4-1-深度的诅咒：梯度消失" class="headerlink" title="2.4.1 深度的诅咒：梯度消失"></a><strong>2.4.1 深度的诅咒：梯度消失</strong></h3><ul>
<li><strong>问题</strong>: VGG 等深层网络，反向传播时梯度因连乘效应衰减为零。</li>
<li><strong>结果</strong>: 底层网络学不到有效特征。</li>
</ul>
<h3 id="2-4-2-解决方案：构建残差块-ResidualBlock"><a href="#2-4-2-解决方案：构建残差块-ResidualBlock" class="headerlink" title="2.4.2 解决方案：构建残差块 (ResidualBlock)"></a><strong>2.4.2 解决方案：构建残差块 (ResidualBlock)</strong></h3><ul>
<li><p><strong>核心思想</strong>:<br>使用 <strong>跳跃连接 (Skip Connection)</strong>，公式：<br>$$<br>H(x) &#x3D; F(x) + x<br>$$</p>
<p> 让梯度能直接反传，避免消失。</p>
</li>
</ul>
<h4 id="第一部分-ResidualBlock-残差块-ResNet的“乐高积木”"><a href="#第一部分-ResidualBlock-残差块-ResNet的“乐高积木”" class="headerlink" title="第一部分: ResidualBlock (残差块) - ResNet的“乐高积木”"></a><strong>第一部分: <code>ResidualBlock</code> (残差块) - ResNet的“乐高积木”</strong></h4><p>这个类定义了ResNet最基本、可重复使用的单元。它的天才之处在于同时包含了一条“普通公路”和一条“高速公路”。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ResidualBlock</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, in_channels, out_channels, stride=<span class="number">1</span></span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 主路径 (&quot;普通公路&quot;): 数据在这里经过深度处理</span></span><br><span class="line">        self.conv1 = nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">3</span>, stride=stride, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(out_channels)</span><br><span class="line">        self.relu = nn.ReLU(inplace=<span class="literal">True</span>)</span><br><span class="line">        self.conv2 = nn.Conv2d(out_channels, out_channels, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn2 = nn.BatchNorm2d(out_channels)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 快捷连接 (&quot;高速公路&quot;): 数据在这里“跳过”处理</span></span><br><span class="line">        self.shortcut = nn.Sequential()</span><br><span class="line">        <span class="keyword">if</span> stride != <span class="number">1</span> <span class="keyword">or</span> in_channels != out_channels:</span><br><span class="line">            <span class="comment"># 如果维度不匹配，高速公路也需要一个“适配器”</span></span><br><span class="line">            self.shortcut = nn.Sequential(</span><br><span class="line">                nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">1</span>, stride=stride, bias=<span class="literal">False</span>),</span><br><span class="line">                nn.BatchNorm2d(out_channels)</span><br><span class="line">            )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># 1. 数据走“普通公路”，得到处理结果 F(x)</span></span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = self.bn2(self.conv2(out))</span><br><span class="line">        <span class="comment"># 2. 原始数据走“高速公路”，得到 x&#x27; (可能经过了适配器)</span></span><br><span class="line">        <span class="comment"># 3. 两条路在终点汇合！ F(x) + x&#x27;</span></span><br><span class="line">        out += self.shortcut(x)</span><br><span class="line">        <span class="comment"># 4. 通过最终的激活函数</span></span><br><span class="line">        out = self.relu(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br></pre></td></tr></table></figure>

<h5 id="init-初始化-定义两条路"><a href="#init-初始化-定义两条路" class="headerlink" title="__init__ (初始化 - 定义两条路)"></a><code>__init__</code> (初始化 - 定义两条路)</h5><ol>
<li><strong>主路径 (<code>self.conv1</code>, <code>self.bn1</code>, etc.)</strong>:<ul>
<li>这是一条标准的卷积路径：<code>卷积 -&gt; 批量归一化 -&gt; ReLU -&gt; 卷积 -&gt; 批量归一化</code>。</li>
<li>它负责学习输入特征 <code>x</code> 和期望输出 <code>H(x)</code> 之间的“差异”或“残差” <code>F(x)</code>。</li>
<li><code>nn.BatchNorm2d</code> (批量归一化) 是一个重要的辅助层，它可以稳定和加速训练过程。</li>
</ul>
</li>
<li><strong>快捷连接 (<code>self.shortcut</code>)</strong>:<ul>
<li>这是ResNet的灵魂。它的目标是让输入 <code>x</code> 能够直接与主路径的输出 <code>out</code> 相加。</li>
<li><strong><code>if stride != 1 or in_channels != out_channels:</code></strong>: 这是一个至关重要的判断。加法要求两个张量的维度必须完全相同。这个<code>if</code>检查了两种维度可能发生变化的情况：<ul>
<li><code>stride != 1</code>: 如果步长为2，主路径的输出图像尺寸会减半。</li>
<li><code>in_channels != out_channels</code>: 如果输入通道数和输出通道数不同。</li>
</ul>
</li>
<li><strong>如果维度不匹配</strong>，快捷连接就不能直接“跳过”，它也需要经过一个简单的变换来匹配主路径输出的维度。这个变换通常是一个1x1的卷积，它的作用就是调整通道数和尺寸。</li>
</ul>
</li>
</ol>
<h5 id="forward-前向传播-数据如何在两条路上跑"><a href="#forward-前向传播-数据如何在两条路上跑" class="headerlink" title="forward (前向传播 - 数据如何在两条路上跑)"></a><code>forward</code> (前向传播 - 数据如何在两条路上跑)</h5><ol>
<li><code>out = ...</code>: 输入 <code>x</code> 先走一遍主路径，得到变换后的结果 <code>F(x)</code>。</li>
<li><code>out += self.shortcut(x)</code>: 将主路径的结果 <code>F(x)</code> 与（可能经过变换的）原始输入 <code>x</code> <strong>逐元素相加</strong>。这一步就是梯度的“高速公路”的入口，确保了梯度可以无损地反向传播。</li>
<li><code>out = self.relu(out)</code>: 对相加后的结果进行最终的激活。</li>
</ol>
<hr>
<h4 id="第二部分-ResNet-用“乐高积木”搭建城堡"><a href="#第二部分-ResNet-用“乐高积木”搭建城堡" class="headerlink" title="第二部分: ResNet - 用“乐高积木”搭建城堡"></a><strong>第二部分: <code>ResNet</code> - 用“乐高积木”搭建城堡</strong></h4><p>这个类使用我们上面定义的 <code>ResidualBlock</code> 作为基本组件，来搭建一个完整的、分阶段的ResNet模型。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ResNet</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, block, num_blocks, num_classes=<span class="number">10</span></span>):</span><br><span class="line">        <span class="comment"># ... (init部分)</span></span><br><span class="line">        <span class="comment"># 初始卷积层</span></span><br><span class="line">        self.conv1 = nn.Conv2d(<span class="number">1</span>, <span class="number">16</span>, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(<span class="number">16</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 堆叠残差块</span></span><br><span class="line">        self.layer1 = self._make_layer(block, <span class="number">16</span>, num_blocks[<span class="number">0</span>], stride=<span class="number">1</span>)</span><br><span class="line">        self.layer2 = self._make_layer(block, <span class="number">32</span>, num_blocks[<span class="number">1</span>], stride=<span class="number">2</span>)</span><br><span class="line">        self.layer3 = self._make_layer(block, <span class="number">64</span>, num_blocks[<span class="number">2</span>], stride=<span class="number">2</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 分类器</span></span><br><span class="line">        self.avg_pool = nn.AdaptiveAvgPool2d((<span class="number">1</span>, <span class="number">1</span>))</span><br><span class="line">        self.fc = nn.Linear(<span class="number">64</span>, num_classes)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_make_layer</span>(<span class="params">self, block, out_channels, num_blocks, stride</span>):</span><br><span class="line">        <span class="comment"># ... (辅助函数)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x))) <span class="comment"># 1. 初始卷积</span></span><br><span class="line">        out = self.layer1(out)                   <span class="comment"># 2. 第一个残差阶段</span></span><br><span class="line">        out = self.layer2(out)                   <span class="comment"># 3. 第二个残差阶段</span></span><br><span class="line">        out = self.layer3(out)                   <span class="comment"># 4. 第三个残差阶段</span></span><br><span class="line">        out = self.avg_pool(out)                 <span class="comment"># 5. 全局平均池化</span></span><br><span class="line">        out = out.view(out.size(<span class="number">0</span>), -<span class="number">1</span>)          <span class="comment"># 6. 压平</span></span><br><span class="line">        out = self.fc(out)                       <span class="comment"># 7. 全连接分类</span></span><br><span class="line">        <span class="keyword">return</span> out</span><br></pre></td></tr></table></figure>

<h5 id="init-初始化-定义模型蓝图"><a href="#init-初始化-定义模型蓝图" class="headerlink" title="__init__ (初始化 - 定义模型蓝图)"></a><code>__init__</code> (初始化 - 定义模型蓝图)</h5><ol>
<li><strong>初始卷积层 (<code>self.conv1</code>, <code>self.bn1</code>)</strong>: 在进入残差块之前，对输入图片进行一次初步的特征提取。</li>
<li><strong>残差层 (<code>self.layer1</code>, <code>layer2</code>, <code>layer3</code>)</strong>:<ul>
<li>这是模型的主体，由 <code>_make_layer</code> 这个辅助函数创建。</li>
<li><code>self.layer1</code>: <code>stride=1</code>，<strong>不改变</strong>图像尺寸，只加深通道。</li>
<li><code>self.layer2</code>: <code>stride=2</code>，将图像尺寸<strong>减半</strong>，并加深通道。</li>
<li><code>self.layer3</code>: <code>stride=2</code>，将图像尺寸<strong>再次减半</strong>，并加深通道。</li>
</ul>
</li>
<li><strong>分类器 (<code>self.avg_pool</code>, <code>self.fc</code>)</strong>:<ul>
<li><code>nn.AdaptiveAvgPool2d((1, 1))</code>: 这是一个非常智能的池化层。不管输入的特征图尺寸是多大（比如<code>7x7</code>或<code>5x5</code>），它都能将其降维成<code>1x1</code>。它通过计算每个通道特征图的平均值来实现。这使得模型对输入图片尺寸的变化更具鲁棒性。</li>
<li><code>self.fc</code>: 最后的标准全连接层，用于最终的分类。</li>
</ul>
</li>
</ol>
<h5 id="make-layer-辅助函数-“施工队”"><a href="#make-layer-辅助函数-“施工队”" class="headerlink" title="_make_layer (辅助函数 - “施工队”)"></a><code>_make_layer</code> (辅助函数 - “施工队”)</h5><ul>
<li>这个函数的作用是“修建”一个完整的残差阶段，比如 <code>self.layer2</code>。</li>
<li>一个阶段包含 <code>num_blocks</code> 个残差块。</li>
<li><code>strides = [stride] + [1]*(num_blocks-1)</code>: 这个聪明的写法确保了在一个阶段中，只有<strong>第一个</strong>残差块可能会进行下采样（<code>stride=2</code>），而后续的所有块都保持尺寸不变（<code>stride=1</code>）。</li>
<li><code>self.in_channels = out_channels</code>: 这是一个重要的状态更新，确保下一个阶段的输入通道数是正确的。</li>
</ul>
<h5 id="forward-前向传播-数据的完整旅程"><a href="#forward-前向传播-数据的完整旅程" class="headerlink" title="forward (前向传播 - 数据的完整旅程)"></a><code>forward</code> (前向传播 - 数据的完整旅程)</h5><p><code>forward</code>方法清晰地展示了数据是如何流经整个ResNet的：从初始卷积，到一系列的残差块堆叠，再到最后的池化和分类，一气呵成。</p>
<ol>
<li><strong>1x1 卷积的角色</strong><ul>
<li>作用：调整通道数 (<code>out_channels</code>) 和空间尺寸 (<code>stride</code>)。</li>
<li>使 <code>F(x)</code> 和 <code>x</code> 的维度一致，从而可相加。</li>
</ul>
</li>
<li><strong><code>bias=False</code> in Conv2d</strong><ul>
<li>原因：卷积后的 <code>BatchNorm</code> 会抵消偏置，因此可省略，减少冗余参数。</li>
</ul>
</li>
<li><strong><code>inplace=True</code> in ReLU</strong><ul>
<li>原因：节省内存，直接在输入张量上修改数据，适用于 <code>conv → bn → relu</code> 链。</li>
</ul>
</li>
</ol>
<h5 id="ResNet-结构图与维度变化"><a href="#ResNet-结构图与维度变化" class="headerlink" title="ResNet 结构图与维度变化"></a><strong>ResNet 结构图与维度变化</strong></h5><p>假设输入：[Batch_Size, 1, 28, 28]</p>
<p>（例如：[64, 1, 28, 28]，即64张28x28的灰度图）</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">                                      输入图片 (x)</span><br><span class="line">                                      [B, <span class="number">1</span>, <span class="number">28</span>, <span class="number">28</span>]</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line"><span class="number">1.</span> 初始卷积层 (Initial Conv)            `nn.Conv2d(<span class="number">1</span>, <span class="number">16</span>, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)`</span><br><span class="line">                                      `nn.BatchNorm2d(<span class="number">16</span>)`</span><br><span class="line">                                      `nn.ReLU()`</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      输出形状: [B, <span class="number">16</span>, <span class="number">28</span>, <span class="number">28</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line"><span class="number">2.</span> Layer <span class="number">1</span> (残差阶段<span class="number">1</span>)	`self.layer1 = self._make_layer(ResidualBlock, <span class="number">16</span>, num_blocks[<span class="number">0</span>]=<span class="number">2</span>,stride=<span class="number">1</span>)`</span><br><span class="line">   - 包含 <span class="number">2</span> 个 ResidualBlock (通道数: <span class="number">16</span> -&gt; <span class="number">16</span>, 尺寸: 28x28 -&gt; 28x28)</span><br><span class="line">   - 每个ResidualBlock内部:</span><br><span class="line">     - 主路径: Conv(<span class="number">16</span>,<span class="number">16</span>,k3,s1,p1) -&gt; BN -&gt; ReLU -&gt; Conv(<span class="number">16</span>,<span class="number">16</span>,k3,s1,p1) -&gt; BN</span><br><span class="line">     - 快捷连接: (直连, 因为通道和尺寸都匹配)</span><br><span class="line">     - F(x) + x -&gt; ReLU</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      输出形状: [B, <span class="number">16</span>, <span class="number">28</span>, <span class="number">28</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line"><span class="number">3.</span> Layer <span class="number">2</span> (残差阶段<span class="number">2</span>)	`self.layer2 = self._make_layer(ResidualBlock, <span class="number">32</span>, num_blocks[<span class="number">1</span>]=<span class="number">2</span>,stride=<span class="number">2</span>)`</span><br><span class="line">   - 包含 <span class="number">2</span> 个 ResidualBlock (通道数: <span class="number">16</span> -&gt; <span class="number">32</span>, 尺寸: 28x28 -&gt; 14x14)</span><br><span class="line">   - **第一个** ResidualBlock (下采样):</span><br><span class="line">     - 主路径: Conv(<span class="number">16</span>,<span class="number">32</span>,k3,s2,p1) -&gt; BN -&gt; ReLU -&gt; Conv(<span class="number">32</span>,<span class="number">32</span>,k3,s1,p1) -&gt; BN</span><br><span class="line">     - 快捷连接: **Conv(<span class="number">16</span>,<span class="number">32</span>,k1,s2) -&gt; BN** &lt;-- 1x1卷积调整通道和尺寸</span><br><span class="line">     - F(x) + x<span class="string">&#x27; -&gt; ReLU</span></span><br><span class="line"><span class="string">   - **第二个** ResidualBlock (尺寸不变):</span></span><br><span class="line"><span class="string">     - 主路径: Conv(32,32,k3,s1,p1) -&gt; BN -&gt; ReLU -&gt; Conv(32,32,k3,s1,p1) -&gt; BN</span></span><br><span class="line"><span class="string">     - 快捷连接: (直连)</span></span><br><span class="line"><span class="string">     - F(x) + x -&gt; ReLU</span></span><br><span class="line"><span class="string">                                            |</span></span><br><span class="line"><span class="string">                                            v</span></span><br><span class="line"><span class="string">                                      输出形状: [B, 32, 14, 14]</span></span><br><span class="line"><span class="string">------------------------------------------------------------------------------------------------------------</span></span><br><span class="line"><span class="string">                                            |</span></span><br><span class="line"><span class="string">                                            v</span></span><br><span class="line"><span class="string">4. Layer 3 (残差阶段3)	`self.layer3 = self._make_layer(ResidualBlock, 64, num_blocks[2]=2, stride=2)`</span></span><br><span class="line"><span class="string">   - 包含 2 个 ResidualBlock (通道数: 32 -&gt; 64, 尺寸: 14x14 -&gt; 7x7)</span></span><br><span class="line"><span class="string">   - **第一个** ResidualBlock (下采样):</span></span><br><span class="line"><span class="string">     - 主路径: Conv(32,64,k3,s2,p1) -&gt; BN -&gt; ReLU -&gt; Conv(64,64,k3,s1,p1) -&gt; BN</span></span><br><span class="line"><span class="string">     - 快捷连接: **Conv(32,64,k1,s2) -&gt; BN** &lt;-- 1x1卷积调整通道和尺寸</span></span><br><span class="line"><span class="string">     - F(x) + x&#x27;</span> -&gt; ReLU</span><br><span class="line">   - **第二个** ResidualBlock (尺寸不变):</span><br><span class="line">     - 主路径: Conv(<span class="number">64</span>,<span class="number">64</span>,k3,s1,p1) -&gt; BN -&gt; ReLU -&gt; Conv(<span class="number">64</span>,<span class="number">64</span>,k3,s1,p1) -&gt; BN</span><br><span class="line">     - 快捷连接: (直连)</span><br><span class="line">     - F(x) + x -&gt; ReLU</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      输出形状: [B, <span class="number">64</span>, <span class="number">7</span>, <span class="number">7</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line"><span class="number">5.</span> 全局平均池化 (Global Average Pooling)   `nn.AdaptiveAvgPool2d((<span class="number">1</span>, <span class="number">1</span>))`</span><br><span class="line">   - 将每个 7x7 的特征图池化成 1x1 的值 (计算平均值)</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      输出形状: [B, <span class="number">64</span>, <span class="number">1</span>, <span class="number">1</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line"><span class="number">6.</span> 压平 (Flatten)                          `out.view(out.size(<span class="number">0</span>), -<span class="number">1</span>)`</span><br><span class="line">   - 将 `[B, <span class="number">64</span>, <span class="number">1</span>, <span class="number">1</span>]` 变为 `[B, <span class="number">64</span>]`</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      输出形状: [B, <span class="number">64</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line"><span class="number">7.</span> 全连接分类器 (Fully Connected Classifier) `nn.Linear(<span class="number">64</span>, <span class="number">10</span>)`</span><br><span class="line">   - 将 <span class="number">64</span> 维特征映射到 <span class="number">10</span> 个类别得分</span><br><span class="line">                                            |</span><br><span class="line">                                            v</span><br><span class="line">                                      最终输出: [B, <span class="number">10</span>]</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br></pre></td></tr></table></figure>

<h2 id="2-5-概念解惑"><a href="#2-5-概念解惑" class="headerlink" title="2.5 概念解惑"></a>2.5 概念解惑</h2><h3 id="2-5-1-下采样-Downsampling"><a href="#2-5-1-下采样-Downsampling" class="headerlink" title="2.5.1 下采样(Downsampling)"></a>2.5.1 下采样(Downsampling)</h3><h4 id="核心定义：降低图像分辨率"><a href="#核心定义：降低图像分辨率" class="headerlink" title="核心定义：降低图像分辨率"></a><strong>核心定义：降低图像分辨率</strong></h4><p><strong>下采样</strong>，在计算机视觉的语境下，最直观的理解就是<strong>缩小图像的尺寸</strong>。</p>
<p>想象一下，你有一张 <code>100x100</code> 像素的高清图片，你把它在画图软件里缩小到 <code>50x50</code> 像素。这个过程就是下采样。你为了让图片变小，丢弃了一部分像素信息，保留了最关键的视觉特征。</p>
<p>在卷积神经网络中，下采样指的是降低<strong>特征图 (Feature Map)</strong> 的<strong>空间维度（高度和宽度）</strong>。</p>
<h4 id="CNN中实现下采样的两种主要方式"><a href="#CNN中实现下采样的两种主要方式" class="headerlink" title="CNN中实现下采样的两种主要方式"></a><strong>CNN中实现下采样的两种主要方式</strong></h4><h5 id="1-池化层-Pooling-Layers-，尤其是-nn-MaxPool2d"><a href="#1-池化层-Pooling-Layers-，尤其是-nn-MaxPool2d" class="headerlink" title="1. 池化层 (Pooling Layers)，尤其是 nn.MaxPool2d"></a><strong>1. 池化层 (Pooling Layers)，尤其是 <code>nn.MaxPool2d</code></strong></h5><p>这是最直接、最常见的下采样方法。</p>
<ul>
<li><strong>工作原理</strong>: <code>MaxPool2d(kernel_size=2)</code> 会将输入的特征图分割成一个个不重叠的 <code>2x2</code> 的小方块。然后，在每个小方块中，它只保留<strong>值最大的那一个像素</strong>，并丢弃其他三个。</li>
<li><strong>效果</strong>: 因为它把 <code>2x2</code> 的区域压缩成了 <code>1x1</code> 的区域，所以特征图的高度和宽度都<strong>减半</strong>了。</li>
</ul>
<p><strong>一个简单的例子：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">原始 4x4 区域          经过 MaxPool2d(2) 后</span><br><span class="line">[[1, 2, 5, 6],</span><br><span class="line"> [3, 4, 7, 8],   -&gt;     [[4, 8],</span><br><span class="line"> [9, 0, 1, 2],          [9, 4]]</span><br><span class="line"> [7, 5, 3, 4]]</span><br></pre></td></tr></table></figure>

<ul>
<li>左上角的 <code>[[1, 2], [3, 4]]</code> 中最大的是 <code>4</code>。</li>
<li>右上角的 <code>[[5, 6], [7, 8]]</code> 中最大的是 <code>8</code>。</li>
<li>以此类推…</li>
</ul>
<h5 id="2-带步长-Stride-的卷积-nn-Conv2d-stride-2"><a href="#2-带步长-Stride-的卷积-nn-Conv2d-stride-2" class="headerlink" title="2. 带步长(Stride)的卷积 (nn.Conv2d(..., stride=2))"></a><strong>2. 带步长(Stride)的卷积 (<code>nn.Conv2d(..., stride=2)</code>)</strong></h5><p>这是另一种更现代的下采样方法，我们在<code>ResNet</code>的<code>shortcut</code>连接中学到过。</p>
<ul>
<li><strong>工作原理</strong>: 普通卷积的步长 <code>stride=1</code>，意味着卷积核每次在图像上移动一个像素。而当 <code>stride=2</code> 时，卷积核会<strong>每次跳过一个像素</strong>，移动两个像素的距离。</li>
<li><strong>效果</strong>: 因为卷积核“跳”着走，它进行计算的次数变少了，自然产生的输出特征图的高度和宽度也大约<strong>减半</strong>了。</li>
</ul>
<h4 id="为什么要进行下采样？（三个核心目的）"><a href="#为什么要进行下采样？（三个核心目的）" class="headerlink" title="为什么要进行下采样？（三个核心目的）"></a><strong>为什么要进行下采样？（三个核心目的）</strong></h4><p><strong>1. 减少计算量和参数数量</strong></p>
<ul>
<li>这是最直接的好处。特征图的尺寸减半后，后续卷积层需要处理的数据量就大大减少了（减少了75%！）。这使得我们可以在不耗尽内存和计算资源的情况下，构建更深、更强大的网络。</li>
</ul>
<p><strong>2. 扩大感受野 (Receptive Field)</strong></p>
<ul>
<li>这是<strong>最重要</strong>的 концептуальный 好处。</li>
<li><strong>感受野</strong>指的是输出特征图上的一个像素，对应到<strong>原始输入图像</strong>上的区域大小。</li>
<li>在下采样<strong>之前</strong>，一个 <code>3x3</code> 的卷积核看到的是原始图像上 <code>3x3</code> 的区域。</li>
<li>在一次 <code>2x2</code> 的下采样<strong>之后</strong>，新的特征图上的一个像素就代表了原始图像上一个 <code>2x2</code> 的区域。此时，再对这个新特征图用一个 <code>3x3</code> 的卷积核，它看到的区域实际上对应到了原始图像上一个更大的区域（比如<code>6x6</code>）。</li>
<li><strong>比喻</strong>: 就像你看地图。当你“下采样”（缩小地图）时，你屏幕上的一个点从代表一条街道，变成了代表一个街区，甚至整个城市。</li>
<li><strong>效果</strong>: 下采样让网络中更深层的卷积核能够“看到”更大范围的特征，从而学习到更宏观、更抽象的模式（比如从“边缘”和“曲线”组合成“眼睛”或“轮胎”）。</li>
</ul>
<p><strong>3. 增加特征的平移不变性 (Translation Invariance)</strong></p>
<ul>
<li><code>MaxPool2d</code>尤其能带来这个好处。如果在一个 <code>2x2</code> 的区域内，那个最重要的特征（最大值）稍微移动了一下位置，但仍然在这个 <code>2x2</code> 的区域内，那么池化后的输出是<strong>完全一样</strong>的。</li>
<li><strong>效果</strong>: 这让模型对目标物体在图像中的微小位移不那么敏感。无论数字“7”的笔画稍微偏左还是偏右一点，模型都能稳定地识别出它是“7”。</li>
</ul>
<p><strong>总结</strong>: 下采样是CNN中的一个核心操作。它通过牺牲空间分辨率，换来了<strong>计算效率的提升</strong>、<strong>感受野的扩大</strong>和<strong>特征的鲁棒性</strong>，是CNN能够学习到从低级到高级的层次化特征的关键所在。</p>
<hr>
<h3 id="2-6-ResNet-Fasion-MNIST完整代码"><a href="#2-6-ResNet-Fasion-MNIST完整代码" class="headerlink" title="2.6 ResNet-Fasion-MNIST完整代码"></a>2.6 ResNet-Fasion-MNIST完整代码</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> torchvision <span class="keyword">import</span> datasets, transforms</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"></span><br><span class="line">LR = <span class="number">0.001</span></span><br><span class="line">EPOCHS = <span class="number">10</span></span><br><span class="line">BATCH_SIZE = <span class="number">128</span></span><br><span class="line">DEVICE = torch.device(<span class="string">&#x27;cuda&#x27;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&#x27;cpu&#x27;</span>)</span><br><span class="line"></span><br><span class="line">transform = transforms.Compose([</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize((<span class="number">0.2860</span>,), (<span class="number">0.3530</span>,))</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line">train_dataset = datasets.FashionMNIST(</span><br><span class="line">    root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">True</span>, transform=transform, download=<span class="literal">True</span></span><br><span class="line">)</span><br><span class="line">test_dataset = datasets.FashionMNIST(</span><br><span class="line">    root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">False</span>, transform=transform, download=<span class="literal">True</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>)</span><br><span class="line">test_loader = DataLoader(dataset=test_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ResidualBlock</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, in_channels, out_channels, stride</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># Main Path</span></span><br><span class="line">        self.conv1 = nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">3</span>, stride=stride, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(out_channels)</span><br><span class="line">        self.relu = nn.ReLU(inplace=<span class="literal">True</span>)</span><br><span class="line">        self.conv2 = nn.Conv2d(out_channels, out_channels, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn2 = nn.BatchNorm2d(out_channels)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Shortcut</span></span><br><span class="line">        self.shortcut = nn.Sequential()</span><br><span class="line">        <span class="keyword">if</span> stride != <span class="number">1</span> <span class="keyword">or</span> in_channels != out_channels:</span><br><span class="line">            self.shortcut = nn.Sequential(</span><br><span class="line">                nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">1</span>, stride=stride, bias=<span class="literal">False</span>),</span><br><span class="line">                nn.BatchNorm2d(out_channels)</span><br><span class="line">            )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># Main Path</span></span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = self.bn2(self.conv2(out))</span><br><span class="line">        <span class="comment"># Shortcut</span></span><br><span class="line">        out += self.shortcut(x)</span><br><span class="line">        out = self.relu(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ResNet</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, block, num_blocks, num_classes=<span class="number">10</span></span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.in_channel = <span class="number">16</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># Initial Conv</span></span><br><span class="line">        self.conv1 = nn.Conv2d(<span class="number">1</span>, <span class="number">16</span>, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(<span class="number">16</span>)</span><br><span class="line">        self.relu = nn.ReLU(inplace=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Residual Blocks</span></span><br><span class="line">        self.layer1 = self._make_layer(block, <span class="number">16</span>, num_blocks[<span class="number">0</span>], stride=<span class="number">1</span>)</span><br><span class="line">        self.layer2 = self._make_layer(block, <span class="number">32</span>, num_blocks[<span class="number">1</span>], stride=<span class="number">2</span>)</span><br><span class="line">        self.layer3 = self._make_layer(block, <span class="number">64</span>, num_blocks[<span class="number">2</span>], stride=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Global Average Pooling &amp; Fully Connected Classifier</span></span><br><span class="line">        self.avg_pool = nn.AdaptiveAvgPool2d((<span class="number">1</span>, <span class="number">1</span>))</span><br><span class="line">        self.fc = nn.Linear(<span class="number">64</span>, num_classes)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_make_layer</span>(<span class="params">self, block, out_channels, num_blocks, stride</span>):</span><br><span class="line">        strides = [stride] + [<span class="number">1</span>]*(num_blocks - <span class="number">1</span>)</span><br><span class="line">        layers = []</span><br><span class="line">        <span class="keyword">for</span> s <span class="keyword">in</span> strides:</span><br><span class="line">            layers.append(block(self.in_channel, out_channels, s))</span><br><span class="line">            self.in_channel = out_channels</span><br><span class="line">        <span class="keyword">return</span> nn.Sequential(*layers)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = self.layer1(out)</span><br><span class="line">        out = self.layer2(out)</span><br><span class="line">        out = self.layer3(out)</span><br><span class="line">        out = self.avg_pool(out)</span><br><span class="line">        out = out.view(out.size(<span class="number">0</span>), -<span class="number">1</span>)</span><br><span class="line">        out = self.fc(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line">model = ResNet(ResidualBlock, [<span class="number">2</span>, <span class="number">2</span>, <span class="number">2</span>]).to(DEVICE)</span><br><span class="line">criterion = nn.CrossEntropyLoss()</span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line">scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=<span class="number">3</span>, gamma=<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_one_epoch</span>(<span class="params">model, loader, criterion, optimizer</span>):</span><br><span class="line">    model.train()</span><br><span class="line">    running_loss = <span class="number">0.0</span></span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> loader:</span><br><span class="line">        features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line">        running_loss += loss.item()</span><br><span class="line">    <span class="keyword">return</span> running_loss / <span class="built_in">len</span>(loader)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">evaluate</span>(<span class="params">model, loader</span>):</span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    all_preds, all_labels = [], []</span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">for</span> features, labels <span class="keyword">in</span> loader:</span><br><span class="line">            features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line">            outputs = model(features)</span><br><span class="line">            _, preds = torch.<span class="built_in">max</span>(outputs, <span class="number">1</span>)</span><br><span class="line">            all_labels.extend(labels.cpu().numpy())</span><br><span class="line">            all_preds.extend(preds.cpu().numpy())</span><br><span class="line">        acc = accuracy_score(all_labels, all_preds)</span><br><span class="line">        <span class="keyword">return</span> acc, all_labels, all_preds</span><br><span class="line"></span><br><span class="line">train_losses, val_accuracies = [], []</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    train_loss = train_one_epoch(model, train_loader, criterion, optimizer)</span><br><span class="line">    val_acc, all_labels, all_preds = evaluate(model, test_loader)</span><br><span class="line"></span><br><span class="line">    train_losses.append(train_loss)</span><br><span class="line">    val_accuracies.append(val_acc)</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;Epoch <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span>/<span class="subst">&#123;EPOCHS&#125;</span> | Loss: <span class="subst">&#123;train_loss:<span class="number">.4</span>f&#125;</span> | Val Acc: <span class="subst">&#123;val_acc*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line">    scheduler.step()</span><br></pre></td></tr></table></figure>

<h3 id="2-7-ResNet-应用到更具挑战性的彩色图片数据集——CIFAR-10"><a href="#2-7-ResNet-应用到更具挑战性的彩色图片数据集——CIFAR-10" class="headerlink" title="2.7 ResNet 应用到更具挑战性的彩色图片数据集——CIFAR-10"></a>2.7 ResNet 应用到更具挑战性的彩色图片数据集——CIFAR-10</h3><p>新的挑战和学习点：</p>
<ul>
<li><strong>彩色图像 (3通道)</strong>：如何处理彩色图片作为输入，以及这会如何影响你的第一个卷积层。</li>
<li><strong>更复杂的特征</strong>: CIFAR-10 的图片比 Fashion-MNIST 更具多样性，需要模型学习更抽象的视觉特征。</li>
<li><strong>数据增强 (Data Augmentation)</strong>：为了让模型更好地泛化，我们将介绍并应用一些基本的数据增强技术。</li>
</ul>
<h4 id="核心修改点解析："><a href="#核心修改点解析：" class="headerlink" title="核心修改点解析："></a><strong>核心修改点解析：</strong></h4><ol>
<li><p><strong>数据加载 (<code>datasets.CIFAR10</code>)</strong>: 切换到CIFAR-10数据集。</p>
</li>
<li><p><strong><code>transforms.Normalize</code> 参数</strong>:</p>
<ul>
<li>CIFAR-10是彩色图片，所以 <code>mean</code> (均值) 和 <code>std</code> (标准差) 都变成了包含三个值的元组，分别对应R、G、B三个通道。这些是根据整个CIFAR-10训练集的统计数据计算出的。</li>
</ul>
</li>
<li><p><strong>数据增强 (<code>train_transform</code>)</strong>:</p>
<ul>
<li><code>transforms.RandomCrop(32, padding=4)</code>: 随机裁剪，先在图像边缘填充4个像素，然后在 <code>32x32</code> 的区域内随机裁剪出 <code>32x32</code> 的图像。这有助于模型学习到物体在图片中的不同位置。</li>
<li><code>transforms.RandomHorizontalFlip()</code>: 随机水平翻转图像。这有助于模型学习到物体左右翻转后的特征。</li>
<li>这些技术能有效地增加训练数据的多样性，防止模型过拟合，提高泛化能力。</li>
</ul>
</li>
<li><p><strong><code>ResNet</code> 类的 <code>conv1</code> 层</strong>:</p>
<ul>
<li><code>self.conv1 = nn.Conv2d(3, 16, ...)</code>: <strong>最关键的修改</strong>！因为CIFAR-10是RGB三通道彩色图像，所以第一个卷积层的 <code>in_channels</code> 必须从 <code>1</code> (灰度图) 变为 <code>3</code>。</li>
</ul>
</li>
<li><p><strong><code>EPOCHS</code> 和 <code>scheduler.step_size</code></strong>:</p>
<ul>
<li>CIFAR-10比Fashion-MNIST更难，因此通常需要更多的训练周期 (<code>EPOCHS</code>)。</li>
<li><code>StepLR</code> 的 <code>step_size</code> 也进行了调整，表示每隔多少个 epoch 学习率衰减一次。</li>
</ul>
<h4 id="完整代码："><a href="#完整代码：" class="headerlink" title="完整代码："></a>完整代码：</h4></li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">from</span> torchvision <span class="keyword">import</span> datasets, transforms</span><br><span class="line"><span class="keyword">from</span> torch.utils.data <span class="keyword">import</span> DataLoader</span><br><span class="line"><span class="keyword">from</span> sklearn.metrics <span class="keyword">import</span> accuracy_score</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line">LR = <span class="number">0.1</span></span><br><span class="line">EPOCHS = <span class="number">100</span></span><br><span class="line">BATCH_SIZE = <span class="number">128</span></span><br><span class="line">DEVICE = torch.device(<span class="string">&#x27;cuda&#x27;</span> <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&#x27;cpu&#x27;</span>)</span><br><span class="line"></span><br><span class="line">CIFAR10_MEAN = (<span class="number">0.4914</span>, <span class="number">0.4822</span>, <span class="number">0.4465</span>)</span><br><span class="line">CIFAR10_STD = (<span class="number">0.2471</span>, <span class="number">0.2435</span>, <span class="number">0.2616</span>)</span><br><span class="line"></span><br><span class="line">train_transform = transforms.Compose([</span><br><span class="line">    transforms.RandomCrop(size=<span class="number">32</span>, padding=<span class="number">4</span>),</span><br><span class="line">    transforms.RandomHorizontalFlip(),</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize(CIFAR10_MEAN, CIFAR10_STD)</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line">test_transform = transforms.Compose([</span><br><span class="line">    transforms.ToTensor(),</span><br><span class="line">    transforms.Normalize(CIFAR10_MEAN, CIFAR10_STD)</span><br><span class="line">])</span><br><span class="line"></span><br><span class="line">train_dataset = datasets.CIFAR10(</span><br><span class="line">    root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">True</span>, transform=train_transform, download=<span class="literal">True</span></span><br><span class="line">)</span><br><span class="line">test_dataset = datasets.CIFAR10(</span><br><span class="line">    root=<span class="string">&#x27;../datasets&#x27;</span>, train=<span class="literal">False</span>, transform=test_transform, download=<span class="literal">True</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">train_loader = DataLoader(dataset=train_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">True</span>, num_workers=<span class="number">2</span>)</span><br><span class="line">test_loader = DataLoader(dataset=test_dataset, batch_size=BATCH_SIZE, shuffle=<span class="literal">False</span>, num_workers=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ResidualBlock</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, in_channels, out_channels, stride</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># Main Path</span></span><br><span class="line">        self.conv1 = nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">3</span>, stride=stride, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(out_channels)</span><br><span class="line">        self.relu = nn.ReLU(inplace=<span class="literal">True</span>)</span><br><span class="line">        self.conv2 = nn.Conv2d(out_channels, out_channels, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn2 = nn.BatchNorm2d(out_channels)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Shortcut</span></span><br><span class="line">        self.shortcut = nn.Sequential()</span><br><span class="line">        <span class="keyword">if</span> stride != <span class="number">1</span> <span class="keyword">or</span> in_channels != out_channels:</span><br><span class="line">            self.shortcut = nn.Sequential(</span><br><span class="line">                nn.Conv2d(in_channels, out_channels, kernel_size=<span class="number">1</span>, stride=stride, bias=<span class="literal">False</span>),</span><br><span class="line">                nn.BatchNorm2d(out_channels)</span><br><span class="line">            )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># Main Path</span></span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = self.bn2(self.conv2(out))</span><br><span class="line">        <span class="comment"># Shortcut</span></span><br><span class="line">        out += self.shortcut(x)</span><br><span class="line">        out = self.relu(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ResNet</span>(nn.Module):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, block, num_blocks, num_classes=<span class="number">10</span></span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        self.in_channel = <span class="number">16</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># Initial Conv</span></span><br><span class="line">        self.conv1 = nn.Conv2d(<span class="number">3</span>, <span class="number">16</span>, kernel_size=<span class="number">3</span>, stride=<span class="number">1</span>, padding=<span class="number">1</span>, bias=<span class="literal">False</span>)</span><br><span class="line">        self.bn1 = nn.BatchNorm2d(<span class="number">16</span>)</span><br><span class="line">        self.relu = nn.ReLU(inplace=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Residual Blocks</span></span><br><span class="line">        self.layer1 = self._make_layer(block, <span class="number">16</span>, num_blocks[<span class="number">0</span>], stride=<span class="number">1</span>)</span><br><span class="line">        self.layer2 = self._make_layer(block, <span class="number">32</span>, num_blocks[<span class="number">1</span>], stride=<span class="number">2</span>)</span><br><span class="line">        self.layer3 = self._make_layer(block, <span class="number">64</span>, num_blocks[<span class="number">2</span>], stride=<span class="number">2</span>)</span><br><span class="line">        self.layer4 = self._make_layer(block, <span class="number">128</span>, num_blocks[<span class="number">3</span>], stride=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># Global Average Pooling &amp; Fully Connected Classifier</span></span><br><span class="line">        self.avg_pool = nn.AdaptiveAvgPool2d((<span class="number">1</span>, <span class="number">1</span>))</span><br><span class="line">        self.fc = nn.Sequential(</span><br><span class="line">            nn.Dropout(<span class="number">0.3</span>),</span><br><span class="line">            nn.Linear(<span class="number">128</span>, <span class="number">256</span>),</span><br><span class="line">            nn.ReLU(inplace=<span class="literal">True</span>),</span><br><span class="line">            nn.Dropout(<span class="number">0.3</span>),</span><br><span class="line">            nn.Linear(<span class="number">256</span>, num_classes)</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_make_layer</span>(<span class="params">self, block, out_channels, num_blocks, stride</span>):</span><br><span class="line">        strides = [stride] + [<span class="number">1</span>]*(num_blocks - <span class="number">1</span>)</span><br><span class="line">        layers = []</span><br><span class="line">        <span class="keyword">for</span> s <span class="keyword">in</span> strides:</span><br><span class="line">            layers.append(block(self.in_channel, out_channels, s))</span><br><span class="line">            self.in_channel = out_channels</span><br><span class="line">        <span class="keyword">return</span> nn.Sequential(*layers)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        out = self.relu(self.bn1(self.conv1(x)))</span><br><span class="line">        out = self.layer1(out)</span><br><span class="line">        out = self.layer2(out)</span><br><span class="line">        out = self.layer3(out)</span><br><span class="line">        out = self.layer4(out)</span><br><span class="line">        out = self.avg_pool(out)</span><br><span class="line">        out = out.view(out.size(<span class="number">0</span>), -<span class="number">1</span>)</span><br><span class="line">        out = self.fc(out)</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br><span class="line">model = ResNet(ResidualBlock, [<span class="number">3</span>, <span class="number">4</span>, <span class="number">6</span>, <span class="number">3</span>]).to(DEVICE)</span><br><span class="line"><span class="comment"># criterion = nn.CrossEntropyLoss()</span></span><br><span class="line">criterion = nn.CrossEntropyLoss(label_smoothing=<span class="number">0.1</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span></span><br><span class="line"><span class="comment"># scheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=7, gamma=0.1)</span></span><br><span class="line"></span><br><span class="line">optimizer = torch.optim.SGD(</span><br><span class="line">    model.parameters(),</span><br><span class="line">    lr=LR, momentum=<span class="number">0.9</span>, weight_decay=<span class="number">5e-4</span></span><br><span class="line">)</span><br><span class="line">scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=EPOCHS)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">train_one_epoch</span>(<span class="params">model, loader, criterion, optimizer</span>):</span><br><span class="line">    model.train()</span><br><span class="line">    running_loss = <span class="number">0.0</span></span><br><span class="line">    <span class="keyword">for</span> features, labels <span class="keyword">in</span> loader:</span><br><span class="line">        features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line">        outputs = model(features)</span><br><span class="line">        loss = criterion(outputs, labels)</span><br><span class="line"></span><br><span class="line">        optimizer.zero_grad()</span><br><span class="line">        loss.backward()</span><br><span class="line">        optimizer.step()</span><br><span class="line">        running_loss += loss.item()</span><br><span class="line">    <span class="keyword">return</span> running_loss / <span class="built_in">len</span>(loader)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">evaluate</span>(<span class="params">model, loader</span>):</span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    all_preds, all_labels = [], []</span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">for</span> features, labels <span class="keyword">in</span> loader:</span><br><span class="line">            features, labels = features.to(DEVICE), labels.to(DEVICE)</span><br><span class="line">            outputs = model(features)</span><br><span class="line">            _, preds = torch.<span class="built_in">max</span>(outputs, <span class="number">1</span>)</span><br><span class="line">            all_labels.extend(labels.cpu().numpy())</span><br><span class="line">            all_preds.extend(preds.cpu().numpy())</span><br><span class="line">        acc = accuracy_score(all_labels, all_preds)</span><br><span class="line">        <span class="keyword">return</span> acc, all_labels, all_preds</span><br><span class="line"></span><br><span class="line">train_losses, val_accuracies = [], []</span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    train_loss = train_one_epoch(model, train_loader, criterion, optimizer)</span><br><span class="line">    val_acc, all_labels, all_preds = evaluate(model, test_loader)</span><br><span class="line"></span><br><span class="line">    train_losses.append(train_loss)</span><br><span class="line">    val_accuracies.append(val_acc)</span><br><span class="line"></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;Epoch <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span>/<span class="subst">&#123;EPOCHS&#125;</span> | Loss: <span class="subst">&#123;train_loss:<span class="number">.4</span>f&#125;</span> | Val Acc: <span class="subst">&#123;val_acc*<span class="number">100</span>:<span class="number">.2</span>f&#125;</span>%&#x27;</span>)</span><br><span class="line">    scheduler.step()</span><br><span class="line"></span><br><span class="line">plt.figure(figsize=(<span class="number">10</span>,<span class="number">4</span>))</span><br><span class="line">plt.subplot(<span class="number">1</span>,<span class="number">2</span>,<span class="number">1</span>)</span><br><span class="line">plt.plot(train_losses, label=<span class="string">&quot;Train Loss&quot;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&quot;Epoch&quot;</span>); plt.ylabel(<span class="string">&quot;Loss&quot;</span>); plt.legend()</span><br><span class="line"></span><br><span class="line">plt.subplot(<span class="number">1</span>,<span class="number">2</span>,<span class="number">2</span>)</span><br><span class="line">plt.plot(val_accuracies, label=<span class="string">&quot;Val Accuracy&quot;</span>, color=<span class="string">&quot;orange&quot;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&quot;Epoch&quot;</span>); plt.ylabel(<span class="string">&quot;Accuracy&quot;</span>); plt.legend()</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>

<h2 id="Part3-训练技巧"><a href="#Part3-训练技巧" class="headerlink" title="Part3 训练技巧"></a>Part3 训练技巧</h2><p>在构建了强大的模型架构（如 ResNet）之后，下一步是采用更智能的训练策略来最大化其性能。</p>
<hr>
<h3 id="3-1-正则化-Regularization-：防止模型“死记硬背”"><a href="#3-1-正则化-Regularization-：防止模型“死记硬背”" class="headerlink" title="3.1 正则化 (Regularization)：防止模型“死记硬背”"></a>3.1 正则化 (Regularization)：防止模型“死记硬背”</h3><ul>
<li><strong>问题：过拟合 (Overfitting)</strong><ul>
<li><strong>定义</strong>：当模型在训练数据上表现完美，但在未见过的数据上表现很差时，就发生了过拟合。</li>
<li><strong>类比</strong>：一个学生死记硬背练习册答案，却不会解新题。</li>
</ul>
</li>
<li><strong>解决方案：Dropout</strong><ul>
<li><strong>思想</strong>：在训练时，以概率 <code>p</code> 随机将部分神经元输出置为零。</li>
<li><strong>效果</strong>：迫使网络学到更稳健的特征，减少对单一神经元组合的依赖。</li>
<li><strong>原则</strong>：<ul>
<li>仅在 <code>model.train()</code> 模式下生效；</li>
<li>在 <code>model.eval()</code> 时自动关闭。</li>
</ul>
</li>
</ul>
</li>
</ul>
<blockquote>
<p><strong>实现示例</strong></p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">self.net = nn.Sequential(</span><br><span class="line">    nn.Linear(num_features, <span class="number">128</span>),</span><br><span class="line">    nn.ReLU(),</span><br><span class="line">    nn.Dropout(p=<span class="number">0.3</span>),   <span class="comment"># 全连接层常用较高概率</span></span><br><span class="line">    nn.Linear(<span class="number">128</span>, <span class="number">64</span>),</span><br><span class="line">    nn.ReLU(),</span><br><span class="line">    nn.Dropout(p=<span class="number">0.2</span>),   <span class="comment"># 卷积层后可用较低概率</span></span><br><span class="line">    nn.Linear(<span class="number">64</span>, num_classes)</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="3-2-智能训练循环：早停法与学习率调度"><a href="#3-2-智能训练循环：早停法与学习率调度" class="headerlink" title="3.2 智能训练循环：早停法与学习率调度"></a>3.2 智能训练循环：早停法与学习率调度</h3><ul>
<li><strong>问题</strong>：如何避免过拟合，又能自动找到最佳训练轮数与学习率？</li>
</ul>
<h4 id="早停法-Early-Stopping"><a href="#早停法-Early-Stopping" class="headerlink" title="早停法 (Early Stopping)"></a>早停法 (Early Stopping)</h4><ul>
<li><strong>策略</strong>：<ol>
<li>每个 epoch 结束后计算验证集损失；</li>
<li>若验证损失连续 <code>patience</code> 个 epoch 无改善，则提前终止；</li>
<li>保存验证损失最低时的模型作为最终模型。</li>
</ol>
</li>
<li><strong>效果</strong>：自动找到欠拟合与过拟合之间的平衡点，节省训练时间。</li>
</ul>
<h4 id="学习率调度器-Learning-Rate-Scheduler"><a href="#学习率调度器-Learning-Rate-Scheduler" class="headerlink" title="学习率调度器 (Learning Rate Scheduler)"></a>学习率调度器 (Learning Rate Scheduler)</h4><ul>
<li><strong>策略</strong>：根据验证指标的变化自动调整学习率。</li>
<li><strong>常见调度器</strong>：<ul>
<li><code>ReduceLROnPlateau</code>：验证损失长期不降时，学习率 × factor；</li>
<li><code>StepLR</code>：每隔固定 epoch 衰减学习率；</li>
<li><code>CosineAnnealingLR</code>：余弦退火曲线，常见于 ResNet、Transformer；</li>
<li><code>OneCycleLR</code>：在 NLP、CV 任务中广泛使用。</li>
</ul>
</li>
</ul>
<hr>
<h4 id="智能训练循环框架"><a href="#智能训练循环框架" class="headerlink" title="智能训练循环框架"></a>智能训练循环框架</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> copy</span><br><span class="line"></span><br><span class="line">optimizer = torch.optim.Adam(model.parameters(), lr=LR)</span><br><span class="line">scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(</span><br><span class="line">    optimizer, mode=<span class="string">&#x27;min&#x27;</span>, factor=<span class="number">0.1</span>, patience=<span class="number">5</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">best_val_loss = <span class="built_in">float</span>(<span class="string">&#x27;inf&#x27;</span>)</span><br><span class="line">epochs_no_improve = <span class="number">0</span></span><br><span class="line">early_stop_patience = <span class="number">10</span></span><br><span class="line">best_model_state = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> epoch <span class="keyword">in</span> <span class="built_in">range</span>(EPOCHS):</span><br><span class="line">    <span class="comment"># --- 训练阶段 ---</span></span><br><span class="line">    model.train()</span><br><span class="line">    <span class="comment"># ... 训练代码 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># --- 验证阶段 ---</span></span><br><span class="line">    model.<span class="built_in">eval</span>()</span><br><span class="line">    val_loss = <span class="number">0.0</span></span><br><span class="line">    <span class="keyword">with</span> torch.no_grad():</span><br><span class="line">        <span class="keyword">for</span> x, y <span class="keyword">in</span> val_loader:</span><br><span class="line">            preds = model(x)</span><br><span class="line">            loss = criterion(preds, y)</span><br><span class="line">            val_loss += loss.item()</span><br><span class="line">    avg_val_loss = val_loss / <span class="built_in">len</span>(val_loader)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># --- 学习率调度 ---</span></span><br><span class="line">    scheduler.step(avg_val_loss)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># --- 早停法 ---</span></span><br><span class="line">    <span class="keyword">if</span> avg_val_loss &lt; best_val_loss:</span><br><span class="line">        best_val_loss = avg_val_loss</span><br><span class="line">        epochs_no_improve = <span class="number">0</span></span><br><span class="line">        best_model_state = copy.deepcopy(model.state_dict())</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        epochs_no_improve += <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> epochs_no_improve &gt;= early_stop_patience:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;触发早停：第 <span class="subst">&#123;epoch+<span class="number">1</span>&#125;</span> 个 epoch&quot;</span>)</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 恢复最佳模型</span></span><br><span class="line"><span class="keyword">if</span> best_model_state:</span><br><span class="line">    model.load_state_dict(best_model_state)</span><br></pre></td></tr></table></figure>

<hr>
<h4 id="训练循环逻辑图"><a href="#训练循环逻辑图" class="headerlink" title="训练循环逻辑图"></a>训练循环逻辑图</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">训练开始 → 每个 epoch 结束 → 计算验证损失</span><br><span class="line">     ↓</span><br><span class="line">   是否改善？</span><br><span class="line">     ↓                ↓</span><br><span class="line">   是 → 保存模型   否 → epochs_no_improve+1</span><br><span class="line">     ↓                ↓</span><br><span class="line">继续训练         是否超过耐心值？</span><br><span class="line">                       ↓</span><br><span class="line">                   是 → 提前停止</span><br></pre></td></tr></table></figure>

<hr>
]]></content>
  </entry>
  <entry>
    <title>Win10去除桌面桌面快捷方式小箭头</title>
    <url>/posts/46f6a79.html</url>
    <content><![CDATA[<p>每次重装系统都要去找一下代码 :@(想一想)<br>索性丢到这儿 :@(小眼睛) </p>
<span id="more"></span>

<p>#去除小箭头</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">reg add &quot;HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Shell Icons&quot; /v 29 /d &quot;%systemroot%\system32\imageres.dll,197&quot; /t reg_sz /f</span><br><span class="line">taskkill /f /im explorer.exe</span><br><span class="line">attrib -s -r -h &quot;%userprofile%\AppData\Local\iconcache.db&quot;</span><br><span class="line">del &quot;%userprofile%\AppData\Local\iconcache.db&quot; /f /q</span><br><span class="line">start explorer</span><br><span class="line">pause</span><br></pre></td></tr></table></figure>

<p>#恢复小箭头</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">reg delete &quot;HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\Shell Icons&quot; /v 29 /f</span><br><span class="line">taskkill /f /im explorer.exe</span><br><span class="line">attrib -s -r -h &quot;%userprofile%\AppData\Local\iconcache.db&quot;</span><br><span class="line">del &quot;%userprofile%\AppData\Local\iconcache.db&quot; /f /q</span><br><span class="line">start explorer</span><br><span class="line">paus</span><br></pre></td></tr></table></figure>









]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>Windows引导修复</title>
    <url>/posts/92ae578f.html</url>
    <content><![CDATA[<p>如果遇到windows丢了引导，且手边没有PE修复工具<br>插上Windows安装U盘，按住Shift + F10<br>一句命令行就搞定了 </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">bcdboot c:\windows /l zh-cn</span><br></pre></td></tr></table></figure>
<p>不需要借助额外的工具</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Windows</tag>
      </tags>
  </entry>
  <entry>
    <title>arrow</title>
    <url>/posts/d837ee3e.html</url>
    <content><![CDATA[<p>Arrow-12.0 For UMI Unofficial</p>
<p>Changelog for 20211018 version:</p>
<ul>
<li>First Android 12 release.</li>
</ul>
<p>Notice:</p>
<ul>
<li>Need Stable 12.5.8 firmware.</li>
<li>Use the old LR Team twrp if u want to sideload magisk, etc. Any other TWRP won’t work!</li>
</ul>
<p>Known issues:</p>
<ul>
<li>WFD doesn’t work.</li>
<li>SELinux status is permissive.</li>
</ul>
<p>Download:<br><a class="link"   href="https://drive.google.com/file/d/12flI0tFeEQLeQHnie3JQBWgMs7bZFACT/view?usp=sharing" >Gdrive<i class="fas fa-external-link-alt"></i></a></p>
<p>Screenshot:<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-3.jpg" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-4.jpg" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-5.jpg" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-7.jpg" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-8.jpg" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/arrow-12.0-9.jpg" ></p>
]]></content>
  </entry>
  <entry>
    <title>c盘分多大</title>
    <url>/posts/8f90ce31.html</url>
    <content><![CDATA[<p>SSD不提倡分区，但是习惯了把文件数据啥的和系统分开放。即使系统崩了，重装也不用动别的盘，所以还是坚持单硬盘分区<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/image.3rlkdl2qp9e0.png" ><br>装上了win11正式版，一番配置之后发现C盘分小了:(<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/c.4qgl6cq2azg0.png" ><br>60G平时注意点虽然够用，但是得时常清理来着<br>对于懒人来说，还是100G比较舒服<br>不说了，格式化再来一遍</p>
]]></content>
  </entry>
  <entry>
    <title>diskutil U盘格式化工具</title>
    <url>/posts/894173b7.html</url>
    <content><![CDATA[<p>首先查看你当前系统下有几块磁盘</p>
<pre><code>#终端运行命令
diskutil list
</code></pre>
<p>带有(external, physical)的就是外置磁盘了</p>
<pre><code>#格式化为常用格式FAT32
diskutil partitionDisk /dev/disk2 MBR FAT32 UNTITLED 0b
</code></pre>
<p>这样就可</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Mac OS</tag>
      </tags>
  </entry>
  <entry>
    <title>gcp配置记录</title>
    <url>/posts/d6b069ed.html</url>
    <content><![CDATA[<h3 id="1-开启第三方SSH登录"><a href="#1-开启第三方SSH登录" class="headerlink" title="1 开启第三方SSH登录"></a>1 开启第三方SSH登录</h3><span id="more"></span>

<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ sed -i <span class="string">&#x27;s/#PermitRootLogin prohibit-password/PermitRootLogin yes/g&#x27;</span> /etc/ssh/sshd_config</span><br><span class="line">$ sed -i <span class="string">&#x27;s/PasswordAuthentication no/PasswordAuthentication yes/g&#x27;</span> /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure>
<h3 id="2-添加用户"><a href="#2-添加用户" class="headerlink" title="2 添加用户"></a>2 添加用户</h3><p><code>$ adduser chaptsand</code><br>Or<br><code>$ useradd -m -s /bin/bash chaptsand</code></p>
<h3 id="3-授予sudo权限"><a href="#3-授予sudo权限" class="headerlink" title="3 授予sudo权限"></a>3 授予sudo权限</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">tee</span> /etc/sudoers.d/chaptsand &lt;&lt;&lt; <span class="string">&#x27;chaptsand ALL=(ALL) ALL&#x27;</span></span><br><span class="line">$ <span class="built_in">chmod</span> 440 /etc/sudoers.d/chaptsand</span><br></pre></td></tr></table></figure>
<h3 id="4-Oh-my-zsh"><a href="#4-Oh-my-zsh" class="headerlink" title="4 Oh my zsh"></a>4 Oh my zsh</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ sh -c <span class="string">&quot;<span class="subst">$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)</span>&quot;</span></span><br></pre></td></tr></table></figure>
<h3 id="5-zsh-autosuggestions"><a href="#5-zsh-autosuggestions" class="headerlink" title="5 zsh-autosuggestions"></a>5 zsh-autosuggestions</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ git <span class="built_in">clone</span> git://github.com/zsh-users/zsh-autosuggestions <span class="variable">$ZSH_CUSTOM</span>/plugins/zsh-autosuggestions</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>Paranoid Android Ruby Beta 4</title>
    <url>/posts/7a5406ed.html</url>
    <content><![CDATA[<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/pa-ruby-umi.jpg" ></p>
<p><strong>Paranoid Android 是一个CAF based的自定义ROM，旨在扩展系统，致力于增强 Android 现有的美感，并遵循 Google 为 AOSP项目提出的设计理念。</strong></p>
<h4 id="机型：-小米10-umi"><a href="#机型：-小米10-umi" class="headerlink" title="机型： 小米10 umi"></a>机型： 小米10 umi</h4><h4 id="编译日期：-21-09-08"><a href="#编译日期：-21-09-08" class="headerlink" title="编译日期： 21&#x2F;09&#x2F;08"></a>编译日期： 21&#x2F;09&#x2F;08</h4><h4 id="Selinux-状态：-enforcing"><a href="#Selinux-状态：-enforcing" class="headerlink" title="Selinux 状态： enforcing"></a>Selinux 状态： <code>enforcing</code></h4><h4 id="更新日志："><a href="#更新日志：" class="headerlink" title="更新日志："></a>更新日志：</h4><p>&#x3D;&#x3D;&#x3D;&#x3D;&#x3D; 2021-09-07 &#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</p>
<ul>
<li>修复USB网络共享</li>
<li>禁用口袋模式</li>
<li>内核合并Linux stable 4.19.206</li>
<li>优化续航减少发热</li>
<li>人脸识别支持付款应用</li>
<li>完善人脸解锁自定义设置</li>
</ul>
<p>&#x3D;&#x3D;&#x3D;&#x3D;&#x3D; 2021-09-05 &#x3D;&#x3D;&#x3D;&#x3D;&#x3D;</p>
<ul>
<li>更改系统默认字体为Google Sans + 筑紫A丸</li>
<li>支持微信、支付宝指纹支付</li>
<li>提高SQLite读写性能</li>
<li>开启P3色域颜色模式（自动调节）</li>
<li>更新blobs至<code>LA.UM.9.12.r1-11500-SMxx50.0</code> &amp;&amp; <code>V12.5.7.0.RJBCNXM</code></li>
<li>开启AOD防烧屏机制</li>
<li>内核合并CAF上游tag <code>LA.UM.9.12.r1-12800-SMxx50.0</code> 和 Linux stable 4.19.205</li>
<li>增加了一些实用的自定义功能</li>
<li>美化系统界面</li>
</ul>
<h4 id="下载："><a href="#下载：" class="headerlink" title="下载："></a>下载：</h4><ul>
<li><strong>Vanilla版本： <a class="link"   href="https://github.com/chaptsand/platform_device_xiaomi_umi/releases/download/v4.0-beta/pa-ruby-4-umi-20210908-beta-vanilla.zip" >pa-ruby-4-umi-20210908-beta-vanilla.zip<i class="fas fa-external-link-alt"></i></a></strong></li>
<li><strong>Gapps版本：  <a class="link"   href="https://github.com/chaptsand/platform_device_xiaomi_umi/releases/download/v4.0-beta/pa-ruby-4-umi-20210908-beta-vanilla.zip" >pa-ruby-4-umi-20210907-beta-gapps.zip<i class="fas fa-external-link-alt"></i></a></strong></li>
</ul>
<p><strong><a class="link"   href="https://github.com/chaptsand/platform_device_xiaomi_umi/releases" >发布页面<i class="fas fa-external-link-alt"></i></a></strong></p>
<h4 id="Md5值校验"><a href="#Md5值校验" class="headerlink" title="Md5值校验:"></a>Md5值校验:</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">md5sum</span> pa-ruby-4-umi-20210908-beta-vanilla.zip 5355e382b1a67abb992d7bbe418b7251</span><br><span class="line">$ <span class="built_in">md5sum</span> pa-ruby-4-umi-20210907-beta-gapps.zip 8d9ec28d42dde62c475d0978821dc466</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>Pixel Experience For UMI</title>
    <url>/posts/4ad97f8e.html</url>
    <content><![CDATA[<h3 id="PixelExperience-11-Plus-for-UMI-Final"><a href="#PixelExperience-11-Plus-for-UMI-Final" class="headerlink" title="PixelExperience 11 Plus for UMI Final"></a>PixelExperience 11 Plus for UMI Final</h3><h4 id="编译日期：21-11-14"><a href="#编译日期：21-11-14" class="headerlink" title="编译日期：21&#x2F;11&#x2F;14"></a>编译日期：21&#x2F;11&#x2F;14</h4><p>更新日志：</p>
<ul>
<li>提高系统稳定性</li>
<li>优化系统流畅度</li>
</ul>
<p>链接：<a class="link"   href="https://github.com/chaptsand/android_device_xiaomi_umi/releases/download/11.0-oct/PixelExperience_Plus_umi-11.0-20211014-0839-UNOFFICIAL.zip" > PixelExperience_Plus_umi-11.0-20211014-0839-UNOFFICIAL.zip<i class="fas fa-external-link-alt"></i></a></p>
]]></content>
  </entry>
  <entry>
    <title>rss</title>
    <url>/posts/652a6f9f.html</url>
    <content><![CDATA[<p>博客已开启RSS订阅功能，戳右下角工具按钮组里面RSS按钮</p>
]]></content>
  </entry>
  <entry>
    <title>Typecho博客迁移至转换Hexo</title>
    <url>/posts/ca31b60c.html</url>
    <content><![CDATA[<p>1202年了，我终于还是换到了Hexo ‘-’<br>其实我对这个框架并不陌生，早就有所了解，也试着搭建过，但是觉得不喜欢<br>每次更新发布都需要依赖Node.js环境编译静态资源，另外就是如果放Github Page的话访问速度实在有亿点点慢<br>在这之前本来打算趁着假期在就家，给闲置的电视盒子兼软路由兼游戏机的斐讯N1盒子装个armbian,在上面搭建typocho的，而且我连Ipv6 DDNS解析都弄好了，外网也能访问博客。不过有个缺点就是，域名解析ipv6地址之后只能用ipv6网络访问，那可不行啊。虽然套个cloudflare的免费cdn就可以解决访问问题，但是经过渣渣cdn线路解析后访问速度实在慢的离谱。寻思弄个国内的cdn吧，而且我又拍云账户本来是还有余额的，但在我服务器挂掉的这段时间，域名的备案也掉了。国内的CDN一般要求加速域名要有备案。。。<br>还有一个只要原因就是主题，我实在是没能找到比较中意的Hexo主题，直到我看见Keep @爱心<br>以及这个<br><a class="link"   href="https://juejin.cn/post/6943895271751286821" >如何使用 GitHub Actions 自动部署 Hexo 博客<i class="fas fa-external-link-alt"></i></a><br>这可太方便了吧，只要把源文件push到github就会自动编译成静态资源<br>然后试了下keep主题自带的cdn加速，效果竟意外地好，国内网络也基本上秒开<br>这下可了，咱搬<br>首要问题就是，如何把typecho的文章提取出来，文章数据都在sql数据库里<br>一番搜索之后发现这个项目<br><a class="link"   href="https://github.com/NewbMiao/typecho2Hexo" >typecho2Hexo<i class="fas fa-external-link-alt"></i></a><br>问题又来了，这得连接到一个存在本地的数据库，那咋怎<br>当然是开一台服务器，搭个宝塔，导入数据库备份，再操作咯（逃<br>花钱是不可能的<br>我当然选择在本地搭个宝塔不就完了吗<br><a class="link"   href="https://www.bt.cn/" >宝塔官网<i class="fas fa-external-link-alt"></i></a><br>然后就是安装LNMP环境，phphmyadmin导入备份的typecho数据库等常规操作<br>下载typecho2Hexo脚本<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/tepecho2hexo.443g4narvdmo.png"  alt="tepecho2hexo"><br>解压后cd进入文件夹<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/tepecho2hexo.6olsno8sby4g.png" ><br>将用户名密码数据库名表前缀改成自己数据库的<br>接着</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">php converter.php</span><br></pre></td></tr></table></figure>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/wq.1016idqtasm8.png" ><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/bt.6mywxa63iq2o.png" ></p>
<p>搞定</p>
]]></content>
  </entry>
  <entry>
    <title>使用WSL2安装配置深度学习环境</title>
    <url>/posts/90f74cc1.html</url>
    <content><![CDATA[<h3 id="1-为什么选择Windows-Subsystem-for-Linux（WSL）？"><a href="#1-为什么选择Windows-Subsystem-for-Linux（WSL）？" class="headerlink" title="1. 为什么选择Windows Subsystem for Linux（WSL）？"></a>1. 为什么选择Windows Subsystem for Linux（WSL）？</h3><p>在<strong>Linux系统</strong>下进行深度学习训练的<strong>效率</strong>通常高于<strong>Windows系统</strong>。经过测试，在 Windows Subsystem for Linux (WSL) 中的训练效率比直接在Windows上提升了大约<strong>25%</strong>。</p>
<p><strong>实体Linux</strong>对普通用户来说有着<strong>诸多不便</strong>，例如有些软件只能在Windows中运行，导致需要频繁<strong>切换环境</strong>，同时<strong>数据同步</strong>和共享也较为麻烦。</p>
<p>经过多个版本更新，微软的WSL2已非常完善。</p>
<h4 id="WSL的优点："><a href="#WSL的优点：" class="headerlink" title="WSL的优点："></a>WSL的优点：</h4><ol>
<li><strong>无需虚拟机或双系统</strong>：WSL允许用户在Windows环境中运行Linux命令行和应用程序，无需配置虚拟机或双启动系统。</li>
<li><strong>高性能</strong>：尤其是从WSL 2版本开始，微软实现了完整的Linux内核支持，通过轻量化虚拟化技术提供接近原生Linux的性能体验。</li>
<li><strong>集成性强</strong>：可以在Windows和Linux之间共享文件，互相调用应用程序。例如，WSL用户可以直接从Windows应用程序中调用Linux命令，反之亦然。</li>
<li><strong>支持更多Linux发行版</strong>：WSL支持包括Arch Linux、Ubuntu、Debian、Fedora等多个Linux发行版，用户可以根据需求选择最适合的发行版。</li>
</ol>
<p>因此，我选择使用<strong>Arch Linux on WSL2</strong>来搭建深度学习开发环境（CUDA + cuDNN + Conda + PyTorch）</p>
<h3 id="2-安装Arch-Linux-on-WSL2"><a href="#2-安装Arch-Linux-on-WSL2" class="headerlink" title="2. 安装Arch Linux on WSL2"></a>2. 安装Arch Linux on WSL2</h3><p>官方Wiki: <a class="link"   href="https://wiki.archlinux.org/title/Install_Arch_Linux_on_WSL" >Arch Linux on WSL<i class="fas fa-external-link-alt"></i></a></p>
<h4 id="2-1-安装WSL-Linux内核"><a href="#2-1-安装WSL-Linux内核" class="headerlink" title="2.1 安装WSL Linux内核"></a>2.1 安装WSL Linux内核</h4><p>对于<strong>Windows 11</strong>用户，可以通过一键安装来完成。</p>
<p>在具有<strong>管理员权限</strong>的 <strong>Windows命令行Shell</strong>中执行以下命令：</p>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--update</span></span><br></pre></td></tr></table></figure>

<p>此命令会为当前系统安装WSL Linux内核支持并启用相关功能。安装完成后需要<strong>重启</strong>。通过<code>wsl -v</code>命令可以查看WSL支持是否已启用。</p>
<h4 id="2-2-安装Arch-Linux"><a href="#2-2-安装Arch-Linux" class="headerlink" title="2.2 安装Arch Linux"></a>2.2 安装Arch Linux</h4><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--install</span> <span class="literal">-d</span> archlinux <span class="literal">--location</span> D:\WSLOS\Arch</span><br></pre></td></tr></table></figure>

<p><strong>参数说明</strong>：</p>
<ul>
<li><code>-d</code>：发行版本</li>
<li><code>--location</code>：自定义安装路径</li>
</ul>
<p>建议将安装路径设置为<strong>剩余空间较大的分区</strong>，因为系统体积会随着使用逐渐扩大。</p>
<p>命令执行完毕后，会自动进入到<strong>Linux shell</strong>环境。</p>
<p>Linux系统初始化及基础配置略过，以下为成功安装Arch Linux的截图：</p>
<h3 id="3-安装并配置深度学习环境"><a href="#3-安装并配置深度学习环境" class="headerlink" title="3. 安装并配置深度学习环境"></a>3. 安装并配置深度学习环境</h3><h4 id="3-1-安装CUDA"><a href="#3-1-安装CUDA" class="headerlink" title="3.1 安装CUDA"></a>3.1 安装CUDA</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo pacman -S cuda</span><br></pre></td></tr></table></figure>

<p>安装完成后，使用以下命令验证CUDA是否安装成功：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvcc -V</span><br></pre></td></tr></table></figure>

<h4 id="3-2-安装cuDNN"><a href="#3-2-安装cuDNN" class="headerlink" title="3.2 安装cuDNN"></a>3.2 安装cuDNN</h4><p>从<a class="link"   href="https://developer.nvidia.com/cudnn-downloads" >NVIDIA开发者网站<i class="fas fa-external-link-alt"></i></a>下载适用于Linux和相应CUDA版本的cuDNN。</p>
<p>因为我使用的是Arch Linux，我选择的版本为 <code>v9.13.0 for Linux X86_64 and CUDA 12.x</code>（Tarball格式）。</p>
<p>使用命令下载：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://developer.download.nvidia.com/compute/cudnn/redist/cudnn/linux-x86_64/cudnn-linux-x86_64-9.13.0.50_cuda12-archive.tar.xz</span><br></pre></td></tr></table></figure>

<p>下载完成后，进入文件所在目录并解压：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xvf cudnn*.tar.xz</span><br></pre></td></tr></table></figure>

<p>解压完成后，将cuDNN的头文件（<code>include</code>）和库文件（<code>lib</code>）复制到系统目录：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo <span class="built_in">mv</span> cudnn-*-archive/include/cudnn*.h /opt/cuda/include</span><br><span class="line">sudo <span class="built_in">mv</span> cudnn-*-archive/lib/libcudnn* /opt/cuda/lib64</span><br><span class="line">sudo <span class="built_in">chmod</span> a+r /opt/cuda/include/cudnn*.h /opt/cuda/lib64/libcudnn*</span><br></pre></td></tr></table></figure>

<h4 id="3-3-安装conda"><a href="#3-3-安装conda" class="headerlink" title="3.3 安装conda"></a>3.3 安装conda</h4><p>Conda 是一个开源的包管理系统和环境管理工具，可以在Windows、macOS和Linux上运行，适合安装和管理各种软件包及其依赖。</p>
<h5 id="Anaconda-vs-Miniconda"><a href="#Anaconda-vs-Miniconda" class="headerlink" title="Anaconda vs Miniconda"></a>Anaconda vs Miniconda</h5><ul>
<li><strong>Anaconda</strong>：包含多个常用的科学计算库（如numpy、scipy、pandas等），开箱即用，但体积大，更新较慢。</li>
<li><strong>Miniconda</strong>：仅提供conda包管理器和Python，体积小，灵活性高，适合长期维护。</li>
</ul>
<h5 id="3-3-1-安装Miniconda"><a href="#3-3-1-安装Miniconda" class="headerlink" title="3.3.1 安装Miniconda"></a>3.3.1 安装Miniconda</h5><p>Arch AUR仓库提供了<code>miniconda3</code>，使用以下命令安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yay -S miniconda3</span><br></pre></td></tr></table></figure>

<p>安装后，将conda的激活脚本添加到shell环境变量中：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&quot;[ -f /opt/miniconda3/etc/profile.d/conda.sh ] &amp;&amp; source /opt/miniconda3/etc/profile.d/conda.sh&quot;</span> &gt;&gt; ~/.zshrc</span><br></pre></td></tr></table></figure>

<h4 id="3-4-创建conda虚拟环境"><a href="#3-4-创建conda虚拟环境" class="headerlink" title="3.4 创建conda虚拟环境"></a>3.4 创建conda虚拟环境</h4><p>创建并激活一个名为<code>cuda_pytorch</code>的虚拟环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create --name cuda_pytorch python=3.13</span><br><span class="line">conda activate cuda_pytorch</span><br></pre></td></tr></table></figure>

<h4 id="3-5-安装PyTorch"><a href="#3-5-安装PyTorch" class="headerlink" title="3.5 安装PyTorch"></a>3.5 安装PyTorch</h4><p>访问PyTorch官网：<a class="link"   href="https://pytorch.org/get-started/locally/" >PyTorch Get Started<i class="fas fa-external-link-alt"></i></a>。</p>
<p>PyTorch官网不再推荐使用conda安装，而是只提供了pip安装方式。</p>
<h5 id="PyTorch-官方停止发布-Conda-包"><a href="#PyTorch-官方停止发布-Conda-包" class="headerlink" title="PyTorch 官方停止发布 Conda 包"></a>PyTorch 官方停止发布 Conda 包</h5><p>PyTorch 团队宣布将停止发布依赖于 Anaconda 默认包的 Conda 构建版本，主要原因是 Conda 构建的维护成本过高，且与 PyPI 的下载量差异较大，性价比不高。详情参考 <a class="link"   href="https://dev-discuss.pytorch.org/t/pytorch-deprecation-of-conda-nightly-builds/2590?utm_source=chatgpt.com" >PyTorch Developer Mailing List<i class="fas fa-external-link-alt"></i></a>。</p>
<h4 id="3-5-1使用pip安装-GPU-版-PyTorch"><a href="#3-5-1使用pip安装-GPU-版-PyTorch" class="headerlink" title="3.5.1使用pip安装 GPU 版 PyTorch"></a>3.5.1使用pip安装 GPU 版 PyTorch</h4><p>由于 PyTorch 官方镜像在国内下载速度较慢，建议使用科学上网环境或更换国内镜像。经验证，目前除了南京大学，清华源、阿里源或其他高校镜像站均没有提供最新的 PyTorch 源码。</p>
<p>同时，很多国内教程错误地将镜像源设置为默认的 CPU 版 PyTorch，导致安装的版本不支持 GPU。</p>
<h5 id="正确安装方式："><a href="#正确安装方式：" class="headerlink" title="正确安装方式："></a>正确安装方式：</h5><p>要安装支持 <strong>CUDA 12.8</strong> 的 PyTorch（版本 2.8.0），可以使用官方的安装命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu128</span><br></pre></td></tr></table></figure>

<p>对于国内用户，建议使用南京大学镜像站来加速下载：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip3 install torch torchvision --index-url https://mirrors.nju.edu.cn/pytorch/whl/cu128</span><br></pre></td></tr></table></figure>

<h4 id="3-6-验证PyTorch安装"><a href="#3-6-验证PyTorch安装" class="headerlink" title="3.6 验证PyTorch安装"></a>3.6 验证PyTorch安装</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;PyTorch version:&quot;</span>, torch.__version__)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;CUDA available:&quot;</span>, torch.cuda.is_available())</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;CUDA version:&quot;</span>, torch.version.cuda)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;GPU name:&quot;</span>, torch.cuda.get_device_name(<span class="number">0</span>) <span class="keyword">if</span> torch.cuda.is_available() <span class="keyword">else</span> <span class="string">&quot;No GPU detected&quot;</span>)</span><br></pre></td></tr></table></figure>]]></content>
  </entry>
  <entry>
    <title>动态分区刷GSI(通用镜像)的正确姿势</title>
    <url>/posts/da8abb0.html</url>
    <content><![CDATA[<h2 id="一、通用镜像-GSI-和动态分区"><a href="#一、通用镜像-GSI-和动态分区" class="headerlink" title="一、通用镜像(GSI)和动态分区"></a>一、通用镜像(GSI)和动态分区</h2><h3 id="通用镜像-GSI"><a href="#通用镜像-GSI" class="headerlink" title="通用镜像(GSI)"></a>通用镜像(GSI)</h3><p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/gsi.png"  alt="GSI"></p>
<span id="more"></span>

<h3 id="动态分区"><a href="#动态分区" class="headerlink" title="动态分区"></a>动态分区</h3><p>Google在Android10开始引入了动态分区（Dynamic Partitions）<br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/dynamic_partitions.png"  alt="Dynamic Partitions"><br>简单来说，就是把原来的system , vendor , product还有odm分区整合到了一起，构成super分区 ，在刷入设备的时候动态调整system等分区的大小,这个改变给让我们刷机、修改system分区等操作带来了困难，在Twrp里挂载后并不能直接刷入分区镜像</p>
<h2 id="二、动态分区刷GSI的方法"><a href="#二、动态分区刷GSI的方法" class="headerlink" title="二、动态分区刷GSI的方法"></a>二、动态分区刷GSI的方法</h2><ul>
<li>针对以下内容，<ul>
<li>解锁Bootloader ,开启ADB调试后食用</li>
<li>适合具备相关刷机、救砖经验者进行尝试</li>
<li>你的设备因此损坏，与我没有关系</li>
</ul>
</li>
</ul>
<h3 id="方法１-使用动态系统更新（DSU"><a href="#方法１-使用动态系统更新（DSU" class="headerlink" title="方法１.使用动态系统更新（DSU)"></a>方法１.使用动态系统更新（DSU)</h3><blockquote>
<p>Goole介绍：<a class="link"   href="https://source.android.google.cn/devices/tech/ota/dynamic-system-updates?hl=zh-cn" >WhatIsDSU<i class="fas fa-external-link-alt"></i></a></p>
</blockquote>
<h4 id="步骤1-启用-DSU-功能标志"><a href="#步骤1-启用-DSU-功能标志" class="headerlink" title="步骤1.启用 DSU 功能标志"></a>步骤1.启用 DSU 功能标志</h4><p>安卓10正式版及以上，可以在开发者选项中Feature flags &gt; settings_dynamic_system 中启用该功能。国内系统UI大多隐藏了，可以尝试用下面的adb 命令开启：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">adb shell setprop persist.sys.fflag.override.settings_dynamic_system true</span><br></pre></td></tr></table></figure>
<h4 id="步骤2-安装前的准备"><a href="#步骤2-安装前的准备" class="headerlink" title="步骤2.安装前的准备"></a>步骤2.安装前的准备</h4><p>通过DSU安装的GSI需要是可直接刷写的raw格式镜像，在开始前先检查你的镜像是不是raw格式<br>linux或者mac系统可以用<code>file system.img</code>命令查看</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">file system.img</span></span><br><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">system.img: Linux rev 1.0 ext2 filesystem data, UUID=91180515-3f1c-501d-888d-6f81f7ca3301 (extents) (large files) (huge files)</span></span><br></pre></td></tr></table></figure>
<p>若返回值是这样的就是raw格式<br><strong>若为稀疏格式（simg），可以使用以下命令把system镜像转为raw格式镜像</strong></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">simg2img system.img system_raw.img</span></span><br></pre></td></tr></table></figure>

<ul>
<li>一般gsi镜像都是simg格式，先检查转换</li>
</ul>
<h4 id="步骤3-将镜像打包为gz格式"><a href="#步骤3-将镜像打包为gz格式" class="headerlink" title="步骤3.将镜像打包为gz格式"></a>步骤3.将镜像打包为gz格式</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">gzip -c system_raw.img &gt; system_raw.gz</span><br></pre></td></tr></table></figure>
<p>*也可以直接用压缩工具压缩为gzip压缩包</p>
<h4 id="步骤4-用adb推到手机内置储存"><a href="#步骤4-用adb推到手机内置储存" class="headerlink" title="步骤4.用adb推到手机内置储存"></a>步骤4.用adb推到手机内置储存</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">adb push system_raw.gz /storage/emulated/0/Download/</span><br></pre></td></tr></table></figure>
<h4 id="步骤5-安装动态系统更新"><a href="#步骤5-安装动态系统更新" class="headerlink" title="步骤5.安装动态系统更新"></a>步骤5.安装动态系统更新</h4><ul>
<li>下面的命令要全部一起复制执行</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">adb shell am start-activity \</span><br><span class="line">    -n com.android.dynsystem/com.android.dynsystem.VerificationActivity  \</span><br><span class="line">    -a android.os.image.action.START_INSTALL    \</span><br><span class="line">    -d file:///storage/emulated/0/Download/system_raw.gz  \</span><br><span class="line">    --el KEY_SYSTEM_SIZE $(du -b system_raw.img|cut -f1)  \</span><br><span class="line">    --el KEY_USERDATA_SIZE 8589934592	</span><br></pre></td></tr></table></figure>

<p><strong>这里第五行的<code>$(du -b system_raw.img|cut -f1)</code>可直接用	system_raw.img	的大小代替，不然用windows刷会报错</strong><br>比如我的个system_raw.img的大小为2370265088</p>
<p><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/systemsize.png"  alt="systemimage size"></p>
<p>命令就改为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">adb shell am start-activity \</span><br><span class="line">    -n com.android.dynsystem/com.android.dynsystem.VerificationActivity  \</span><br><span class="line">    -a android.os.image.action.START_INSTALL    \</span><br><span class="line">    -d file:///storage/emulated/0/Download/system_raw.gz  \</span><br><span class="line">    --el KEY_SYSTEM_SIZE 2370265088  \</span><br><span class="line">    --el KEY_USERDATA_SIZE 2370265088</span><br></pre></td></tr></table></figure>

<p>然后你就会看到状态栏有正在安装的动态更新的提示，安装完重启进入第二个系统</p>
<p>小米10出厂安卓10，必然是支持DSU的</p>
<p>你可以用此方法尝试安装phh的aosp gsi</p>
<h3 id="方法2-通过fastbootd刷入GSI镜像（推荐）"><a href="#方法2-通过fastbootd刷入GSI镜像（推荐）" class="headerlink" title="方法2.通过fastbootd刷入GSI镜像（推荐）"></a>方法2.通过fastbootd刷入GSI镜像（推荐）</h3><p><code>fastbootd</code>——用户空间中的fastboot<br><strong>注意，fastbootd和fastboot是两个不同的模式</strong><br><img   src="https://cdn.jsdelivr.net/gh/chaptsand/blog_image_hosting@master/img/fastbootd.png"  alt="fastbootd"></p>
<ul>
<li>针对以下内容<ul>
<li>Android 10引入了 启动时验证 (AVB)，在刷写 GSI之前，先下载并刷入vbmeta.img以停用 AVB</li>
<li>以下步骤请使用最新版android-platform-tools进行操作</li>
<li>如果不知道说的是什么，建议先百度fastboot刷机</li>
</ul>
</li>
</ul>
<p><code>vbmeta.img</code>一般包含会随gsi镜像一起发布</p>
<h4 id="步骤1-重启到fastboot"><a href="#步骤1-重启到fastboot" class="headerlink" title="步骤1.重启到fastboot"></a>步骤1.重启到fastboot</h4><p>关机状态同时按电源+音量减 或者开启adb调试后<code>adb reboot bootloader</code></p>
<h4 id="步骤2-刷入vbmeta-img停用AVB验证"><a href="#步骤2-刷入vbmeta-img停用AVB验证" class="headerlink" title="步骤2.刷入vbmeta.img停用AVB验证"></a>步骤2.刷入vbmeta.img停用AVB验证</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot --disable-verification flash vbmeta vbmeta.img</span><br></pre></td></tr></table></figure>
<h4 id="步骤3-还原MIUI官方recovery"><a href="#步骤3-还原MIUI官方recovery" class="headerlink" title="步骤3.还原MIUI官方recovery"></a>步骤3.还原MIUI官方recovery</h4><p>目的是可以启动fastbootd<br>或者使用twrp里的用户空间fastboot也可以，注意如果twrp有这个功能可以不用刷回miui recovery</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot flash recovery recovery.img</span><br></pre></td></tr></table></figure>
<h4 id="步骤4-在fastboot模式下进入fastbootd模式"><a href="#步骤4-在fastboot模式下进入fastbootd模式" class="headerlink" title="步骤4.在fastboot模式下进入fastbootd模式"></a>步骤4.在fastboot模式下进入fastbootd模式</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot reboot fastboot</span><br></pre></td></tr></table></figure>
<p><strong>一定是要通过fastboot命令运行这句，才能启动到fastbootd模式</strong></p>
<ul>
<li>到这里如果卡在waiting for any devices就是没有安装小米usb驱动，设备管理器会识别你的手机，右键更新驱动程序。<br> PS：<a class="link"   href="http://bigota.d.miui.com/tools/xiaomi_usb_driver.rar" >小米USB驱动<i class="fas fa-external-link-alt"></i></a></li>
</ul>
<h4 id="步骤5-刷入system镜像"><a href="#步骤5-刷入system镜像" class="headerlink" title="步骤5.刷入system镜像"></a>步骤5.刷入system镜像</h4><p>安装好驱动，进入fastbootd之后，这时候fastboot命令已经可以识别system分区了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot flash system system.img</span><br></pre></td></tr></table></figure>
<h4 id="步骤6-清楚数据"><a href="#步骤6-清楚数据" class="headerlink" title="步骤6.清楚数据"></a>步骤6.清楚数据</h4><p>*会格式化data,防止加密（注意提前备份）</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot -w</span><br></pre></td></tr></table></figure>
<h4 id="步骤7-重启系统"><a href="#步骤7-重启系统" class="headerlink" title="步骤7.重启系统"></a>步骤7.重启系统</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fastboot reboot</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>GSI</tag>
      </tags>
  </entry>
  <entry>
    <title>在Manjaro上驱动BCM94360系网卡</title>
    <url>/posts/5b71d0ea.html</url>
    <content><![CDATA[<p>起因：因为折腾黑苹果，我将我电脑的网卡换成了博通BCM943360CS<br>Mac OS下免驱，Windows有现成驱动不过每次装完系统要自己装<br>很老的卡了，千兆速率，蓝牙4.1，总的来说勉强能用，比原装网卡强一些<br>当然咱也是Linux用户（逃</p>
<span id="more"></span>

<p>Ubuntu和Deepin，装好系统之后会检测到专有驱动，下载安装一下就能用了<br>但是在Arch系（Manjaro）上就得换个姿势了<br>装完系统，设置里找不到无线网络，也打不开无线开关<br>上操作</p>
<h3 id="1-更新系统软件"><a href="#1-更新系统软件" class="headerlink" title="1.更新系统软件"></a>1.更新系统软件</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pacman -Syu</span><br></pre></td></tr></table></figure>
<h3 id="2-安装对应的linux-headers"><a href="#2-安装对应的linux-headers" class="headerlink" title="2.安装对应的linux-headers"></a>2.安装对应的linux-headers</h3><ul>
<li>在系统设置-内核中查看自己的内核</li>
<li>然后安装对应的headers<br><code>sudo pacman -S linux-headers</code></li>
</ul>
<h3 id="3-安装博通网卡驱动"><a href="#3-安装博通网卡驱动" class="headerlink" title="3.安装博通网卡驱动"></a>3.安装博通网卡驱动</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo pacman -S broadcom-wl-dkms</span><br></pre></td></tr></table></figure>

<h3 id="4-重启"><a href="#4-重启" class="headerlink" title="4.重启"></a>4.重启</h3><p>重启之后就可以打开Wifi上网了</p>
<p>文章参考：<a class="link"   href="https://rowe98.github.io/2019/07/19/bodcom_failure/" >论如何在linux上正确驱动博通网卡<i class="fas fa-external-link-alt"></i></a></p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>如何在身边只有一台手机的情况下给电脑重装系统</title>
    <url>/posts/3795bd0c.html</url>
    <content><![CDATA[<p><strong>电脑系统突然崩了，身边又没有pe启动盘。送修是不可能送修的，这辈子都不可能送修。</strong></p>
<span id="more"></span>


<p>进不去系统咋办嘞？身边还没有第二台电脑可以做个启动U盘。<br>啦，正是前几天我刚经历了这样无奈的时刻。好在用手机成功救砖了，下面把经验分享出来。<br>可以尝试以下两种工具</p>
<p><strong>A.[Driverdroid]</strong><br><img   src="https://i.niupic.com/images/2020/03/29/7b0P.jpg"  alt="Driverdeoid"><br>需要root权限，对没有root权限的同学来说就不可以用啦。软件的使用也是比较简单，授予root权限之后，找到要引导的镜像，一路NEXT就可以了。</p>
<p><strong>B.[Etchdroid]</strong><br><img   src="https://i.niupic.com/images/2020/03/29/7b0Q.jpg"  alt="Etchdroid"></p>
<p>啦，软件标题上就说明了不需要root，那简直是很妙啊<br>贴个下载链接:<a class="link"   href="http://t.cn/A6ZfraVB" >兰奏云<i class="fas fa-external-link-alt"></i></a><br>项目地址:<a class="link"   href="https://github.com/EtchDroid/EtchDroid" >https://github.com/EtchDroid/EtchDroid<i class="fas fa-external-link-alt"></i></a></p>
<p>开始之前呢需要这几样东西<br>1.手机（这个肯定有啦）<br>2.Otg线（必须）<br>3.U盘  读卡器加TF卡也是可以的<br>4.下载一下.img格式的pe镜像</p>
<p>准备好了之后呢，就打开软件<br><img   src="https://i.niupic.com/images/2020/03/29/7b0S.jpg"  alt="点这个"><br>你看，这里明明写着写入iso的嘛，不过.iso真的是死也这不成功。我找了很多个.iso的pe镜像测试，都引导不起来。<br>也是找了好就才找到这个.img的pe镜像<br><img   src="https://i.niupic.com/images/2020/03/29/7b0X.jpg"  alt="选择引导镜像"></p>
<p>原贴地址:<a class="link"   href="https://www.coolapk.com/feed/17632027?shareKey=NDlkMmFhMDRlMGI0NWU4MDdkMjU~&shareUid=697466&shareFrom=com.coolapk.market_10.0.3" >酷安基佬的作品<i class="fas fa-external-link-alt"></i></a></p>
<p>然后选择USB驱动器<br><img   src="https://i.niupic.com/images/2020/03/29/7b11.jpg"  alt="点一下"></p>
<p>确认无误之后就点下面的图标<br><img   src="https://i.niupic.com/images/2020/03/29/7b13.jpg"  alt="确认"></p>
<p>然后会提示你会擦出U盘的数据（记得提前备份哦）<br>然后点写入镜像<br><img   src="https://i.niupic.com/images/2020/03/29/7b15.jpg"  alt="写入"></p>
<p>确认之后就会回到桌面，写入进度会在状态栏显示。<br>等写入成功之后，插上电脑，开机，选择U盘引导<br>Wow~Awsome 终于进pe了<br>电脑有救了<br>本篇完结</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>用Mac给安卓手机刷机</title>
    <url>/posts/dbb9570f.html</url>
    <content><![CDATA[<p>这就需要用到一个工具 <code>android-platform-tools</code><br>android-platform-tools是安卓的SDK平台工具</p>
<blockquote>
<p>SDK 平台工具版本说明</p>
</blockquote>
<p>Android SDK 平台工具是 Android SDK 的一个组件。 它包含与 Android 平台进行交互的工具，例如 adb、fastboot 和 systrace。开发 Android 应用时需要使用这些工具。如果您想要解锁设备的引导加载程序并为其刷入新的系统映像，则同样需要使用这些工具。</p>
<p>在Windows使用，用CMD命令就可以啦<br>在Mac上其实更方便，连驱动都不用装<br>安装可以去<a class="link"   href="https://developer.android.google.cn/studio/releases/platform-tools" >android开发者网站<i class="fas fa-external-link-alt"></i></a>下载<br>或者用Mac包管理神器homebrew一句命令行就搞定了</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">brew cask install android-platform-tools</span><br></pre></td></tr></table></figure>

<p><em><strong>手机到开发者选项打开ADB调试食用</strong></em></p>
<p>adb&#x2F;fastboot 常用命令</p>
<pre><code>ADB
#重启到recovery/fastboot模式
adb shell reboot recovery（fastboot）
#启用ADB服务
adb start-server 
#关闭ADB服务
adb kill-server 
#列出ADB设备
adb devices 
#连接局域网设备
adb connect xxx.xxx.xxx.xxx(设备的局域网ip)

fastboot
#刷入recovery/system/boot
//先切到目标文件夹，比如镜像在下载文件夹里//
cd /Users/liqianimage/Downloads
fastboot flash recovery recovery.img
fastboot flash system system.img
fastboot flash boot boot.img
</code></pre>
<p><strong>Mac上刷小米线刷包（代替Miflash）</strong><br>装好android-platform-tools<br>手机进入fastboot模式（关机按住电源和音量-），数据线连接电脑<br>解压线刷包</p>
<pre><code>#cd到线刷包目录
cd /Users/liqianimage/Downloads/umi_images_V11.0.10.0.QJBCNXM_20200226
输入sh (注意空格)
把线刷包内的sh刷机脚本拖到终端里回车
</code></pre>
<p>脚本运行，开始线刷</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Mac OS</tag>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>解决yum更新报错 CentOS7</title>
    <url>/posts/eaae739c.html</url>
    <content><![CDATA[<p>用centOS来做轻量级服务器的系统是比较舒服的,占用低，宝塔面板还有特殊优化</p>
<p>###建站第一件事就是先给阿里服务器重装个系统###<br>自带的定制系统多了很多用不到的组件，感觉很臃肿，遂自选重装了CentOS7。镜像市场找的，看介绍是原版镜像，但我感觉还是被改过的，不过好在没有阿里云盾那些组件</p>
<span id="more"></span>

<p>重装系统后，yum更新一下，发现报错了</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Error: initscripts conflicts with centos-release-7-3.1611.el7.centos.x86_64</span><br><span class="line">You could try using --skip-broken to work around the problem</span><br><span class="line">You could try running: rpm -Va --nofiles --nodigest</span><br></pre></td></tr></table></figure>

<p>看样子是这个软件包出问题了，遂想着卸载完事</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">yum remove -y initscripts</span><br></pre></td></tr></table></figure>

<p>执行成功但是更新还是报错，头更大了</p>
<p>正确的解决方式：修改yum配置文件，阻止yum更新内核与initscripts包<br><code>vim /etc/yum.conf</code></p>
<p><strong>在文件后面加上</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 更新的时候不更新内核</span><br><span class="line">exclude=kernel*</span><br><span class="line">exclude=centos-release*</span><br><span class="line">exclude=initscripts</span><br></pre></td></tr></table></figure>
<p><code>ESC :wq</code> 回车<br>退出保存</p>
<p>再执行<code>yum update -y</code>就不会报错了</p>
]]></content>
      <categories>
        <category>diy</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
</search>
